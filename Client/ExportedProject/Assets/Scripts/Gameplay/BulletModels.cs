using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Serialization;

public class BulletModels : Singleton<BulletModels>
{
	[Serializable]
	public class BulletMesh
	{
		public string name;

		public Mesh mesh;
	}

	[FormerlySerializedAs("IHCGLKBIAJN")]
	public List<BulletMesh> LMANNHINEPE;

	private Dictionary<string, Mesh> AFKHLGGGNNG = new Dictionary<string, Mesh>();

	protected virtual void OBEHLLBBEIL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh GGGFCBCJLCK(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh GetMesh(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh HMDLBEEGFMG(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh HNPIEHBNEGH(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void DDJBFEJLMBP()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void NOGLOKJINKN()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void OMDILMPFKII()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void EJFMJDFPGFE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh OOLKMFDFMNK(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void CIGLDDGDAPM()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh KFFLNGAPCOD(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void NIAEEICNNHL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected override void Awake()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void NLOODGLGPHE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void JLOKAFKICIM()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void FJOCJOOFJBI()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh BDOLCMFLEFL(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void OIKLJGMKMAE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void HNLOEOJMMJM()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh ENKMNCFJBFA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh PMOMJPMMDDH(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh JLJCFIBILJF(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh KABBNBAJANN(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void CPFDALHPFJI()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh CJKODDHIDDP(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh NJHDLKOLOHA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh MJAHFHDACEA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh CEFCJGDIOKJ(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh AJDOGFIPHII(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh KFBEJIMIIDI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh KDIEEKBDEKC(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void FIGOGIMJIIN()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void BOLPPMANCCC()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void CALILPEEAMB()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void FJJAEHJDKNL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void IBLPGJOHIAE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void OBPDFCOPCMK()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh MAHDDHCJPBD(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void HMMKNCDDDLH()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void OGFAHEALGFN()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void PIIFEPFCOJF()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void EALPHOPMAMP()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh MMOFJEENLHE(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh OLGNKHLBBML(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh JDPOBDOFKMJ(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void LCLEKFMLENA()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void CJOLAJGALCD()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void PNCPCHDFBOB()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh KKDHKFKGGHI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh MAABOKFNCLI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void HOINLOMCGFD()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void KALEMFKOGLE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void EKEHGBNPAFC()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void DKMHPLCCLKI()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void MLBAFICPECE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void LBMFKIDPCBL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh CFHMMIIMADE(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void OBPMHHGJFEL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh MDBIHIFPNME(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh CPLJPKJKPBP(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void ACPHOCKEOII()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh CFBEMNDOCLD(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh ILBCGIHHPAB(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh APODMDNKOMA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh EKKANHMOOAA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh CKALOEDGEHA(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh LMCONELFGPC(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh HFPFHBDNBFI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh DMDEACPCNBD(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void FOIKLCECNBL()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh OJMJGJJLGBI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void LFKHEBNFNEE()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void PDLNCCFMDFG()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void EJNJCILPBEC()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh OHHBLCJHOGB(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void IIPMNCCFNIA()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void JJEONLDHCLO()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh JIADGDAEJMO(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	protected virtual void PCDHCIDJJOB()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void GEPHFAECCII()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void NLLLLJGKNIC()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void AKKKDIBLICM()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	protected virtual void JDPDEKENPLD()
	{
		base.Awake();
		foreach (BulletMesh item in LMANNHINEPE)
		{
			AFKHLGGGNNG[item.name] = item.mesh;
		}
	}

	public Mesh FFAPJCCLKMI(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh GCPOBKFOKDL(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}

	public Mesh BHBMKLFHAPO(string CPFKNBDNGFB)
	{
		AFKHLGGGNNG.TryGetValue(CPFKNBDNGFB, out var value);
		return value;
	}
}
