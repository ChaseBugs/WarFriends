using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Serialization;

public class UnitModelLoaderWithAnimations : UnitModelLoader
{
	[FormerlySerializedAs("ALHIHIFGNCB")]
	public Animation LFPAGFBLOEF;

	public virtual void PHEIGPKKPGE()
	{
		base.GGCLMIMHKBD();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void FEADMLFNECA(GameObject NBHFGEAPACG)
	{
		base.EEIHAFJGAGM(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public virtual void IODFILFCENJ()
	{
		base.LDBLCPIGLOF();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void HGCHEPGNHNP()
	{
		base.GGCLMIMHKBD();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public override void LoadModel(GameObject NBHFGEAPACG)
	{
		base.LoadModel(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public virtual void ILIDKENNPLC(GameObject NBHFGEAPACG)
	{
		base.MMAGABBGMHD(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public override void OGCLFMNLNPD(GameObject NBHFGEAPACG)
	{
		base.EEIHAFJGAGM(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public virtual void KAMHJDKOCKA(GameObject NBHFGEAPACG)
	{
		base.OGCLFMNLNPD(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public virtual void HEOPOOMLPJF()
	{
		base.GFGEEFGMNDP();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public override void MMAGABBGMHD(GameObject NBHFGEAPACG)
	{
		base.DBEAHIEIHFK(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public virtual void JJOAHIOBOPO()
	{
		base.GEEKOICFEIB();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void GFPDOCDEGPA()
	{
		base.MHKKAFGKBJK();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void JBCGCMMGJMO(GameObject NBHFGEAPACG)
	{
		base.DBEAHIEIHFK(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public override void Reset()
	{
		base.Reset();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void EAJBJLCFJFP(GameObject NBHFGEAPACG)
	{
		base.BBIKDHCJEBH(NBHFGEAPACG);
		List<string> list = new List<string>();
		Animation component = NBHFGEAPACG.GetComponent<Animation>();
		foreach (object item in component)
		{
			AnimationState animationState = item as AnimationState;
			if (animationState != null)
			{
				list.Add(animationState.name);
			}
		}
		foreach (string item2 in list)
		{
			if (LFPAGFBLOEF.GetClip(item2) != null)
			{
				LFPAGFBLOEF.RemoveClip(item2);
			}
		}
		foreach (string item3 in list)
		{
			LFPAGFBLOEF.AddClip(component.GetClip(item3), item3);
		}
	}

	public override void GCEMCPODLNM()
	{
		base.CPOKHODGGLN();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public virtual void FGLAPPKJHBK()
	{
		base.ABBPOMCBIIH();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}

	public override void IPKNPABKAJD()
	{
		base.IPKNPABKAJD();
		List<string> list = new List<string>();
		foreach (AnimationState item in LFPAGFBLOEF)
		{
			list.Add(item.name);
		}
		foreach (string item2 in list)
		{
			LFPAGFBLOEF.RemoveClip(item2);
		}
	}
}
