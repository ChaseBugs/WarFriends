using UnityEngine;

public class WFX_Demo_Wall : MonoBehaviour
{
	public WFX_Demo CINIJFMBGPJ;

	private void CIEMJOPBEDO()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 596f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void BEONBBNPMOL()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 295f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void ELHFDIMMAGL()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 78f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void NKBCKOIPOGD()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 969f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void MKKECBPNPAM()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1825f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void MPLBHIIBDAI()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1383f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void KMCEIHDOFMG()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1957f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void PAGBODAILKL()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 156f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void DBIKKFPJDDC()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1994f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void NIECJLGNCLN()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 595f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void OnMouseDown()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 9999f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void CKKKIGOHNLB()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1360f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void JEHPBJOIKLF()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 365f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void JLKDAKDDFOL()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 887f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void CCENENKDEIH()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 233f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void CFDACHICBCP()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1089f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void AJOILDOKDML()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 423f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void FLELDDGJECH()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 750f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void FFIAJIBLEJL()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1894f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void HBCKOPIPHDK()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1804f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void GEEEDKCKLJA()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 341f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void DAAFEKOBMMD()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 406f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void OOMHLCDKAKO()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 569f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void IIEBGABJLOP()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 856f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void EKCBNDNJMIO()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1863f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void KKJLPLGNAOP()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1260f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void DIJMAKLJNPC()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 1467f))
		{
			GameObject gameObject = CINIJFMBGPJ.spawnParticle();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void EOGALKKEOEK()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 583f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}

	private void HPLDOJOIAHO()
	{
		RaycastHit hitInfo = default(RaycastHit);
		if (GetComponent<Collider>().Raycast(Camera.main.ScreenPointToRay(Input.mousePosition), out hitInfo, 102f))
		{
			GameObject gameObject = CINIJFMBGPJ.IGPGFHGFMOE();
			gameObject.transform.position = hitInfo.point;
			gameObject.transform.rotation = Quaternion.FromToRotation(Vector3.forward, hitInfo.normal);
		}
	}
}
