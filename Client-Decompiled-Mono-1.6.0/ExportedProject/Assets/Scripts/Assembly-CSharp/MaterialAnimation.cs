using UnityEngine;

public class MaterialAnimation : MonoBehaviour
{
	public Vector2 OAMOHLEEKFE;

	public Vector2 GNEJKKLGNOK;

	public Vector2 EMKHCBIAHDM;

	private Renderer IMAADHLAEPJ;

	private Material BHIPFFEOAMC;

	private Vector2 LPGJDNILPAN;

	private float PDDOJAMAOHB;

	private void AJHIIBAEKMB()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void ELJHCOHCAJI()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void PDJMNFGLIGO()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void JCLABHIBIGK()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void GEICOLPLFDL()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void JMHFNCOODKH()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void ANLFCJJCGMG()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void ADLJAKCCELG()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void BHIABNNGKFO()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void JBGDAOIDBGF()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void OOCJOBIIKAH()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void IDPOHKDKDNL()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void FJIDLPDMAFM()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void AOKEHEDCPFB()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void IDBHIEPOHHP()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void CJAHPFMAMKK()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void PMOIOADCKPO()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void PIMNKPOIFOM()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void LIMHNKKJODK()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void BLOBBBLIJPI()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void Start()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void COJACIBIPEF()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void FAHGDKLEKMN()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void Update()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void FLDCHPBPKND()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void KDCKHKNGILB()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void PEEFHOCPCLK()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void CFFHIADNNGA()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void GBMNDJIFNMP()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void OBBHECACANB()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void KBKILDENNLM()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void NPJFPBNCJNL()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void EBGBDDIADEJ()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void JOCHCGHAIGH()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void IGBCCGDMCDE()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void FPPLLLKNCIE()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void KLPFAIDKHHJ()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void HFFEELCNDOE()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void IAPBHPLNNNO()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void FGECFIBLFMD()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void INHEEMEAIDJ()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void JCKCDCFJAHP()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void MNFDMKFLAKO()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void CFHILPHMOOG()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void LHJAGNOOBAF()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void FMKLDIJPHIH()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}

	private void ILFMPFCEPNN()
	{
		IMAADHLAEPJ = GetComponent<Renderer>();
		BHIPFFEOAMC = IMAADHLAEPJ.sharedMaterial;
	}

	private void PMPDBJCPDHJ()
	{
		PDDOJAMAOHB += Time.deltaTime;
		Vector2 vector = new Vector2(Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.x) * EMKHCBIAHDM.x, Mathf.Sin(PDDOJAMAOHB * GNEJKKLGNOK.y) * EMKHCBIAHDM.y);
		BHIPFFEOAMC.mainTextureOffset = PDDOJAMAOHB * OAMOHLEEKFE + vector;
	}
}
