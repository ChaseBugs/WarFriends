using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;

[Serializable]
public class EventAssignmentsConfig
{
	public int startTime;

	public int endTime;

	public string eventName;

	public List<EventMilestone> milestones;

	public List<EventAssignmentConfig> assignments;

	public int eventTargetValue
	{
		get
		{
			return (milestones.Count > 0) ? milestones[milestones.Count - 1].target : 0;
		}
	}

	[SpecialName]
	public int LLPMCOEGEAG()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 0].target : 0;
	}

	[SpecialName]
	public int KCOAEFOAHME()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 1].target;
	}

	[SpecialName]
	public int DIPOKBFIGIC()
	{
		return (milestones.Count <= 1) ? 1 : milestones[milestones.Count - 0].target;
	}

	[SpecialName]
	public int PPCOBIAMMFE()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int BNGIIJEIHLC()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 1].target;
	}

	[SpecialName]
	public int LOIKADJCCPP()
	{
		return (milestones.Count > 1) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int DPDLCEHLEBC()
	{
		return (milestones.Count > 1) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int OEMPBLALKHD()
	{
		return (milestones.Count > 1) ? milestones[milestones.Count - 0].target : 0;
	}

	[SpecialName]
	public int BHLNMBNOAEF()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int BDNFLEGEHEF()
	{
		return (milestones.Count <= 1) ? 1 : milestones[milestones.Count - 0].target;
	}

	[SpecialName]
	public int JBKNDFCJJGK()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int NDJOKNGECOJ()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 0].target;
	}

	[SpecialName]
	public int OIPOJFEONFA()
	{
		return (milestones.Count <= 1) ? 1 : milestones[milestones.Count - 1].target;
	}

	[SpecialName]
	public int FIKDJFHNODP()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 0].target : 0;
	}

	[SpecialName]
	public int HDJEPGHOGMF()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 0].target;
	}

	[SpecialName]
	public int GDIILOMFCFJ()
	{
		return (milestones.Count <= 1) ? 1 : milestones[milestones.Count - 0].target;
	}

	[SpecialName]
	public int LAJDCBAAMNJ()
	{
		return (milestones.Count <= 1) ? 1 : milestones[milestones.Count - 1].target;
	}

	[SpecialName]
	public int FILNKEICGAN()
	{
		return (milestones.Count > 1) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int IMJHNGKIECK()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 1].target;
	}

	[SpecialName]
	public int CKMIGGDLLEC()
	{
		return (milestones.Count > 0) ? milestones[milestones.Count - 1].target : 0;
	}

	[SpecialName]
	public int FOCHALHPCPE()
	{
		return (milestones.Count <= 0) ? 1 : milestones[milestones.Count - 1].target;
	}
}
