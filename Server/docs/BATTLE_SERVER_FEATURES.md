# BattleServer completion checklist

Active objective: implement every BattleServer feature required by the recovered WarFriends 1.4.0 Client, with a working Client integration, then verify the complete result. This checklist does not redefine completion as the existing prototype.

Authority: [Server guide section 6](../../Server_guide.md), active `Clients/ExportedProject` scripts/assets, [communication inventory](client-communication-inventory.json), and executable source/tests. Backend wallet/account implementations belong to the separate Backend effort. Battle-owned allocation/control/result contracts and their integration proof remain in scope. Solo campaign may stay local, but its Photon-free adapter is in scope. No Docker; Windows development and Linux deployment; protobuf; no runtime Photon service.

Status meanings: **partial** = implemented evidence exists but the feature is not complete; **pending** = necessary work is not yet implemented/verified. A green kernel test alone does not close a gameplay feature. No feature below is declared production-complete yet.

## Dependency-ordered work

| ID | Required feature | Status / present evidence | Completion evidence required |
|---|---|---|---|
| B01 | Complete source contract and RPC migration ledger | Partial: source inventory, guide, 165 RPC definitions and 173 literal calls indexed; [first reviewed lifecycle/weapon/unit RPC migration slice](BATTLE_RPC_MIGRATION.md) maps replacement authority and remaining Client gaps | Every RPC, stream, room/property call and dynamic call classified; replacement and test linked; no unreviewed gameplay dependency |
| B02 | Validated immutable combat catalogs and loadouts | Partial: 132 exported sheets; prototype manifests; [the complete 66-family/3,426-stage weapon identity graph](WEAPON_BINDINGS.md) now pins every inventory index, controller, concrete weapon, animation family, muzzle/throw origin, ammo setup and projectile prefab; [11 rifle stat lanes / 586 stages and live scene behavior](RIFLE_STATS.md); [all eight SMG bindings / 398 stages and their recovered burst inputs](SMG_SOURCE.md); [all six pistol bindings / 306 stages and infinite-reserve behavior](PISTOL_SOURCE.md); [eight burst-LMG bindings / 398 stages and two-round runtime timing](LMG_SOURCE.md); [the dedicated 36-stage minigun package](MINIGUN_SOURCE.md) pins its non-reloadable pool, controller, projectile and timing inputs; [all eight sniper bindings / 428 stages](SNIPER_SOURCE.md) pin zoom/release gesture, muzzle and projectile inputs; [all seven bazooka bindings / 412 stages](BAZOOKA_SOURCE.md) pin hold timing, finite ammo, primary/secondary muzzles, missile trajectory and explosion inputs; [the M320 plus seven swipe-grenade families / 398 stages](GRENADE_SOURCE.md) pin finite ammo, gesture bounds, ballistic and explosion inputs; `BattleLoadoutPolicy` uses a static canonical identity pattern and rejects malformed, duplicate, over-capacity, and catalog-absent weapon, unit, and War Card identities at the admission boundary | Every playable weapon/unit/card/loadout bound to source rows, limits, hashes and ownership supplied by Backend; invalid inputs rejected |
| B03 | Scene identities, covers, static collision, navigation | Partial: five maps, 40 cover nodes, 60 paths, 200 Unity ray fixtures; each active dynamic collider retains its exact map-scoped source index, including duplicate hierarchy paths. `MapEntityIdentityPolicy` uses static canonical patterns and requires canonical map IDs, lowercase SHA-256 map revisions, and bounded positive source indices before registry admission. [All five baked NavMesh binaries, 9,658 Unity triangles, and 432 Unity-calculated spawn-to-Rusher path fixtures](NAVMESH_SOURCE.md) are source-pinned. | Exact map/content binding, runtime scene/entity identities, supported collision shapes/layers, dynamic geometry and source navigation behavior |
| B04 | Player initialization and animated hitboxes | Partial: 61-clip/1,065-frame catalog, 6,390 Unity rays, 11,715 weapon muzzle samples; [142-transform rig and 12 Unity blend comparisons](RIG_BLENDING.md); `PlayerHitboxRigPolicy` rejects duplicate source paths, empty rigs, and rigs with no active collision geometry; `PlayerAnimationStateMachine` gates legal idle/walk/run/aim/fire/reload/dead transitions and treats same-state retries idempotently while keeping dead terminal; eight recovered pose families including minigun/sniper idle and bazooka uncover/shoot/run/idle | All other weapon stances, source blends/aim/procedural transforms, muzzle/part transforms and death transitions match Client observations |
| B05 | Match allocation and admission | Partial: signed startup manifests, authenticated runtime registration with 32-match cap, host-issued retry-stable player grants and request-ID-bound reconnect grants with Date-bounded 120-second expiries, static canonical room/manifest identity patterns, case-insensitive duplicate-player rejection in the room handshake, durable terminal-ID reuse rejection, independent match clocks, authenticated prestart cancellation, terminal cleanup, and live two-peer UDP proof; control requests after shutdown return `control-unavailable` and queued requests drain rather than hanging; Mongo-backed `BattleAllocationStore` persists validated per-player card, buddy, and upgrade projections, and its bounded `GetMany` lookup fails closed on missing or duplicate participant records; Worker manifests can carry these projections and bind them before admission; when omitted, the Worker now fetches the exact ordered two-player projection through an HMAC-authenticated Backend allocation endpoint before registration and journals the enriched manifest; `BattleMatchControlClient` now signs exact canonical manifest bytes from Backend to Worker and strictly validates the returned match/hash, ordered roster grants, session identities, keys, endpoint, and expiry before any grant can be delivered; Mongo-backed `BattleGrantStore` atomically retains the complete two-player grant assignment with TTL expiry and digest/roster/session validation, while authenticated `POST /v1/battle/grant` and `BackendClient.MatchGrantAsync` return only the caller's roster-bound grant; `BattleMatchProvisioner` now performs Worker registration followed by idempotent durable two-grant publication, and the HMAC-protected Backend `/internal/battle/matches/provision` route exposes that complete control transaction to the future matchmaker; Mongo-backed `BattleMatchQueueStore` now owns retry-stable tickets and atomic pair rows, uses a unique multikey player index to prevent one player entering two matches across concurrent Backend processes, and pairs only identical server-owned compatibility keys without inventing absent rank/region policy; queue tickets now have an exact 120-second half-open lifetime with a Mongo TTL index, expired retries renew safely, candidate scans exclude expired authority, and cancellation is idempotent while refusing to dissolve an already durable pair; authenticated protobuf `/v1/battle/queue/join` and `/cancel` plus `BackendClient` methods expose only an empty client intent, while one validated deployment-owned `Battle__MatchmakingCompatibilityKey` selects the actual queue; an optional operator-reviewed `Battle__MatchManifestTemplatePath` now lets `BattleManifestFactory` replace only canonical `MatchId`, ordered `PlayerId`/scene-master identity, and Mongo-trusted allocation projections, after which paired join retries invoke the durable provisioner automatically; before provisioning, the factory now replaces each template participant's single combat weapon from an immutable allocator-side catalog loaded from the same combat manifest as the Worker; all 11 recovered assault-rifle families and 586 stages are source/revision validated, the durable first equipped source ID and upgrade select the row, unsupported weapons fail closed, and a cross-component test proves the Backend and Worker derive the same FAMAS stage; Backend roster extraction now uses the Worker's exact Pascal-case manifest contract; `content/local-rifle-match-template.json` is generated and validated from the pinned City/AK-47 stage-zero live-rifle fixture, with a repeatable test-runner generation command; authenticated queue admission now requires a present, fully validated `BattleAllocationStore` projection before creating a durable ticket, so two players cannot become permanently paired around absent/corrupt manifest authority; queue admission also requires a complete durable recovered-player presentation, and `BattleManifestFactory` now embeds validated ordered name/progression, four visual slots, equipped weapon source/index/upgrades, and equipped unit lanes from `LegacyPlayerStore` rather than client queue assertions; provisioning now attaches both ordered presentations to each authenticated protobuf match grant, `BattleGrantStore` validates and persists the complete view set, and the portable SDK validates ownership/order and exposes defensive view copies; portable `BackendClient.FindMatchAsync` now performs a 5-second-to-5-minute cancellation-bounded join poll, validates waiting/paired shapes and canonical roster IDs, fetches the authenticated player-only grant, and rejects a grant outside the paired roster before `MatchConnection` creation; a focused HTTP/protobuf test exercises waiting -> paired -> player-only grant across the real portable SDK method | Backend-authenticated assignment and loadout projection into the Worker, safe grant delivery, crash-safe allocation/retry, and real Client loading handshake |
| B06 | Player input, cover, movement, aim and stance | Partial: source cover paths, exact normal-mode speed 1, run poses and walking-shot overlays drive live moving hitboxes; `PlayerMovementPolicy` bounds finite per-tick displacement by stance speed and rejects dead-player movement; `CoverOccupancyRegistry` enforces exclusive, idempotent cover claims, canonicalizes player GUID casing across reconnects, supports explicit release, and exposes deterministic occupancy snapshot rows; [two Unity rigs receive moving UDP snapshots](LIVE_RIFLE_COMBAT.md) | Existing controls drive authoritative actions; legal transitions/occupancy, mode-specific speed/timing, reconciliation and full walking/transition animations verified |
| B07 | Equipped weapons and firing modes | Partial: finite-ammo cadence/reload; `WeaponFireModePolicy` now requires an explicit recovered source capability before accepting click/hold/burst/swipe/zoom modes; [all eleven rifle source animation families, first-shot scheduling and moving-fire ammo checks](LIVE_RIFLE_COMBAT.md); matchmaking derives the simulated weapon from the durable first equipped slot through the same 11-family/586-stage recovered rifle package the Worker validates; unsupported classes fail closed; signed manifests now bind canonically ordered rifle slots to exact recovered inventory indexes and stages, the Worker preserves independent ammo/reload/cadence state, reliable switching is movement-gated and idempotent, projectile source attribution survives later switches, and the live Unity bridge switches AK-47 to FAMAS with its recovered animation family; the SMG package validates all eight families/398 stages, exact inventory/muzzle/projectile bindings, three-shot size, source 0.11-second cadence/lock and serialized first-shot inputs; the live unscored mode now connects recovered default/pistol/QBZ2 poses, cover first-shot scheduling, animated muzzles, collision, damage, ammunition and reload cancellation, and a real Worker UDP test proves one Client command produces exactly three cadence-spaced rounds after release; mixed rifle/SMG/shotgun slots preserve independent catalog and ammunition authority, and a live Unity 2018/UDP audit switches the recovered rig AK-47 -> CPW -> P90, renders CPW pistol-family cover poses and P90 QBZ2 run, projects a three-round burst into the original inventory, then selects Desert Eagle and projects its single click/finite clip with immutable `int.MaxValue` reserve, then selects M249 and renders its family-15 pose with exactly two authoritative burst rounds; [the dedicated minigun path](MINIGUN_SOURCE.md) validates all 36 stages, its finite non-reloadable pool, 0.1-second spin-up and HEATTIME acceleration, owns press/update/release through typed protobuf, automatically fires on host time, publishes held state, and passes real UDP plus Unity family-4 pose/motor/ammunition/release proof; [the eight-family sniper path](SNIPER_SOURCE.md) validates 428 stages, owns press/update/release and cooldown arming through typed protobuf, publishes the strict scope gate, fires one release projectile, and passes real UDP plus Unity family-10 pose/scope/ammunition proof; [the seven-family bazooka path](BAZOOKA_SOURCE.md) validates 412 stages, owns strict hold/cancel through typed protobuf, finite ammunition, straight/curved missiles and M202 delayed real/fake sequencing, publishes targeting state, and passes MatchEngine plus live UDP proof; [the eight-family grenade path](GRENADE_SOURCE.md) validates 398 stages and 289 Unity pose samples, accepts distinct typed M320/swipe evidence, recomputes swipe strength, delays launch on host animation time, and consumes finite ammo through real UDP | Complete grenade rendering, flamethrower and remaining special classes, denial reconciliation and Android/two-device proof |
| B08 | Projectile lifecycle and server shot randomness | Partial: `ProjectileAuthorityPolicy` now validates canonical owner/entity identity, finite origin/direction, normalized aim, speed, spawn tick, and bounded lifetime before flight; [live rifle mode](LIVE_RIFLE_COMBAT.md) connects source cover/run poses, moving hitboxes, muzzle, bullet flight and damage through real two-peer UDP; rifle, shotgun, SMG, pistol, LMG, minigun, sniper, bazooka and grenade now create catalog-bound host projectiles; sniper retains its source speed-80 slow flight, bazooka retains speed-4 straight/curved flight, collision delay, expiry and M202 fake/real distinctions, and grenade reproduces source 30/45-degree ballistic launch, gravity, drag and falling-only collision in MatchEngine | Complete remaining special projectile classes, critical/spread and source fake/real distinctions, temporal/source comparison and normal Client presentation |
| B09 | Shotgun, explosions and special ammunition | Partial: `ExplosionAuthorityPolicy` now validates server-owned explosion identity, origin, radius, damage, and bounded pellet count; [live shotgun mode](SHOTGUN_SOURCE.md) validates nine bindings/464 stages, animated muzzles, source cover delay, host overlap and atomic fast-pellet volleys; two-client UDP verifies player/shield damage and identical ordered events; bazooka impacts now resolve current animated player hitboxes, inner/outer radial damage, shield occlusion, overtime coefficients and M202 half damage, and mutate source-bound shield/barrel lifecycles with recovered coefficients and synchronous barrel chains; grenade impacts use stage-specific radii, the recovered 0.7 player-radius coefficient, quadratic falloff, behind-shield ratio, shield and barrel-chain lifecycles and ordinary/Molotov event identity, while named FLASH/POISON/SMOKE remain the serialized Frag branch; a focused live MatchEngine M320 launch mutates a source barrel through the shared chain authority | Backend ownership allocation, live two-process Client impact-effect proof and remaining special ammunition |
| B10 | Player health, armor, healing and statuses | Partial: bounded server-owned status-effect leases now enforce duration, immunity, and stacking rules; `StatusEffectState.Expire` prunes expired leases and exposes a bounded immutable snapshot count; each authoritative `MatchEngine` player now owns that state and prunes it on simulation ticks; authoritative healing clamps to maximum health; bounded armor mitigation is applied before damage; source damage-order/refund/death kernel; live stationary rifle collision changes SDK health snapshots | Kevlar/accessories/perks, durations/stacking/immunity, faithful normal Client health/death presentation and complete mode integration |
| B11 | Shields, environmental destructibles and parts | Partial: [live opt-in shield lifecycle](SHIELD_SOURCE.md) covers all 44 rank HP rows, 66 weapon ratios, host damage, collider suppression, repair, snapshots and events; live overtime disables repair and destroys the eight shields under the pinned `DestroyShieldsInOverTime=1` source policy. [Barrel source kernels](DYNAMIC_ENTITIES.md) bind 29 scene objects, 43-level health, shot/death, signed scene-master rank, runtime layer 8, 58 Unity scene probes, 180 player-shape probes, part origins, explosive coefficients and a pinned depth-first barrel-chain preview with one ordered effect stream and a source-checked City beam no-op HP boundary, moving-player chain damage preview, live overtime-aware barrel damage using source 0.88/0.60 coefficients, and stale-plan-checked barrel-state commit and live source projectile barrel damage and chain commit verified through two UDP peers, a separate MTU-bounded authenticated barrel-state poll with portable source-ID/revision projection | Backend-proven rank/ownership, card-activated parts, normal battle-controller scene-object binding and barrel/explosion presentation, positive moving-player blast where source movement permits it, spawned-entity end-to-end proof, plus other environmental destructibles, stable entity generations |
| B12 | Stable entities, spawning and despawning | Partial: [87 source-bound dynamic collider identities and 29 scene-bound barrels](DYNAMIC_ENTITIES.md) distinguish separate barrels with identical hierarchy paths; `StableEntityIdentityPolicy` now requires nonzero bounded IDs and generations and rejects stale generation observations; bounded `StableEntityRegistry` provides common spawn, deterministic ID-sorted snapshot, generation-bound lookup/despawn, capacity, and owner cleanup; the barrel file-ID map is pinned and validated at Worker startup; paged entity rows carry 30 Hz covered-Rusher position ticks and batches carry a server tick without consuming roster revision | View association, owner/team, bounded capacity, ordered spawn/despawn/reconnect snapshots, and full Client presentation |
| B13 | Army deployment and spawn eligibility | Partial: [source-pinned deathmatch army extraction](../content/recovered-army-deployment.json) covers 24 scene families/48 runtime options; [101 Unity-verified spawn points](../content/recovered-army-spawn-points.json), [160 pinned Rusher slots](../content/recovered-army-rusher-points.json), occupied and pending vehicle-route and Rusher-slot checks, source Minigunner alternating point selection, trusted equipped/new IDs, Classic unit-type and first-new offer selection, two-peer UDP offer/deploy/spawn-record and paged reconnect proof; host-only source death/refund transition; opt-in original army-card poll/command bridge compiles and passes a Unity Editor audit | Backend ownership/tier/upgrades, remaining mode offer rules, combat AI and damage proof, normal scene bootstrap, unit visuals and result authority |
| B14 | Infantry AI and special abilities | Partial: pinned upgrade-lane stat and firing-parameter composition, all 24 serialized base and source-sheet runtime speeds, common infantry NavMeshAgent settings, trusted perk-adjusted speed and accuracy ledgers, separately trusted HP and source `scaleDamage` factors, private spawned-unit health/damage ledgers, host vitality/death transition, and reconnect health projection. The combat package pins both serialized player shot-target lists and reproduces the Client's ordered nearest-target filter, including strict tie order. The two direct-child Body targets follow host player placement, and a spawned walking Rusher selects the nearer one from its host position. Its Moving target follows the sampled animated player rig; a separate match-owned projection binds the active Rusher, reserved opponent-cover slot, and that target, invalidating on unit death and transferring to a new source slot after the inactive-cover delay. [Rusher arrival/state rules, all five Unity NavMesh triangulations, and 2,863 Play Mode motion samples across eight traces on all five maps and the three stock ground-infantry speeds](NAVMESH_SOURCE.md) are source-pinned; all four simultaneous spawned Rushers obtain a planar-covered route from their host positions to their occupied source slots, preferring pinned Unity corners for exact complete spawn/point pairs, and death invalidates it. A pinned two-agent Park trace keeps source-corner host motion within 0.250 units of Unity where generic routing diverges by 2.371 units; broader avoidance remains unproven. A match-owned motion state advances every tick and publishes host positions for covered spawned Rusher routes, pauses with match simulation, retains its old route through opponent cover departure until a validated retarget, and is removed on death. Its underlying candidate, using covered shortcuts and bounded mesh-vertex visibility search, reaches the source arrival gate on all eight sampled trajectories (median planar error 0.010–0.224, maximum 0.247), including the prior City detour; an allied source-radius tick gate keeps mature four-Rusher host pairs at least 0.34 units apart, guided by a pinned nine-frame staggered Unity trace; a bounded covered NavMesh detour now clears a deterministic retarget stall behind an allied Rusher (focused seeds 0–20 pass), and a pinned three-agent Unity retarget probe shows that nominally stopped blockers drift under avoidance, leaving this static-obstacle detour without a covered route from the same initial snapshot; a separate pinned opposing-player crossing proves that a global wait gate deadlocks; bounded right-hand lateral steering now reaches both slots with 0.482-unit minimum separation, while general avoidance and combat collision remain unproven; the match-owned state candidate covers the source 0.5-second Rusher settle/shot-delay gate and projects a current Moving target only after that gate; a tested source 1.5-second inactive-cover timer, ordered four-slot permutation selector, and 24 recovered directed near-neighbour links per map now drive a fixed-seed live retarget and later arrival gate with three active Rushers; after removing the other host units, the same Rusher transfers again without leaking the intermediate slot; other cover-change cases, [six pinned Rusher base shot definitions and a tested server-owned attack scheduler and bounded match-owned shot intents and entity-owned projectile flight and live player-health impacts; serialized Rusher weapon references are now pinned; runtime weapon identity resolution, shield/barrel effects, and Client event integration remain open](RUSHER_ATTACK_SOURCE.md). The combat revision now pins the zero-tick Flamethrower, nine-tick ordinary stand-shoot, and 30-tick SWAT shield-unhide windups from their exact legacy clips; the host also applies the Rusher-specific random 2-4 second post-batch cooldown instead of retaining generic upgrade timing. `PrepareToShoot` now latches one Moving target position for the whole batch, and windup/firing pauses the inactive-cover retarget clock; completion, death, and a later committed transfer clear that authority. [The army flamethrower path](FLAMETHROWER_SOURCE.md) now owns its 0.5-second shot cadence, six strict 0.35-second pulse intervals, recovered cone/range/falloff, current animated player overlap, Flame damage and bounded lifecycle; the pinned enemy-rig and weapon transform chains place its rest-pose muzzle under authoritative facing, and a real two-peer UDP test deploys it through navigation to shot, pulse impact, and player health loss with distinct owner/target authority; a validated per-unit coefficient applies recovered perk accuracy after upgrade-stage probability composition, while durable Backend entitlement derivation remains open; shoot-animation muzzle sampling, non-player targets and Client effects remain open. Collision damage remains unproven. | Every recovered soldier behavior's target/navigation/attack/ability/death state machine and deterministic server-owned decisions |
| B15 | Ground vehicles | Partial: deployment startup validates the seven recovered vehicle families, exact source movement speeds and serialized projectile contracts; `VehicleEntityRegistry` enforces recovered per-family generation caps and generation-bound destruction cleanup, binds per-entity `VehicleAttackState` contracts, and exposes server-owned vehicle health/damage/destruction with generation cleanup, begin/advance/commit attack transitions; `VehicleProjectileFlight` now runs committed real shots on the authoritative fixed tick and validates collision traces; vehicle impacts emit replayable impact events; `VehicleMotionPolicy` bounds finite per-tick displacement by recovered source speed; collection-specific spawn routing excludes infantry paths; `VehicleTargetPolicy` validates bounded finite target geometry and opposing participant identity; `VehicleImpactResolver` validates entity-bound range and trusted damage; protobuf now carries a sanitized `VehicleAttackCommand`, and `MatchEngine` accepts it only after ownership/faction/range validation; snapshots expose identity/unit/owner/generation/position/attack state, with generation-bound lifecycle events and owner cleanup before reconnect grace | Tank, car, buggy, transporter and mech behaviors: paths, turret projectile flight, parts, passengers and full Client destruction presentation |
| B16 | Air units | Partial: the seven recovered air families are source-validated with dedicated spawn collections, generation caps, projectile contracts, fixed-tick three-dimensional motion, source-probability attack scheduling, and intent-bound collision/damage validation; `AirBattleEntity` composes motion, attack, and server-validated impact; bounded `AirEntityRegistry` provides deterministic spawn/duplicate/lookup/tick/despawn, owner cleanup, and lethal removal; `AirEntityHealthState` is covered by focused tests; `MatchEngine` now owns the registry, admits only authenticated participants during nonterminal phases, advances air motion/attack on the authoritative 30 Hz tick, exposes explicit server-side registration/removal hooks, validates opposing admitted damage through the air health kernel, projects sorted air identity/position/health rows in every protobuf `MatchSnapshot`, emits replayable `AIR_SPAWNED`/`AIR_DESPAWNED` lifecycle events carrying the authoritative entity key and position, and removes owner air entities with lifecycle events before reconnect grace | Deployment/drop rules, durable result/statistics integration, Client-side event consumption, and normal air-unit presentation |
| B17 | Turrets and other deployables | Partial: recovered HeavyTurret and LandMine placement rules are represented by a server policy; LandMine selects at most three distinct opposing hiding slots and HeavyTurret requires a finite trusted placement; bounded `DeployableRegistry` now provides server-owned spawn, detached deterministic snapshots, duplicate rejection, one-shot trigger, lifetime expiry, and cleanup, with focused radius-bound impact and lifecycle tests; `MatchEngine` now admits deployables only for authenticated participants, advances lifetime on the authoritative tick, removes expired/triggered rows, projects identity/owner/card/position/state into protobuf snapshots, emits replayable spawn/despawn events, removes a reconnecting owner's deployables before entering the reconnect grace state, exposes authenticated trigger rules (opponents activate mines; owners activate turrets), and routes trusted radius-bounded deployable damage through the existing authoritative player damage/death kernel | Source placement, source ammo/cooldown rules, and normal Client presentation |
| B18 | War Card activation and effects | Partial: authoritative card inventory reservation is idempotent by request ID and rejects duplicate inventory rows, unavailable cards, and malformed IDs; the explicit catalog leaves unknown cards closed; `BattleCardSelectionState` now validates trusted card identities, freezes one selection per player, rejects conflicting retries, and preserves identical request idempotency; protobuf now carries a bounded `SelectCardsCommand`, including typed normal/special/elite unit-upgrade indexes and buddy-card IDs recovered from `FinishChoosingCardsRPC`; the UDP endpoint sanitizes those fields and `MatchEngine` validates lane lengths/ranges and exact equality with trusted manifest upgrade lanes before committing the selection; `BattleBuddySelectionState` validates immutable Backend-provisioned buddy ownership and idempotent retries; snapshots project the complete selection; validated requests enforce per-kind target, positive damage/heal/shield parameters, and nonzero status/modifier duration before entering bounded `WarCardEffectRuntime` leases with idempotent effect IDs, server-owned stacking, fixed-tick expiry, owner cleanup, server-owned inventory reservation/release, and authoritative damage/heal/status/shield/ammo/modifier resolution; `MatchEngine` now admits card effects for authenticated participants, expires them on the authoritative tick, removes them with owner disconnect cleanup, projects effect identity/owner/card/target/lifetime in protobuf snapshots, emits replayable `CARD_EFFECT_APPLIED`/`CARD_EFFECT_EXPIRED` events, and exposes deterministic owner-scoped active-effect queries for combat kernels | Every playable card's source eligibility/target/parameters, Backend wiring for buddy ownership, spawn/modifier behavior, reservation/consumption result contract, and live Client effect application |
| B19 | Battle objectives and interactables | Partial: recovered crate/skill-shot objective kinds have bounded server-owned progress and target completion; `BattleInteractableRegistry` now admits only registered source entity IDs and routes authenticated player events into the objective state, preserving event-ID idempotency with a bounded 4,096-event ledger; unknown objective events and overlarge increments are rejected in focused tests; objective and interactable registries now expose detached deterministic progress/target and source-identity snapshots; `MatchEngine` now supports authenticated objective-state/interactable registration, idempotent progress recording, replayable `OBJECTIVE_PROGRESS` events, and per-player progress/target/completion rows in protobuf snapshots | Full source crate/skill-shot/mission object catalog, collection/destruction triggers, and all objective modes |
| B20 | PvP and DeathMatch rules | Partial: explicit `BattleModeCapabilityPolicy` closes unsupported mode strings at admission; `BattleTerminalPolicy` validates supported end reasons, winner identity, scoring eligibility, and death winners (including `player-killed`); prototype forfeit/death/deadlines; a server-owned idempotent hit/kill credit ledger rejects malformed and duplicate event IDs; live rifle/shotgun modes enter source DeathMatch overtime at their allocator clock; protobuf snapshots now carry an explicit authoritative `overtime` flag alongside the replayable overtime event | Recovered loading/start and all end reasons, exact both-player disconnect sequence, simultaneous events, victory/defeat, rematch, and normal Client callbacks |
| B21 | War Arena battle rules | Partial: server-owned Arena event identity and one-active-battle-per-player admission policy are implemented; bounded Arena lives/wins, idempotent terminal settlement, first-outcome replay lookup, and deterministic flawless-first ranking scoped to the exact current event and revisioned snapshot validation, bounded settlement history/conservation, and monotonic compare-and-set publication and idempotent server-authored reward claims are implemented; `MatchEngine` now supports authenticated Arena configuration/settlement, projects immutable event/player/lives/wins/terminal/revision state in protobuf snapshots, emits replayable `ARENA_SETTLED` events, and validates those events in the Client SDK; combat modifiers, ranking, rewards, and full persistence remain open | Source modifiers, participant state and end evidence; lifecycle/economy handed to Backend through its contract |
| B22 | Shared co-op missions | Partial: server-owned bounded wave progression, deadline timeout, terminal failure state, coherent heroic modifier validation, two-participant ready admission, immediate lifecycle validation on ready/leave/wave mutations, and authoritative participant departure failure are implemented with idempotent completion events and focused tests; `MatchEngine` now supports authenticated co-op configuration/admission/readiness/progression, advances deadline failure on its tick, and projects wave, deadline, participant, started/failed/completed state plus immutable heroic/enraged/no-respawn modifiers in protobuf snapshots | Source waves, AI, objectives, timers, stars, failure/success and both players' callbacks |
| B23 | Local campaign/tutorial compatibility | Partial: Photon-free local campaign policy now owns bounded mission start/completion state, rejects duplicate active starts, requires an active mission before completion, and never contacts the Worker; Unity adapter and idempotent local objective progress is covered; deterministic active/completed mission and objective-progress snapshots are now exposed for local progression handoff, with bounded objective history and state validation; full tutorial progression remains open | Preserve local gameplay and progression handoff through Photon-free local adapter; do not move all 75 solo missions to Worker unnecessarily |
| B24 | Authoritative statistics and kill credit | Partial: server-owned hit/kill credit IDs are idempotent, terminal statistics validate hit/kill and spawn/loss conservation at MatchEngine termination, `BattleStatisticsLedger` aggregates explicit army transitions with a bounded total event capacity, `BattlePerformanceLedger` bounds card/objective credit IDs while preserving idempotent duplicates and authoritative duration, and `BattleRibbonLedger` bounds/idempotently records ribbon IDs; host projectile impacts credit opposing-player or live enemy-shield contacts even when shield HP does not change; `MatchEngine` now records GUID-backed card/objective credits, binds bounded ribbon recording, exposes authoritative credit totals and sorted ribbon IDs in protobuf snapshots, and starts/ends the performance duration ledger from the actual Running/terminal transitions | Full source MatchStats, all weapon/entity hits/kills/spawns/vehicles/cards/crates/ribbons, correct attribution and durable result handoff |
| B25 | Complete match lifecycle and terminal evidence | Partial: waiting/countdown/running/ended/aborted; `BattleTerminalPolicy` is enforced by `MatchEngine.End` and now requires scored winners to belong to the admitted player set; terminal ticks and command cursors freeze, every terminal transition advances the snapshot revision even without a tick, the performance ledger closes at terminal, and snapshots/outbox carry authoritative duration ticks; `RematchRequestState` now records authenticated terminal rematch requests idempotently and returns `fresh-allocation-required` without reopening the immutable match; the durable outbox rejects mismatched end ticks and impossible phase/reason/winner combinations | Loading/cancel/timeout/rematch allocation integration/ending/settlement/closed paths, complete source statistics and immutable scored result evidence, crash behavior defined and tested |
| B26 | Durable result outbox and Backend handoff | Partial: validated result handoff checks match identity, manifest digest, and conserved terminal statistics; `CompleteBattleEvidenceValidator` now validates one immutable envelope containing terminal statistics, duration, unique bounded ribbons, and bounded nonnegative card/objective credit totals before Backend settlement; acceptance is digest-idempotent and conflicting replays are rejected, retained acceptance IDs are bounded, and `MatchEngine.BuildCompleteBattleEvidence()` and `BuildBattleResultHandoff()` produce validated terminal envelopes from authoritative counters; unscored terminal protobuf snapshots persist before UDP reply; interrupted writes recover and malformed authority blocks startup; Backend `BattleResultStore` durably accepts the first `(matchId,digest,snapshot)` tuple in Mongo, returns `already-accepted` for identical retries, rejects conflicting digests, exposes a defensive validated read, and is reachable through the optional HMAC-protected `/internal/battle/results/accept` endpoint with a 65,536-byte bounded envelope; `BackendResultForwarder` signs and submits the same bounded envelope from the Worker side with the assigned server ID, and `TerminalOutbox.ForwardPendingAsync` processes a bounded deterministic page and acknowledges only accepted/idempotent results, rejecting malformed success codes; Backend rejects a validly signed result from a different configured server ID; `BattleResultStore.Prune` provides bounded, validated retention cleanup with a minimum 30-day replay window; `ReconcileScored` atomically marks one accepted digest scored and is idempotent on retries, with strict match/digest identity validation; `/internal/battle/results/score` exposes that transition through the same assigned-host HMAC boundary. | Economy settlement consumer and wallet mutation integration remain open; no Battle wallet writes |
| B27 | Authenticated protobuf transport | Partial: shared bounded packet-MTU policy, nonzero 256-bit signing-key validation, a separately required distinct 256-bit control-plane key for allocator/reconnect/cancel/result operations, HMAC UDP, MTU/queue/session bounds, and `SessionKeyRotationPolicy` requiring nonzero 256-bit keys with strictly increasing generations; `SessionKeyRing` now owns defensive key copies, atomic generation rotation, and preserves the active key when a rotation is rejected; duplicate/noncanonical and unknown-field envelopes fail before session/replay state; live malformed-probe proof | Complete abuse/resource-exhaustion matrix, reviewed deployment encryption policy, cross-platform live validation |
| B28 | Reliable commands and combat events | Partial: bounded monotonic reliable event window now applies `ReliableEventRetentionPolicy` on append, acknowledgement, and restore, keeps first-retained/last-published/acknowledged cursors inside the fixed replay capacity, exposes a bounded deterministic `ReplayRange`, persists cursors through atomic `ReliableEventCursorStore` replacement, and persists validated payloads through bounded atomic `ReliableEventPayloadStore` replay with explicit stale-cursor rejection for snapshot fallback; canonical `BattleEventFactory` emits validated spawn/despawn/objective/effect event kinds for server-owned entities | Loss/reordering stress tests, normal Client consumption, and complete event coverage |
| B29 | Snapshots and interpolation | Partial: bounded monotonic snapshot buffer and tick interpolation validate finite poses and reject out-of-order samples; `SnapshotTickPolicy` bounds accepted tick gaps; `EntitySnapshotPolicy` binds stable identity, owner, normalized rotation, finite position, and server tick before projection; `SnapshotBaseline` now validates complete revisions before any mutation, rejects duplicate incoming IDs, and emits only changed entity state; `SnapshotChunkAssembler` validates bounded revision chunks, rejects duplicate entity IDs both within and across chunks, deterministically assembles complete snapshots, and atomically applies only complete revisions to a baseline; `SnapshotInterestFilter` rejects malformed and duplicate entities before radius selection and emits selected rows in stable ID order | Normal controller/render ownership, all entities, chunk/baseline recovery integration, interest scope, input acknowledgement and existing Client interpolation/reconciliation |
| B30 | Reconnect and recovery | Partial: reconnect cursors validate retained snapshot revision and event replay boundaries; `ReconnectSnapshotPolicy` now requires a mutually retained entity revision, event cursor, and bounded server tick; `ReconnectRecoveryCoordinator` produces deterministic incremental-replay or full-snapshot plans and can derive the event boundary directly from durable `ReliableEventPayloadStore` authority; reconnect grants expire, rotate, bind to the authenticated player, clear stale bindings when an unbound grant replaces them, reject malformed oversized tokens, and revoke prior sessions; trusted bounded HMAC reissue is idempotent by canonical request ID, and reconnect grant comparison uses constant-time token equality | Backend player-authenticated grant delivery, full entity/event cursor recovery integration, normal Client loading/running/terminal recovery, host-failure behavior |
| B31 | Client room/loading compatibility | Partial: Photon-free room handshake validates match identity, manifest hash, and two distinct participants before loading; loading command validation uses a static canonical manifest pattern; both clients must report the exact manifest and scene readiness before start; readiness has a server-owned deadline and explicit prestart cancellation and observable loading phases; `SelfHostedRoomLifecycle` emits connecting/loading/ready/running/closed transitions through `PhaseChanged`, accepts the authoritative `ready` response, consumes authoritative snapshots through `Observe` so reconnect/poll state can advance or close the lifecycle without local inference, and suppresses duplicate phase callbacks; a focused regression assertion covers repeated waiting snapshots; `SelfHostedBattleClient.RoomPhaseChanged` forwards those transitions to Unity scene controllers, and Unity 2018.3 batch compilation now passes with no C# errors | `SelfHostedBattleClient.MatchmakeAndConnect` now owns the authenticated portable queue -> paired roster -> player-only grant -> UDP room connection chain with bounded timeout, concurrent-operation exclusion, and server queue cancellation; Unity 2018.3 batch compilation passes after refreshing the networking plugins. The protobuf battle routes now accept either the replacement account token or the recovered client's unique hashed legacy bearer session while preserving its canonical player ID; `SelfHostedBattleClient.MatchmakeWithRecoveredSession` reads `GameLoginManager.accessToken` and uses serialized loopback endpoint/timeout settings, and Unity 2018.3 compilation passes. `GameControllerDeathMatch.StartRandomMatchMaking` and retry now invoke the self-hosted session path directly, create one persistent adapter when absent, derive host/client presentation ownership from the authoritative ordered roster, and feed the recovered `OnJoinedNewRoom` callbacks without creating a Photon room; failures enter `GameCancelled` and retain the existing warning UI. Unity 2018.3 compilation passes. The recovered card-confirm button now submits semicolon card IDs and buddy identities through typed protobuf `SelectCardsAsync`, checks the authoritative `cards-selected` result, and never sends `FinishChoosingCardsRPC` in self-hosted mode; the current rifle template deliberately sends empty army upgrade lanes. Unity 2018.3 compilation passes. The host now records card submission independently from list length, so a valid empty selection still projects `cards_selected=true`; `SelfHostedBattleClient.CardsSelectedByBoth` raises once from authoritative snapshots for the next loading transition. The full Windows suite passes 55,121 assertions and Unity compilation passes. A transport-neutral `BattlePlayerPresentation` contract is now validated and embedded in allocator manifests from durable legacy player state, with fail-closed queue admission for missing/malformed visuals, inventory, ownership or upgrades. Both roster views now reach the authenticated Client in `MatchConnectionGrant`, and `SelfHostedBattleClient.PlayerViews` exposes the SDK-validated immutable copies. The normal DeathMatch join now constructs local/opponent `PlayerProperties`, weapons, visuals and unit-upgrade lanes from those views, assigns stable roster network IDs and server-derived original-domain defend positions, creates the opponent without a `PhotonPlayer`, advances card readiness through the authoritative snapshot event, starts scene loading without Photon queue calls, and waits for the authoritative Running phase before the recovered opening presentation continues. Unity 2018.3 compilation passes. Remaining: prove this complete path with two actual Clients, bind normal gameplay inputs/rendering, and migrate non-deathmatch paths. |
| B32 | Client gameplay/visual compatibility | Partial: validated Client battle-event projection bounds sequence, entity, kind, and position data; event validation now permits only canonical spawn/despawn/objective/effect/shot/impact/death/terminal/overtime families; SDK `MatchEventConsumer` rejects stale, gapped, malformed, and over-latest protobuf pages before dispatching contiguous callbacks, and `MatchConnection.PollAndConsumeEventsAsync` binds that validation to the live cursor; lifecycle validation now requires authenticated actor/entity identity and effect/objective metadata for air, deployable, card, and objective events; SDK adapter binds named rifle view rigs and [replay-safe combat event callbacks](BATTLE_EVENTS.md); [two active recovered Unity rigs received live UDP poses and confirmed health loss](LIVE_RIFLE_COMBAT.md) | The normal DeathMatch scene now installs `SelfHostedDeathMatchBridge` after authoritative Running admission: it disables both legacy `PlayerController` and `SoldierAnimationController` simulation writers, binds both recovered rigs to server poses, forwards recovered tap/hold aim, horizontal swipe, and empty-clip reload as typed fire/cover/reload intents, projects authoritative health/death/ammunition into the original player/HUD objects, and maps terminal snapshots through `MatchOutcomeProjection` into the recovered finish flow. Unity 2018.3 compilation passes. The production bridge now also passes the live Worker/Unity Editor audit with two rendered recovered rigs, both legacy writers disabled, authoritative health, recovered-rifle clip/reserve ammunition, poses, cover movement, walking fire, and replay-safe event delivery. The same live Editor path now switches the original recovered inventory from AK-47 slot 0 to FAMAS slot 2 through authenticated UDP, applies the authoritative active slot/ammo, and renders FAMAS run/fire behavior. The normal bridge recognizes `PlayerHoldWeapon`, sends typed bazooka hold/release intents, projects finite ammunition and drives the recovered targeting HUD from authoritative state. It recognizes `PlayerSwipeWeapon`, reproduces the Client screen-to-cover-plane raycasts, sends raw world swipe endpoints/time, routes M320 clicks separately, and renders stable grenade clip IDs 60-71. Host-owned grenade position/velocity snapshots now drive script-free copies of the recovered projectile mesh and authoritative impact events remove the object and select the recovered medium/Molotov effect; a Unity audit matched 867 source collider/muzzle samples plus that projectile mesh path for 868 checks. Remaining: live two-process grenade/effect proof, rendered HUD-label and switch-control feedback, remaining weapon classes, all units/vehicles/cards/effects, and no unrestricted RPC relay |
| B33 | Photon dependency retirement | Partial: `Server/tools/audit_photon_runtime.py` audits gameplay scripts/scenes while excluding retained Photon implementation binaries/source used only for serialized type resolution; [photon-runtime-audit.json](photon-runtime-audit.json) records 293 references and 8 remaining application service calls, [photon-replacement-ledger.json](photon-replacement-ledger.json) maps each call to its self-hosted replacement target, and `verify_photon_replacement_ledger.py` passes the one-to-one mapping. `SelfHostedPhotonCompatibility` now exposes explicit disconnect/master/region-capability semantics without Photon types or cloud calls; region probing fails closed because allocation selects the self-hosted endpoint. `SelfHostedBattleClient` owns this bridge and the lifecycle; `SelfHostedRpcRouter` provides a bounded allow-list for recovered loading/time/presentation RPC equivalents and rejects arbitrary method names; `GameControllerOnline.ResyncTime`, its loading-complete path, and its camera-animation completion path now dispatch through that router during self-hosted play, the legacy Photon loading wait is bypassed, and terminal presentation no longer sends `FinishGameMultiplayerRPC`; `PhotonConnectionManager` now short-circuits Connect/Disconnect, room join/random matchmaking, War Arena matchmaking, reconnect, and offline-room entry when an active self-hosted session exists; `GameController.TestDisconnect` routes through the bridge; recovered online controllers now read the authoritative self-hosted server-tick clock through `GetNetworkTime`, participant counts through `GetPlayerCount`, and match identity through `GetBattleId`; the per-frame Photon flush, match-start/visibility/open property writes, and master-client reassignment are skipped for active self-hosted sessions; Unity 2018.3 batch compilation passes after refreshing the `War.Client`/`War.Shared` plugin binaries. | Wire remaining bounded RPCs into normal controllers, replace remaining player-object RPCs, cover room/property/RPC behavior through the UDP adapter, then pass the full scene/prefab and offline asset audits |
| B34 | Cross-platform deployment and operations | Partial: Program validates bounded port, match capacity, exact fixed 30 Hz tick rate, and MTU values once and registers one immutable `BattleRuntimeConfig`; dependency injection passes that exact snapshot into `NetworkWorker` before binding, while standalone construction uses the same frozen resolver; `/health/ready` follows UDP bind/shutdown, and Windows smoke gates Battle startup on it and now resolves `mongod` from PATH with an explicit missing-executable diagnostic. A self-contained `linux-x64` Battle test runner passed 54,662 assertions under Ubuntu 22.04 WSL2, including 32-match UDP flood coverage. The standalone Worker passed Linux restart and force-killed-allocation recovery probes. The systemd battle unit now provisions a writable persistent outbox under `ProtectSystem=strict`; an adapted unprivileged WSL2 system service passed ready/start/stop with that state directory. The Worker now exposes loopback `/health/metrics` counters for ingress, handling, drop causes, pending queue depth and capped tick backlogs; a running-process HTTP probe and live-flood counter assertions passed. Shutdown now withdraws readiness, closes the control queue, returns 503 for new operations and resolves queued allocations; Windows and Linux tests cover this drain. | Production-host installation and loaded live-match service restart, full config validation, production metrics collection/alerting, bounded maintenance and graceful drain |
| B35 | Capacity, fairness and hostile-network behavior | Partial: bounded per-sender rate windows now reject bursts at live UDP ingress and reset deterministically by tick, and the limiter registry is capped at 4,096 remote endpoints; the full Windows suite passes 55,121 assertions after integration; queue/rate/MTU/command bounds and a 16-pending-datagram limit per UDP endpoint; a 32-match authenticated router test admits 64 peers, advances every match, and preserves 31 running matches when one ends; a live Worker drives all 32 concurrent matches over 64 authenticated UDP sockets, handles eight concurrent 32-peer poll bursts, rejects 256 distributed MAC-valid unknown-field packets, and continues serving all 32 matches during 4,096 malformed datagrams from one sender and a separate 64-sender burst; malformed envelopes and well-formed match packets for unknown session IDs are rejected before entering the shared queue; separate 64-sender unknown-session and wrong-endpoint bursts leave all 32 matches responsive; admitted endpoint ownership is checked before queueing; one correctly bound endpoint also sent 4,096 authenticated polls while its opponent and 31 other matches continued to answer; an eight-second 32-match live-UDP run completed 61 rounds of 32 concurrent polls on both Windows and WSL2 Linux, with every match advancing at least 20 ticks/s and whole-round p95 below 40 ms on loopback; a real UDP proxy drops one shot reply and reorders two poll replies, with exactly one shot and stale-reply rejection | Long-duration real-UDP load/soak and production-host tick budgets, sustained authenticated floods, broader network latency/loss/reordering, deterministic outcomes and no cross-match effects |
| B36 | End-to-end release validation | Pending | Two actual Clients complete each multiplayer mode with original UI; offline tutorial/campaign, Android build/runtime, all asset/runtime audits and Linux host verified |

## Current execution order and close-out rule

1. Continue B07-B09 with the next recovered weapon projectile family and its normal Unity presentation. Keep results unscored until authority and Client presentation are complete.
2. Expand weapon variants and world/entity destruction (B09-B12), then army/AI/vehicles/cards/objectives (B13-B19).
3. Complete mode rules, statistics and durable result/control contracts (B20-B26), with Backend integration limited to those boundaries.
4. Complete transport/event/snapshot/reconnect and normal Client adapters alongside each gameplay feature (B27-B33), rather than postponing all Client work.
5. Complete deployment, load, hostile-network, and end-to-end gates (B34-B36). Audit every row against current source and runtime evidence before declaring the goal complete.

For each feature: inspect its Client caller and serialized data; implement host authority plus the necessary Client bridge; add source-derived positive/negative/replay tests; exercise the real Worker/Unity path; record limitations and evidence here. Never mark a whole feature complete because only its isolated math kernel or synthetic fixture passes.






