# Verification checkpoint — 2026-09-19

- Host: Windows, .NET SDK 10.0.401, native MongoDB 8.3.
- Solution build: zero warnings and zero errors.
- Protocol executable: 17 targeted assertions and 1,000 seeded random malformed-packet authentication checks passed.
- Native smoke: 44 live assertions passed. Covers Mongo account registration, duplicate-name rejection, login, password failure, profile persistence, old bearer-session invalidation, anonymous rejection, content type and exact username boundaries, connection grants, portable SDK hello/ping, raw UDP authentication, idempotent hello retry, endpoint binding, duplicate ping rejection, invalid MAC rejection, and closed-ticket replay rejection.
- Smoke data/processes are isolated. Last passing local run: `Server/.local/smoke-8cb5aacd92064b9fa257dd5f42e5eeac`. The script stopped its backend, worker and Mongo processes afterward. Credentials/signing keys are never printed.
- Linux-x64 backend and worker publish succeeded; Linux execution/systemd startup was not verified at this checkpoint (see the later BattleServer runtime check below).
- No Unity integration, Android build, real battle, Photon removal, chat migration, economy settlement, load benchmark, or production readiness is claimed.

The first smoke readiness loop needed `Invoke-WebRequest -UseBasicParsing` for Windows PowerShell 5. That portability issue was corrected and the complete smoke rerun successfully.

## BattleServer Linux runtime check — 2026-09-20

From Windows, `dotnet publish Server/tests/War.Battle.Tests/War.Battle.Tests.csproj -c Release -r linux-x64 --self-contained true -o Server/.local/linux-battle-tests` succeeded. The published ELF was executed under Ubuntu 22.04 in WSL2 from `/mnt/e/Github/Mobile/WarFriends/Server/.local/linux-battle-tests` and passed 49,165 Battle assertions, including two-peer UDP and recovered-content comparisons. This verifies Linux runtime behavior of the Battle test executable and its in-process Worker. It does not verify a production Linux host, systemd, MongoDB on Linux, or a full release.

The standalone Worker also published self-contained for `linux-x64` and ran under the same WSL2 distribution. With a disposable 32-byte signing key, loopback UDP/control ports, and isolated result spool, it passed two process start, HTTP `/health/live`, SIGTERM graceful-stop, and same-spool restart cycles. The two Worker logs are in `Server/.local/linux-host-probe-e7ZDPr/` and record UDP bind, HTTP 200 health responses, and clean application shutdown. This does not prove startup with loaded production content, systemd supervision, or a production network.

A second standalone Linux probe accepted a valid authenticated `POST /internal/matches` allocation in prototype mode, confirmed its active-allocation journal file, force-killed the Worker, restarted against the same spool, and received the recovered match through authenticated `POST /internal/results/poll`. The restart log explicitly reports one unscored `host-crash` recovery. Evidence is in `Server/.local/linux-loaded-restart-nI8gti/`. The probe did not attach two real Clients or resume combat state; it verifies durable unscored crash handoff.

After adding UDP-bound `/health/ready` with immediate withdrawal on stop, the full Windows solution built with zero warnings/errors; Battle tests passed 49,166 assertions and the native Mongo/HTTP/UDP smoke passed 74 live checks using readiness for Battle startup. The final republished standalone Linux Worker passed both ready/SIGTERM restart cycles (`Server/.local/linux-host-probe-yB51oS/`) and the force-killed allocation recovery probe (`Server/.local/linux-loaded-restart-gttr5a/`) using `/health/ready`.

The `deploy/warfriends-battle.service` template was corrected to provision `/var/lib/warfriends-battle` as a writable systemd `StateDirectory` for its outbox under `ProtectSystem=strict`. An adapted copy used the self-contained ELF, a disposable signing key, and WSL2's unprivileged `nobody` account; `systemd-analyze verify` accepted the service directives, and systemd started it, observed HTTP 200 from `/health/ready`, created the outbox, and stopped it with exit status zero. This is a local service-boundary test, not proof of the final `warfriends` account, framework-dependent runtime installation, or production filesystem/network policy.

Live rifle/shotgun DeathMatch slices now enter source overtime instead of aborting at the configured duration. The host emits one ordered transition, destroys all eight shields under the pinned recovered constant, disables repair, and keeps combat running. The full Windows Battle suite and self-contained Ubuntu 22.04 WSL2 suite each passed 49,171 assertions; protocol checks passed 1,018 and the native Windows Mongo/HTTP/UDP smoke passed 74. This does not validate the original Client's overtime UI or other battle modes.

A subsequent two-peer authenticated UDP test confirmed that both clients receive the same running overtime snapshot and replay the same one `OvertimeStarted` and eight `ShieldDestroyed` events. The full Windows and self-contained WSL2 Battle suites each passed 49,181 assertions. The server implementation did not change after the prior 1,018 protocol and 74 live smoke checks.

The portable .NET Standard battle SDK now projects a validated terminal snapshot to the recovered `GameController.GameEndReason` values for each roster member: lethal `Win=2`/`Killed=1`, and forfeit or reconnect loss `WinByForfeit=3`/`Forfeit=5`. Running overtime and aborted matches project `None=0`. The Windows Battle suite passed 49,187 assertions and the self-contained WSL2 Linux suite passed 49,188 assertions, including corrupt-lethal and roster-boundary checks (the poll count can vary). Original Unity UI wiring remains separate work.

The durable terminal outbox now rejects a `player-killed` record unless its winner is alive and its loser dead, and rejects a `forfeit`/`opponent-disconnected` record with either player dead. The same validator runs on publication and spool replay. Windows Battle tests passed 49,190 assertions; the self-contained WSL2 Linux runner passed 49,191 (the poll count can vary). These checks protect unscored evidence consistency; scored result authority and Backend acceptance remain open.

Every terminal transition now increments `MatchSnapshot.StateRevision`, including same-tick trusted cancellation and controlled shutdown. The new assertions verify that a terminal phase cannot retain its preceding snapshot revision when no simulation tick advances. Windows Battle tests passed 49,191 assertions and the self-contained WSL2 Linux runner passed 49,192 (the poll count can vary). Normal Unity snapshot consumption is still open.

The shared UDP decoder now rebuilds and byte-compares the outer protobuf `Packet` before the Worker can route it to a session or replay window. This rejects a MAC-valid unknown outer field, alongside the existing duplicate/default-field check. Protocol tests passed 1,019 checks; the full Windows and self-contained WSL2 Linux Battle suites each passed 49,191 assertions after the change (the poll count can vary). Nested message fields retain their separate per-body validation; broader hostile-network coverage remains open.

Terminal outbox publication and restart replay now share player/shield validation: finite positions and health, coherent combat health/death, nonnegative ammo, bounded command and geometry identities, cleared reconnect state, and either zero or eight coherent source shields. A test rewrites an acknowledged record with a valid checksum and nonfinite player position and confirms startup rejection. Windows and self-contained WSL2 Linux Battle suites each passed 49,196 assertions. The outbox remains unscored; complete result statistics and Backend settlement are separate work.

Optional rifle pose data in a terminal result now retains the host projection's one-to-four normalized clip layers, bounded sample time, known clip IDs, finite animation times/weights, and unit quaternions. Forged future ticks, unknown clips, and nonfinite rotations fail publication; the shared outbox validator also runs on restart replay. Windows Battle tests passed 49,200 assertions and the self-contained WSL2 Linux suite passed 49,201 (the poll count can vary).

The live barrel impact path now supplies its overtime state to the already source-checked player blast resolver. The pinned normal/overtime explosive coefficients are 0.88/0.60; the chain regression confirms identical ordered hits but greater remaining HP in overtime. Windows Battle tests passed 49,202 assertions and the self-contained WSL2 Linux suite passed 49,203 (the poll count can vary). This fixes the host damage branch, while a positive reachable live barrel-to-player blast and original Unity presentation remain unproven.

Battle allocation and reconnect now reject issuance when the fixed 120-second ticket would expire beyond the supported `DateTimeOffset` Unix range. Signed-ticket validation and the portable SDK also reject that expiry, while the last representable issuance second remains accepted. The rejected reconnect leaves the session generation available for a normal request. Windows Battle tests passed 49,207 assertions and the self-contained WSL2 Linux suite passed 49,208 (the poll count can vary).

A full-capacity authenticated router exercise now allocates 32 matches, admits 64 distinct session grants, accepts each participant's manifest-bound ready command, and advances all matches into running. One forfeit publishes one terminal while the other 31 matches continue at their own tick. Windows and self-contained WSL2 Linux Battle suites each passed 49,591 assertions. This covers bounded router state isolation, not real-UDP throughput, hostile flood fairness, or soak behavior.

An actual `NetworkWorker` run now allocates eight concurrent matches through its control loop and drives 16 separately bound UDP peers through signed admission, ready, running snapshots, and one forfeit. Authenticated replies retain the correct session and match; the other seven remain running. Windows and self-contained WSL2 Linux Battle suites each passed 49,696 assertions. This is a real-socket routing and isolation proof, not a full-capacity throughput or long-duration soak benchmark.

The same real-socket Worker exercise now runs at the configured maximum of 32 concurrent matches and 64 separately bound UDP peers. All admitted and reached running; one forfeit left the other 31 running. Windows Battle tests passed 50,008 assertions and the self-contained WSL2 Linux suite passed 50,007 (the poll count can vary). This closes the immediate full-capacity routing proof, while sustained throughput, tick budgets under load, hostile floods, loss/reordering, and soak behavior remain open.

At full capacity, eight rounds of 32 concurrent authenticated UDP polls now require every match to remain running, retain its session/match identity, and advance its simulation tick during the burst. Windows Battle tests passed 50,521 assertions and the self-contained WSL2 Linux suite passed 50,520 (the poll count can vary). This is a short burst test; sustained tick-budget and hostile-network measurements remain open.

The same 32-match Worker test now sends 256 MAC-valid UDP datagrams with an unknown outer protobuf field and a high sequence number, then successfully polls every match with its next legitimate sequence. All 32 matches remain running and their reliable command cursors remain at ready command 1. Windows Battle tests passed 50,586 assertions and the self-contained WSL2 Linux suite passed 50,585 (the poll count can vary). This verifies one bounded malformed burst, not an unbounded flood or fair service under sustained hostile traffic.

The Worker now limits queued datagrams to 16 per UDP endpoint. A separate sender streamed 4,096 MAC-valid packets with an unknown outer protobuf field while all 32 admitted matches polled through their own sockets. Every match remained running with its reliable command cursor unchanged. The Battle suite passed 50,619 assertions on Windows and 50,618 in self-contained WSL2 Linux (the poll count can vary). This tests one-source queue isolation during a bounded burst; multi-origin floods, long-duration throughput, and latency budgets remain unproven.

Malformed protobuf envelopes are now rejected at UDP ingress before occupying the shared queue. A further real-socket run sent 4,096 MAC-valid unknown-field packets from 64 separate UDP source sockets while all 32 admitted matches polled; every match remained running without a command-cursor change. The Battle suite passed 50,652 assertions on Windows and 50,649 in self-contained WSL2 Linux (poll counts vary). Valid-packet multi-origin floods, long-duration throughput, and latency budgets remain unproven.

Well-formed match packets for unknown session IDs are also rejected at ingress, while signed startup-manifest handshakes and newly registered grants retain their admission path. A separate 64-sender, 4,096-packet unknown-session burst ran against 32 live matches; retry-aware UDP polls reached every match with unchanged command state. The Battle suite passed 50,683 assertions on Windows and 50,685 in self-contained WSL2 Linux (poll counts vary). Known-session multi-origin floods, sustained throughput, and latency budgets remain open.

The ingress filter now checks a published snapshot of admitted session-to-UDP-endpoint bindings. Another 64-sender, 4,096-packet burst used a valid match session key from the wrong source endpoints; all 32 matches answered retry-aware polls and kept their reliable command state. Windows passed 50,717 assertions and self-contained WSL2 Linux passed 50,715 (poll counts vary). Correct-endpoint authenticated floods, long-duration load, and latency budgets remain open.

One correctly bound player endpoint then sent 4,096 authenticated poll datagrams while the opponent and the other 31 matches polled through their own sockets. All continued running with unchanged reliable command cursors. Both Windows and self-contained WSL2 Linux Battle suites passed 50,750 assertions. This covers a bounded noisy-client burst and cross-match service, not sustained throughput, latency budgets, or loss/reordering behavior.

An eight-second live-UDP load segment held 32 matches running and completed 61 concurrent 32-peer poll rounds on both Windows and self-contained WSL2 Linux. Every match advanced at least 20 server ticks per second. The whole-round 95th-percentile loopback response time was 34.9 ms on Windows and 37.0 ms on Linux; these are local measurements, not internet latency claims. The full Battle suites passed 54,656 and 54,648 assertions respectively (poll counts vary). Long-duration soak, production-host budgets, and broader packet-loss and reordering patterns remain open.

The SDK's real UDP proxy now drops a shot reply, delays the next poll reply, and delivers a retry response before that delayed reply. The shot remains applied exactly once, and the following SDK poll ignores the stale response. Windows passed 54,657 Battle assertions and self-contained WSL2 Linux passed 54,653 (poll counts vary). This verifies one controlled loss-and-reordering pattern; variable packet loss, jitter, and broader network conditions remain open.

The Battle Worker now counts received/handled UDP datagrams, malformed and unowned drops, per-endpoint and full-queue drops, pending queue depth, and capped tick backlogs. Live flood tests assert nonzero ingress and drop counters with queue depth within its 512-packet bound. A running standalone BattleServer returned HTTP 200 from loopback `/health/metrics` with the expected initial zero counters. Windows passed 54,659 Battle assertions and self-contained WSL2 Linux passed 54,785 (the number of load-test poll rounds varies). External metrics collection, alerts, and production-host behavior remain open.

Control operations now require UDP-bound readiness. Shutdown atomically withdraws readiness and closes the bounded control queue; later match registration, reconnect, and prestart cancellation calls return `control-unavailable` (HTTP 503), while queued allocations resolve as either completed registrations or `control-unavailable` within the test deadline. Windows passed 54,726 Battle assertions and self-contained WSL2 Linux passed 54,788 (load-test poll rounds vary). Loaded production-host drain and Backend retry behavior still need end-to-end validation.

The recovered `AIObject.SyncUpgradesRPC` damage scale is now an independently validated, allocator-owned army input. Tests mutate the original manifest array after validation, reject nonfinite scales, compare the host's spawned-unit damage with the recovered normal/special stage sum times the trusted scale, and verify confirmed death removes the private damage entry. The full Battle suite passed 54,662 assertions on both Windows and self-contained WSL2 Linux. This proves stat composition and lifecycle only; AI targeting, attacks, and live hit authority remain open.
