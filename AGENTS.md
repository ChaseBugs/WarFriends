# WarFriends repository guidance

## Recovered-client sources

- Treat `Client` as the 4.9.5 resource source of truth.
- Treat `Client-Decompiled-Mono-1.6.0` as the recovered C# script, protocol, and behavioral-contract source.
- When the two versions differ, document the evidence and keep any server fallback explicit; do not invent retired live-ops prices, schedules, weights, or rewards.

## Server implementation rules

- Implement `Server` behavior from recovered client contracts and authoritative server state. Client-provided prices, rewards, scores, ownership, and progression are assertions, never authority.
- Validate every complete durable game-catalog release pointer and entry before publication, operational reporting, or lookup. Releases require exact fields, a SHA-256 revision, conserved safe counts, canonically ordered unique source triples, and safe ordered dates; entries require exact kind/availability identity, a source triple present in that release, bounded canonical JSON, a recomputed content hash, and safe dates no later than pointer publication. Insert deterministic revision rows only once, re-read and validate the complete expected set before atomically replacing the pointer, and include source digest in provenance identity because multiple extraction artifacts may record the same path/schema from different snapshots.
- Keep every currency, item, reward, receipt, and claim transition atomic and retry-safe. Validate a nonnegative server-authored currency reward with `checkedRewardBalance` before publishing its claim or receipt marker.
- Apply that guard to direct, one-time, and tutorial rewards; conversion proceeds; level-up bonuses; PvP settlement components; and duplicate-item compensation. Do not reintroduce one-off `balance + reward` checks.
- Validate server-owned assignment, starter-assignment proof, achievement, War Arena, and Squad War eligibility/placement counters and receipt timestamps as nonnegative safe integers before comparisons, increments, or claim markers; `NaN` and `Infinity` must never satisfy a reward gate or placement through JavaScript comparison behavior.
- Validate the complete durable `AchievementsData` container at shared progression read/publication and before normalization, acknowledgement, increment, claim, or serialization. Require exact outer/group/tier shapes, bounded unique nonnegative group IDs, signed-client-safe offsets and nonnegative values, bounded known/unknown tier arrays, sequential Boolean claims, known values no higher than their final recovered target, and no claimed known tier whose target is unearned. Missing groups and short known tier arrays remain explicit schema-migration inputs; well-formed unknown groups may be preserved but never map to a reward without a server definition.
- Validate the complete War Arena lifecycle at shared progression read/publication and before boot, entry, battle start/settlement, heart/life changes, rewards, or serialization. Require known fields, safe counters and bounded timestamps, Boolean flags, bounded unique opponents and shown-event IDs, a bounded unique terminal-settlement history with supported end reasons and bounded JSON responses, and at most one active receipt bound to the same Arena and no terminal receipt. Only the absent legacy `shownArenaIds` list may normalize empty; malformed history must never be truncated into replay capacity or make a reward gate appear satisfied.
- Validate the complete Daily Missions lifecycle at shared progression read/publication and before rollover, boot, start/settlement, rewards, or serialization. Require the exact three daily and five ordered heroic definitions/config/unit shapes, consistent completion/reward flags and heroic prefix, bounded levels/reward cursor/heroic points, one derived UTC reset/day key, a source-backed or explicitly empty legacy heroic-unit target, and bounded unique active/terminal receipts with valid modes/indexes/end reasons/timestamps and bounded JSON responses. Expired cycles must still validate before rollover because heroic progress and replay receipts carry forward; malformed history must never be truncated into replay capacity.
- Validate the complete limited-time Event Assignment snapshot at shared progression read/publication and before boot, initialization, trusted progress, claims, or serialization. Require an exact safe event ID, lowercase SHA-256 config digest, nonnegative signed-client total, canonical bounded sparse day indexes with exact `{v,c}` records, and canonical bounded sparse milestone indexes whose present values are only `true`. Against the active immutable config, cap each day at its target, require a claimed day to reach its target, derive `totalValue` exactly from claimed-day point rows, and require milestone claims to form an earned prefix; malformed or config-inconsistent progress must never be reset into a fresh claimable event.
- Validate the complete shared Squad Event projection before insertion, join replay, confirmed-match mutation, or active-event serialization. Require exact outer/tier/assignment fields, bounded control-free squad identity, exact event/config identity, safe ordered dates inside the half-open season, a non-exhausted safe revision, source-exact rewards/assignments, canonical binary32 fractions, and coherent tier ordering: earlier tiers complete, the active tier incomplete, and later tiers untouched. A future row, skipped tier, prefilled later tier, or completed active tier must never survive one later match and become reward authority.
- Validate every durable Squad War season before allocation, retry classification, authenticated division reads, or scheduler closure. Require exact known fields, a safe half-open UTC window of at least one hour, a season ID derived exactly from its start second, creation inside that window, and an exact active-without-settlement or settled-with-post-window-settlement shape. Live paths must reject future creation or settlement timestamps; a configuration change must never let a terminal season reopen.
- Validate every complete Squad War round before insertion, assignment/pointer repair, scoring, eligibility changes, authenticated reads, placement, or settlement. Require exact outer/entry/member fields, round ID derived from level/season/division, season-bound windows, bounded unique squad and member identities, safe counters and totals, one-to-50 entries, one-to-50 members per entry, and exact conservation of shared score as the sum of member contributions. Active rounds have no settlement marker; settled rounds require equal update/settlement timestamps after the window. Permit post-window active audit updates only for leave/kick eligibility during scheduler delay; the separate half-open scoring proof must still reject new points.
- Widen Squad War maintenance and allocation guards to select malformed/missing date or status authority that ordinary due-date predicates would hide. Validate the complete round and season candidate batches before the first settlement write, treat any non-settled round as a season-closure blocker, and require the final season compare-and-set to modify exactly one row.
- Validate Player League competition mirrors (`MedalsBalance` and `Skill`), beginner tier, normal tier, and the exact zero-or-one placement counter at the shared account boundary before authentication/gameplay use, boot, tutorial repair, admission, ranking, or settlement; one corrupt member must abort a reward settlement before its first write.
- Validate the complete Player League allocation counter after its atomic upsert and before deriving a `localN` division. Require exact known fields, exact tier/end/season-key binding, a positive safe admission ordinal, signed-client-safe end time, ordered safe audit dates, and an update timestamp equal to the current in-window transaction time. Invalid authority must abort the surrounding PvP/player transaction so it cannot consume capacity or place a player in an unbounded division.
- Keep Player League background discovery bounded inside MongoDB: group scheduler-owned IDs, select only due valid rows plus malformed rows in the exact three-segment `*-local*` namespace, cap the returned batch, and validate every selected ID before the first settlement. Do not use an unbounded historical `distinct`, silently discard malformed locally owned IDs, or claim opaque retired production namespaces.
- Validate the complete `StarterAssignmentsData` snapshot before account creation, boot projection, completion, or claim. Its deadline must be a bounded safe Unix second, only the ten recovered IDs may exist, record flags must be Boolean, and a claimed record must also be completed; corrupt deadlines must never make onboarding rewards permanently claimable.
- When a buffered starter action rejects corrupt durable onboarding state, return an expired empty `StarterAssignmentsData` error view without mutating or reserializing the damaged snapshot; one invalid action must not escape its recovered per-action RequestBuffer result.
- Validate the complete daily-assignment snapshot at shared progression read/publication and before rollover, gameplay mutation, or serialization. Require the exact three server-supported records in recovered order with source IDs/targets, finite fractions, consistent done/claimed flags, a completed counter equal to claimed records, a Boolean skip marker, and a nonnegative safe mega cursor. The `issued`/`tomorrow`/`dayKey` tuple must describe one derived UTC cycle with bounded safe Unix seconds; a non-finite reset must never freeze one assignment day permanently.
- Validate a current-month daily-reward calendar as one exact ordered authority snapshot at shared progression read/publication and before check, claim, or serialization: year/month and `lastCheckDay` must describe a real UTC date, and `0 <= claimReward <= canClaim <= daysInMonth`. Capture one server time for both sides of a publication. Discard an expired prior-month snapshot before validating its cursors because it carries no claim authority forward, but reject malformed calendar coordinates or a future-month snapshot rather than treating it as expired. Route Daily Reward Gold, WarBucks, and Arena Ticket grants through `checkedRewardBalance` before advancing the ordered claim cursor; an invalid amount, damaged wallet, or overflow must leave the calendar day unconsumed, and a feature-local arithmetic helper must not replace the shared guard.
- Validate the complete rewarded-video state at shared progression read/publication and before grant or boot projection. Every one of the four exact ledgers must contain only positive safe Unix seconds within its source cap. The exact private replay receipt must bind a supported reward, safe timestamp, non-future revision, matching ledger grant, and bounded JSON response containing that same ledger snapshot. Only valid entries provably outside their rolling window may be discarded; malformed entries or receipts must never be treated as expired or trusted for replay because that reopens reward capacity or returns forged results.
- Validate the complete ranked-PvP win-streak pair at shared progression read/publication and before settlement or boot projection: `WinCount` must be exactly 0-9, the zero count must pair only with timestamp zero, and a positive count needs a signed-client-`int` Unix second. Keep shared transaction publication deterministic by applying the non-future comparison only against the authoritative boot/settlement request time. Never clamp corruption into the maximum WarBucks tier or expose a future timestamp through the client's `isInfinite` path.
- Validate the paid-VIP lootbox countdown as an exact 1-4 integer before settlement, progression migration, boot projection, or shared progression publication; only an absent legacy field defaults to four. Before duplicate conversion, reject a selected visual whose persisted parts exceed its catalog target instead of clamping unproven ownership into WarBucks.
- Validate the complete Instant Battle public counter tuple and private replay receipt at shared progression read/publication and before readiness, settlement, or boot. Timer, lifetime battles, and paid batches must be nonnegative signed-client integers; paid five-battle groups cannot exceed the lifetime count. The receipt must retain its exact fields, source price/count bounds, level/refill consistency, and a non-future revision. Keep shared publication deterministic by applying timer/receipt non-future time checks only at authoritative boot/action request time. Do not clamp corrupt authority into a valid recharge, replay, reward, or Gold-price state.
- Validate the persisted squad-creation count at shared progression read/publication and before price calculation, mutation, or boot so `(count + 1) * 25` remains positive and representable by the recovered client's signed `int`. Validate the WarBucks wallet, progression revision, and next count before debit; malformed arithmetic must never publish a squad or counter or survive an unrelated progression replacement.
- Validate every complete durable `SquadDocument` before roster, rank, admission, chat, card-pool, event, PvP reward, leaderboard, or Squad War authority is consumed and before a projected replacement is written. Require exact known fields, bounded normalized text and identities, source-valid ranks and join policy, safe nonnegative counters and ordered dates, unique members and pending identities, exactly one Leader who is the founder, member count within the configured capacity, bounded admission queues, and no pending identity that is already a member. MongoDB's implicit `_id` may be accepted only as storage metadata and must be removed before a `$set` replacement so an otherwise valid squad update cannot attempt to modify the immutable field.
- Validate profile-owned `renameCount` before price calculation, error recovery, mutation, or PlayerAnalytics boot. Counts 0-29 may be displayed, but a mutation must validate the increment before debit so count 29 cannot publish the signed-overflowing count 30; validate Gold and progression revision before the atomic profile/count update.
- Validate the complete `collectedRewards` dictionary before progression migration, one-time grant/replay, PlayerAnalytics boot, or shared progression publication. Preserve bounded legacy IDs, but require every present marker to be exact integer `1` because the client uses key presence; any other value must not let the backend pay a reward the client already considers collected.
- Validate the squad-chat read cursor at shared progression read/publication and before action-193 comparison, revision mutation, or PlayerAnalytics boot, using one captured server time for both sides of publication. Only an absent legacy value defaults to zero; the cursor must be a nonnegative signed-client integer within the bounded server-time skew window, remain monotonic across devices, and never let `NaN`, `Infinity`, or a far-future value hide later messages or survive an unrelated replacement.
- Validate every durable Squad Chat row before insertion, sender-nonce replay, history/cursor publication, or cross-node fan-out. Require exact known fields, a UUID message ID, sender-bound idempotency key and bounded nonce, bounded control-free squad/sender/text values, source-valid level/league/rank snapshots, safe ordered dates, and a one-to-365-day retention interval. Use one captured application time to reject future or expired rows, and clamp deployment retention into the same interval so MongoDB TTL delay or configuration overflow cannot extend chat authority.
- Validate every durable replay-video publication before insertion or idempotent replay. Require the exact known fields, a canonical bounded HTTP/HTTPS URL, its exact SHA-256 digest, the deterministic player-and-URL video ID, a bounded control-free player ID, equal creation/update timestamps, and an exact 365-day retention interval. Use application time to reject future or logically expired rows because delayed MongoDB TTL cleanup must never return damaged or stale publication authority.
- Validate the complete feature-introduction snapshot at shared progression read/publication and before any one-way shown action or PlayerAnalytics boot. Only the seven recovered keys may exist and every present value must be Boolean; missing legacy fields default to false, malformed truthy values must never suppress onboarding UI or survive an unrelated progression replacement, and a real false-to-true transition must validate progression revision before mutation.
- Validate the complete ordered tutorial lifecycle at shared progression read/publication and before bootcamp start/finish, Play Warcards start/settlement, rental eligibility, or boot projection. Only absent legacy completion markers default to false; both stored values must be Boolean and Play Warcards completion requires bootcamp completion. Each optional battle receipt must contain exactly a bounded control-free ID and a safe Unix start time, terminal phases must not retain consumed receipts, and every real transition must validate progression revision before publishing its receipt or marker. Keep shared transaction publication deterministic by applying the non-future comparison only against authoritative boot/gameplay request time.
- Bind each real-money purchase action to its reviewed entitlement family; sharing store-proof verification must never let a token cross into an incompatible response or grant path. Validate the complete private player account envelope after every direct or background full-document reload and before delivery, subscription revalidation, voided-purchase reversal, or post-delivery boot projection. A genuinely missing player may retain its explicit recovered/reconciliation behavior, but an existing damaged player must abort the whole transaction before a grant, reversal, or terminal receipt marker makes the provider event non-retryable.
- Keep every subscription receipt on one complete provider-polling lifecycle. A nonterminal, non-orphan receipt requires a Date retry cursor strictly after its latest verification audit; terminal Play states require revocation evidence and no cursor. Query malformed/missing cursor rows into the leased sweep, validate the whole selected batch before its first provider call, and validate retry, deleted-account retirement, and provider-result successors before writing them.
- Validate every complete durable Google Play receipt before replay, replacement, subscription revalidation, void reconciliation, overlap preservation, or insertion. Require its exact HMAC identity, platform, bounded owner/order, catalog-bound kind/store product/response, ordered safe dates, source-exact reversible grant, encrypted subscription token/state/expiry/retry shape, and all-or-none void metadata. Only legacy currency receipts may derive a missing reversible grant from their exact catalog-bound response; pack ownership history must never use that migration. Advance subscription retry counters with a bounded compare-and-set so concurrent workers cannot silently overwrite authority.
- Validate every shared scheduled-job lease before acquisition and after compare-and-set. Require exact fields, a bounded control-free job ID, UUID owner, safe ordered dates, and a one-second-to-24-hour duration; malformed expiry must surface as damaged authority rather than permanent contention. Validate the Google Play voided-purchase cursor before it selects a provider window and before publication: exact singleton ID, equal successful-end/update timestamps, safe dates, and no future cursor that could skip provider events.
- Commit every external-identity link, relink, and unlink as one MongoDB transaction with its sparse player-profile mirror and active `AccountType`. On unlink, preserve the current provider when its authority row remains; otherwise choose the remaining oldest row deterministically, and use Guest only when no external identity survives. Before provider authentication or an open provider-existence response, require the authority row's player ID and external ID to match the exact connected root/DTO mirror, reject disconnected sentinels and missing owners, then re-read and reverify the final row after credential comparison so concurrent ownership or credential rotation cannot authenticate a stale owner. The identity collection remains login authority, and a crash must never expose a credential that Unity does not display or display a provider whose credential was already removed.
- Normalize Facebook identity IDs as the canonical invariant string form of a C# signed 64-bit `long`, preserving large values as strings and reserving exact `-1` for the recovered disconnected sentinel. Link/create mutations must reject noncanonical, fractional, or out-of-range IDs; login and open existence lookups must treat them as no match. Google Play and Game Center retain their bounded nonempty string contract.
- Validate the complete durable platform-identity row at its shared lookup boundary before login, ownership proof, or open existence responses. Provider and external ID must use the supported canonical contract, player ID must be bounded and nonempty, credential HMAC must be exact lower-case 64-character hex, display name must retain its trimmed bounded storage shape, and finite audit dates must satisfy `createdAt <= updatedAt`. Malformed rows are damaged authority, not absent or usable identities.
- Validate the private player account envelope on shared full-document lookup and insertion. A present gameplay session must be exact lower-case 64-character HMAC text; custom-password authority may be only the exact legacy HMAC migration form or a canonical supported `scrypt$v1` digest. Apply that same credential proof to narrow authentication projections after both successful session compare-and-set and concurrent-winner reload; a projection must never become a weaker path that returns a malformed durable token. A missing session remains allowed solely so a proven password/provider login can rotate one for a legacy row. Require a bounded player ID, canonical optional normalized name, trimmed bounded device token, and finite ordered player audit dates before authentication or gameplay uses the document.
- Keep the private player-account proof callable separately from duplicated profile-mirror proof for an explicitly authorized operator repair. Squad-integrity repair may tolerate only the squad mirror damage it is designed to replace, but must validate credentials, identity/name/device shape, audit dates, exact audited `updatedAt`, and any progression successor before writing. Ordinary full-document consumers must continue to apply both proofs.
- Revalidate that complete player account envelope on every raw full-document retry or transaction reload that authorizes an economy/profile mutation. This includes Army Power refresh, Daily Missions, Instant Battle, PvP settlement, inbox reward claims, Player League allocation and its complete settlement population, Squad creation/join/leave/rank/leadership/kick, Squad-card withdrawal participants, and Squad-chat sender publication. Handler authentication proves an earlier snapshot only; a later damaged row must abort before rewards, ranking, inventory transfer, roster/indexed mirrors, messages, or terminal markers are published.
- Validate War Arena opponent candidates at the public/indexed profile-mirror boundary before using their IDs or Army Power. Opponent discovery consumes no private credentials, so it must not couple availability to private account fields, but a split root/DTO profile must never enter a battle offer.
- Validate the complete paid-VIP and exact four-field subscription timelines as nonnegative safe-integer Unix seconds at their shared read/publication boundaries. Paid VIP keeps expiry as the recovered active-benefit predicate, but its display start must be zero with no entitlement and otherwise not exceed expiry. Subscription authority must remain one ordered interval (`subscribeSince <= dogTagTimerLock <= expireTime`); activity requires `subscribeSince <= now < expireTime`, and boot must reject a future start before serializing the expiry-driven stock-client model. A non-finite entitlement expiry must never become permanent access through JavaScript comparison behavior or an invalid durable receipt date.
- Validate every present subscription authority receipt ID as the exact 64-character lowercase hexadecimal HMAC-SHA256 key generated by purchase delivery before read, comparison, or publication. Absence remains the explicit legacy state; arbitrary readable IDs are test-only inputs and never durable authority.
- Validate the complete RequestBuffer replay cache and message-ignore outbox before lookup, acknowledgement, read, or publication. Replay entries must be bounded, uniquely identified by trimmed control-free BufferIds, contain safe Unix timestamps and parseable result arrays, and outbox IDs must be bounded, unique, trimmed, and control-free; malformed rows must fail closed rather than be discarded and reopen buffered economy actions.
- Validate the complete action-221 WarBucks conversion receipt before replay lookup, read, or publication. Accept only the twelve recovered A/B IDs with their exact source Gold debit, a positive safe grant, safe time, and a non-future progression revision; malformed receipts must fail closed rather than be overwritten and reopen a recent conversion.
- Validate the complete paid-VIP daily-card marker before grant comparison, read, or publication. Its UTC day must derive exactly from a safe non-future grant timestamp and its two reward IDs must be implemented recovered Silver/Gold cards, with duplicates allowed; malformed markers must never be overwritten to reopen the daily pair.
- Validate the private War Card play and Gold-card craft lifetime counters as nonnegative safe integers at shared read/publication and reward-derivation boundaries. Missing legacy counters normalize to zero; malformed values must never satisfy starter assignments, achievements, or survive an unrelated progression replacement.
- Validate the complete `CraftData` receipt before boot projection, crafting actions, failure serialization, or progression publication. Only the exact empty zero tuple or three implemented same-rarity Bronze/Silver IDs with the exact source duration is valid; both timestamps must fit the recovered signed C# `int`, and malformed partial state must fail closed rather than reopen already-consumed inputs.
- Validate the complete five-field `CardManagerData` snapshot before boot projection, card gameplay, failure serialization, or progression publication. Normal ownership must remain sparse positive signed-client counts of implemented IDs; Buddy ownership must remain within the source cap with exact bounded loadout records and source-backed unit/weapon indexes; cooldowns must fit the recovered signed C# `int`; and `extraSlot` must be Boolean. Only wholly missing legacy inventory may materialize empty defaults.
- Validate the complete `InventoryData`/`LevelManagerData` snapshot at shared progression read/publication and before mutation or serialization. Require the exact four recovered weapon slots with source-backed identity/index/category/ownership, exact saved weapon/unit record shapes with bounded catalog cursors and consistent ownership flags, the recovered two-per-deployment-category and three-mechanical equipped-unit caps, and either the exact empty delivery tuple or one active delivery matching a permanent saved upgrade cursor. Only a wholly absent legacy item inventory may materialize the recovered starter loadout; malformed nested authority must fail closed rather than be defaulted or survive an unrelated replacement.
- Validate the complete rental lifecycle at shared progression read/publication and before boot projection, loadout, Army Power, acceptance, settlement, or permanent redemption. Only source-backed weapon/unit types, 20-25% discounts, safe timestamps, and status-consistent trial/sale intervals may authorize borrowed inventory; corrupt deadlines must fail closed rather than become permanent access or survive an unrelated replacement.
- Validate the complete Black Market set at shared progression read/publication and before boot projection, action-217 replay/rotation, serialization, or buffered weapon redemption. Expiry and issue cursors must be safe integers; offer identities must be unique and bounded; feature authority remains zero until the retired weighting source is recovered.
- Validate the complete visual inventory at shared progression read/publication and before boot projection, purchase, ownership/equip checks, paid-pack extension, or voided-purchase reversal. Timed `expiresOn` values and part counters must be bounded safe integers, saved flags and equipped slots must retain the recovered shape, and non-finite expiry must never turn a temporary cosmetic into permanent access or survive an unrelated replacement.
- Keep account sanctions as audited server-owned records. Enforce active bans through the shared authentication path only after credential proof, evaluate temporary expiry from application time rather than TTL cleanup, return the recovered `AccountBanned` fields, and require retry-safe operator issue/revoke keys. Validate the complete sanction before authentication enforcement, issue/revoke replay, operator publication, appeal intake, or accepted-appeal mutation: require exact known fields and normalized identities, safe ordered dates, an exact optional duration/expiry pair, and status-consistent resolution/revocation evidence. Query the active slot without an expiry predicate so a malformed Date fails closed instead of hiding from MongoDB comparison.
- Keep moderation report review transitions optimistic and append-audited: require the operator's expected status, never reopen a terminal report, and replay only an identical actor/decision/note operation.
- Keep player appeals ownership-scoped and separate from stock gameplay actions. Accept one appeal per active sanction through the current session credential, expose no private sanction fields, require optimistic append-audited review/withdrawal transitions, and revoke an accepted appeal's live sanction in the same transaction. Validate the complete appeal before submission replay, owned/admin read, list, review, withdrawal, or decision replay: exact UUID/sanction/player/submission identity, normalized bounded message, safe dates, open creation equality, and a unique legal one-or-two-entry audit projection through exact `updatedAt` are required; malformed terminal status has no support authority.
- Keep moderation retention explicit and operator-driven: freeze one preview timestamp for bounded export and purge, validate every eligible report and appeal lifecycle before either enters counts/exports or destructive authority, delete only the still-eligible validated IDs older than configured cutoffs, retain sanctions indefinitely, and publish the purge counts in the same idempotent transaction as deletion. A forged terminal report or appeal must abort before the first delete.
- Validate the complete moderation-retention receipt before replaying its deletion counts. Require exact fields, UUID/operation/actor identity, safe nonnegative counts, safe ordered preview/creation dates, and report/appeal cutoffs that are exact whole-day intervals within the global 30-3650-day bounds. Use global bounds rather than current configuration so a legitimate policy change does not invalidate historical receipts.
- Rotate `AUTH_SECRET` through the bounded fallback-key overlap documented in `Server/README.md`: keep gameplay sessions valid, verify durable password/provider digests against fallbacks only, and compare-and-set rehash a successful fallback login with the active key before retiring the old key.
- Treat each stored custom-password scrypt factor as verification authority within the bounded supported range. Raising `AUTH_SCRYPT_COST` may upgrade a successful login, but a temporary lower setting must never downgrade an already stronger digest.
- Validate the complete persistent login-throttle row after atomic reservation and before a collision replay checks its lock. Require exact known fields, a lower-case HMAC key, bounded positive attempt/revision counters, safe ordered window/update/expiry dates, no future audit time, and a null or bounded post-update lock contained by expiry. Use the globally supported policy bounds so a legitimate deploy-time window/lockout change does not make an older row corrupt; malformed authority must neither become a permanent lock nor reopen credential checks.
- Validate the complete persistent report-throttle row after atomic reservation. Require exact known fields, the expected lower-case HMAC reporter key, a safe positive counter bounded by the global maximum plus one denial sentinel, safe ordered window/update/expiry dates, a non-future update, and the exact storage-retention interval. Saturate repeated denials at that sentinel so attacker traffic cannot drive durable arithmetic unsafe; retain the original fixed-window start so continued spam cannot indefinitely postpone the logical reset.
- Validate the complete report-deduplication winner before it can be upserted into the moderation queue. Bind its exact HMAC key, immutable open-report shape, four semantic identity dimensions, UUID, bounded text/client-claim evidence, safe creation time, and exact ten-minute logical/twenty-minute storage windows. When ranked-match evidence exists, require its exact false-authority marker, the same two participants, source-valid snapshots, bounded claim maps, and a safe six-hour chronology; a concurrent winner may retain its own message/evidence but may never redirect the report identity.
- Validate every durable moderation report before admin detail/list publication, review, or review replay. Treat status as a projection of an exact one-or-two-entry audit chain from `open`: operation IDs must be unique and bounded, transitions legal, actors/notes bounded, timestamps safe and ordered from report creation through the exact `updatedAt`, and terminal notes nonempty. An open row must have neither review history nor an update timestamp; malformed status alone is never operator authority.
- Keep ordinary offline-bot battles outside ranked settlement. Their action-64 metadata may select a durable zero-reward lifecycle only after server-owned Play Warcards eligibility is checked; client-simulated bot results must never grant or consume economy, progression, cards, rentals, achievements, assignments, leagues, or events.
- Validate every complete durable ranked-match row before room admission/start, disconnect or presence authority, relayed-card mutation/delivery, result consensus, moderation correlation, cancellation/restart recovery, settlement, or terminal replay, and validate each projected successor before its write. Require an exact UUID match identity and two distinct bounded server snapshots; participant-only unique joined/disconnect/report/card maps; safe ordered lifecycle dates; state-consistent active, cancelled, or finished fields; exact two-player terminal reward receipts with internally consistent VIP multipliers, streak/league/level/lootbox/rental fields; and bounded participant-complete Squad Event/War projections. Only an absent legacy `rentalSettled` marker may enter the existing idempotent repair path; false/malformed markers or a rental payload without the marker are invalid. Player IDs used as MongoDB dynamic paths must contain neither `.` nor `$`; damaged rows must never protect presence, consume cards, create evidence, or replay rewards.
- Load and validate each complete durable player account and its projected successor inside the transaction before ranked-match admission or a presence mutation. Cancellation must bind exactly both assigned participant IDs; restart recovery must freeze its exact stale-player set; both must verify that MongoDB matched every selected account before commit. A narrow projection may be used only for a proof-scoped credential, compare-and-set, uniqueness, or existence check that cannot write broader account authority; it must never let a status/heartbeat update normalize a damaged credential, profile, or progression document or hide a missing participant behind partial bulk success.
- Validate the complete durable player account before publishing a public `DatabasePlayer` or leaderboard projection. Search, friend, squad-roster, challenge, and ranking views must fail closed when private credential or audit authority is damaged even if the duplicated public mirrors and progression remain believable.
- Validate the complete friendly/offline-bot participant receipt before start replay, `GameEnded` discrimination, settlement, or terminal replay. Require exact known fields, bounded control-free player/battle IDs, action 64/65, one supported zero-reward kind, safe ordered dates, exact 24-hour active or seven-day terminal expiry, and an end reason only on finished rows. Missing `battleKind` is the sole legacy direct-challenge migration. Enforce expiry from application time and in the settlement compare-and-set because asynchronous MongoDB TTL cleanup must never extend gameplay authority.
- Validate the complete type-0 challenge inbox message before insertion, duplicate replay, inbox publication, acceptance, or concurrent acceptance replay. Require exact known fields and recovered payload keys, supported game/region enums, bounded control-free map/room/version/mission data, a parseable bounded opponent snapshot, a sender-and-created-time-derived message ID, an ordered 1-minute-to-7-day TTL, and accepted/read/timestamp consistency with no reward or idempotency fields. Use one captured application time to reject future or expired invitations; clamp invalid deployment TTL configuration into the same bounded interval.
- Validate every supported durable inbox row through its complete type-specific authority before insertion/replay, pagination, read/ignore mutation, serialization, claim, or acceptance. The recovered `HHFHFANGCEJ` base constructor parses the final dash-separated `MessageId` segment with `Convert.ToInt32`, so all message families must end IDs with their positive bounded creation Unix second; millisecond, UUID-final, or text-final IDs are invalid. Keep recipient plus message ID unique because stock read/ignore/accept carries no stronger row identity; replay an identical concurrent challenge winner, but reject a different same-second challenge collision. For types 3/21/27/28, bind exact lifecycle fields, recovered payloads, sender/recipient relationships, source-valid returned cards, and server retry identities; validate projected read/ignore successors transactionally, and reject future rows or unsupported types instead of publishing a partial inbox page.
- Reserve player-authored direct/challenge inbox traffic through one HMAC-keyed atomic fixed-window counter before insertion. Validate any existing complete exact row before the aggregation can fill missing fields, then validate the concurrent winner again; require safe ordered dates, exact retention, bounded saturated denial sentinel, and expected key. Deployment limits must clamp to 1-1000. Resolve identical challenge replay before reserving, keep the original window start on denial, and validate the complete recipient account rather than trusting an ID-only projection. Never reintroduce count-then-insert admission across processes.
- Keep production MongoDB backups authenticated and encrypted before they leave temporary local storage. Retention may delete only an old, path-confined archive/manifest pair whose database, size, and SHA-256 all validate; malformed, damaged, unrelated, or orphaned files must remain for operator review.
- Keep monitoring artifacts on the bounded metrics contract actually exported by `metricsService`. Alerts must require sustained signals and meaningful traffic where ratios are used; Redis alerts apply only to scrape targets explicitly labeled `redis_required=true`.
- Treat schema-migration receipts and the singleton runner lease as complete durable authority. Read and validate every non-lock history row before planning; require exact receipt fields, canonical checksum, manifest-bound description, safe non-future application time, and nonnegative safe duration. Validate an existing lease before deciding that it expired, acquire an absent lease by singleton insert, replace an expired lease only against its complete prior snapshot, and never renew after its stored expiry even when no competitor has taken it yet.
- Keep cross-node scheduled jobs inside a renewable durable lease for their entire sweep. Validate the exact legacy or renewable row before expiry/ownership decisions; acquire absence by singleton insert, replace expiry only against the complete prior shape and values, renew only before the current deadline, serialize heartbeats with explicit ownership fences, and drain renewal before owner-scoped release. Provider calls and batched Player League, Squad War, subscription, and void-reconciliation transitions must re-prove ownership before their next durable mutation; an expired worker must never resume authority.
- Preserve legitimate negative Gold or WarBucks chargeback debt, but reject non-safe-integer rewards and arithmetic overflow.
- Keep the shared DynamoDB numeric wire adapters fail-closed across boot, public profile, experience/Arena leaderboards, durable inbox, Squad Event responses, Squad War member snapshots, and War Arena configuration. Integer fields may preserve the recovered truncating projection for legitimate finite fractional values such as calculated Army Power; Squad Event fraction fields must use the finite decimal-preserving adapter. A non-finite value or unsafe magnitude/projection must abort the response instead of becoming a believable zero or raw `NaN`/`Infinity` string. Durable legacy inbox numeric strings may be converted before validation, but malformed payloads must not become reward or placement zero. Validate the War Arena clock as a bounded safe Unix second before deriving its monthly ID/window. Do not introduce local numeric helpers that bypass these boundaries.
- Validate the duplicated indexed player profile mirrors after every shared player lookup and before account insertion, authentication/gameplay use, boot, public-profile, leaderboard, Arena-leaderboard, or rank projection. Root `id`, account name/type, sparse Facebook/Google Play/Game Center IDs, device token, league tier, Army Power, lifetime experience, squad points, and squad name must exactly match `DatabasePlayerDTO`. Provider DTO values must satisfy their recovered wire shapes even when both copies agree; disconnected Facebook must be exact numeric `-1`, disconnected Google Play/Game Center must be exact empty strings, and their sparse root keys must be absent rather than storing sentinel/empty index entries. A real lookup miss may remain `null`, but an existing damaged row must throw rather than masquerade as a miss. Full-profile saves must set/unset all duplicate roots in the same write; mismatch is damaged authority that must fail closed rather than letting MongoDB select/order/authenticate by one value while Unity displays another.
- Mirror equality is not numeric authority because matching `NaN` values pass `Object.is`. Require Army Power to be finite and nonnegative with a signed-client-int truncation result, lifetime Experience to be a nonnegative safe integer, and Squad Points to be a nonnegative signed-client integer at the shared profile boundary. Before ranked settlement, check Experience and Squad Point additions against those bounds, write their precomputed literal successors, and validate the canonical progression plus complete projected player account before its first write.
- Route every server-authored Reputation addition through the shared signed-client-int checked increment before publishing it. In particular, Squad-card withdrawal must validate all 5/15/45 normal and 30-point Buddy donor rewards before either participant write enters the atomic transfer; JavaScript-safe arithmetic alone is not sufficient for the recovered C# field.
- Validate public account-name and Player League identity shape even when every duplicated mirror agrees. Durable names must be nonempty, trimmed, control-free, and no longer than recovered `DatabasePlayer.maxPlayerName` 15; a nonempty `LeagueId` must make DatabasePlayer's first/final dash-segment parser derive the stored tier and division, while an empty placement fallback retains an empty stored division. Preserve opaque middle segments for unknown production-era IDs instead of inventing their schedule or allocation policy.
- Validate the complete six-field `SettingsManager.Settings` notification-consent snapshot at the shared profile boundary and before boot or update comparison. Only total legacy absence may derive the recovered LoadEmpty defaults; partial objects, unknown durable keys, and non-Boolean values must fail closed rather than silently re-enable a category or survive an unrelated profile write. Unknown request keys may remain forward-compatible but must never become stored authority.
- Validate persisted country and locale metadata at the shared player-profile boundary. Country must be either the exact empty new-account sentinel or an upper-case two-letter code, and locale must be a trimmed bounded language tag accepted by the request grammar. Normalize only new request input; malformed durable text must fail closed rather than becoming believable public, boot, or notification state through an unrelated update.
- Validate client-visible `PlayerStatus` and `SquadRank` as exact recovered enum members at the shared player-profile boundary. Unknown numeric values must fail before authentication, public projection, roster/rank use, or presence mutation; a heartbeat must not overwrite and thereby conceal damaged durable status authority.
- Validate DTO-only player scalars at the shared profile boundary before gameplay or publication: `Level` must select an exact recovered 4.9.5 rank row; Reputation must be a nonnegative signed-client integer; SendLogs must be exact 0/1 consent; awaiting-Squad state must be Boolean; RenameCount must remain in the source-backed 0-29 positive-price range; the legacy `DatabasePlayer.vipExpiration` fallback must be a nonnegative safe integer even when canonical progression owns a different current entitlement; LastAction must be a nonnegative signed-int Unix second; and the War Arena crown must use only empty/bronze/silver/gold/flawless with a signed-int expiry. Do not require the legacy/canonical VIP generations to mirror, but do not wait for final wire serialization or a narrower benefit/rename/boot handler to detect malformed dormant authority.
- Keep public `PlayerStatus` and `LastAction` as one atomic presence snapshot. Action `29` must return the exact signed-int Unix second as `Time`, while valid heartbeats, account creation, and every backend-owned ranked admission/release transition update both durable fields together. Invalid status enums may receive callback-safe response time but must never refresh durable presence; remote clients apply the recovered 2700-second freshness window to the stored pair.
- Validate the persisted Photon routing snapshot at the shared player-profile boundary as well as on action 140 input. Only the ten exact recovered region keys, integer 0-60000 millisecond pings, and numeric `None`/`Cellural`/`Wifi` enum members may survive; only total legacy absence may map to empty regions/`None`. Rebuild the public region JSON in canonical recovered order, and keep these client measurements as routing hints that never authorize matchmaking results, economy, or rewards.
- Validate core progression balances at the shared persisted read boundary, before `PlayerData` boot projection, and again on every progression mutation result before publication. Gold and WarBucks may be safe-integer chargeback debt; Tickets, Scraps, and level experience must be nonnegative safe integers, and malformed values must never bypass a price comparison or be silently serialized as zero.
- Validate the complete dog-tag tuple shape at every shared progression read/publication and before boot projection, regeneration, counting, spend, or refill. Callers that own an authoritative request/boot time must supply one captured server time for both snapshots and enforce a non-future cursor; context-free publishers must remain deterministic while still rejecting every malformed tuple value. Refill seconds and the divisible base cap must be positive safe integers; base seconds must remain between the normal cap and the exact two-tag VIP debt floor; malformed time must never freeze regeneration through min/max behavior or survive an unrelated replacement. Detect the legacy count-only schema by canonical-field absence, never by numeric validity, so corrupt canonical values cannot be silently reconstructed.
- Validate progression revision and core-balance authority through the shared publication validator for ordinary writes and every custom full-document replacement, including external-entitlement transactions. Only an absent legacy revision may migrate to zero; every produced revision must advance monotonically as a nonnegative safe integer, and both snapshots must retain valid core balances before MongoDB can publish them. RequestBuffer may advance multiple internal steps in one atomic replacement.
- Keep all custom progression publishers on that shared successor proof: inbox, Google Play delivery/revalidation/reversal, missions, Instant Battle, rename, Squad creation/cards/departure, tutorial, Squad War, PvP, and operator integrity repair. A tutorial profile-only repair must omit the unchanged progression field; an operator repair must reload and validate the exact audited `updatedAt` snapshot before replacement.
- Require the exact supported embedded progression `schemaVersion` at the shared read and publication boundaries. A wholly absent progression may use the new-account fallback, but a missing, malformed, or future version inside a present document must fail closed until an explicit migration understands it.
- Keep reward-bearing inbox claims in the same MongoDB transaction as their terminal message marker. Validate their pure wallet/revision successor and bind the player replacement to the stored progression revision so a stale custom transaction cannot overwrite unrelated economy state.
- Validate every type-9, type-11, and type-23 reward message as one complete durable authority snapshot before producer insertion, inbox publication, claim, or replay. Require the exact type-specific payload, bounded control-free identities/text, safe creation time, non-challenge lifecycle, parseable bounded embedded arrays, and a terminal claim marker whose stored response exactly equals its server-authored Gold payload and is both read and ignored. Only canonical safe-integer strings are accepted for explicitly migrated DynamoDB numeric rows, and only the absent legacy idempotency key may remain optional; malformed or extra authority fields must fail closed.
- Fail closed when authoritative combat evidence, live-event configuration, or source-backed balancing is unavailable.
- Add detailed English comments where recovered names are opaque or atomicity, replay, migration, or fallback decisions are not obvious.

## Change discipline

- Run `npm test` from `Server` for backend changes.
- Update `Server/BACKEND_FEATURES.md` and `Server/README.md` when backend behavior or a documented gap changes.
- Keep unresolved backend gaps explicit. Do not mark a feature complete while its authoritative source or external provider integration is missing.
- The worktree may contain unrelated recovered-client changes. Stage and commit only files belonging to the current increment.
