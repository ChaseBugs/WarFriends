# WarFriends repository guidance

## Recovered-client sources

- Treat `Client` as the 4.9.5 resource source of truth.
- Treat `Client-Decompiled-Mono-1.6.0` as the recovered C# script, protocol, and behavioral-contract source.
- Do not promote a presentation-only enum into a backend feature. Recovered 1.6.0 Loyalty exists only as an `InappScreen` tab and has no action/DTO/parser contract; keep Loyalty catalog rows closed unless later evidence supplies one. Treat the paid extra War Card slot as implemented through reviewed pack entitlement, boot projection, receipt replay, and void reversal rather than listing it as a missing reward family.
- When the two versions differ, document the evidence and keep any server fallback explicit; do not invent retired live-ops prices, schedules, weights, or rewards.

## Server implementation rules

- Implement `Server` behavior from recovered client contracts and authoritative server state. Client-provided prices, rewards, scores, ownership, and progression are assertions, never authority.
- Keep player-profile mutations on their recovered request fields: action 29 uses `PlayerStatus`, action 196 uses `NewCountryCode`, and action 13 requires `DeviceToken` plus `Locale` as one validated tuple. A diagnostic alias may only accompany and agree with its canonical field; it must never authorize a mutation by itself, and malformed status heartbeats remain callback-safe without refreshing durable presence.
- Keep stock inventory and visual mutations inside the exact `RequestBuffer.data` contract. Direct replacement-client routes require their documented canonical `LevelName`, `ObjData`, or `DecalId`; generic `Name`/`Data`/`data` aliases may only accompany and exactly agree with that canonical field. Treat ambiguous direct action 108 as non-mutating telemetry rather than inferring a visual identity from diagnostic aliases.
- Preserve provider display names exactly: recovered Facebook and Google Play link requests require canonical nonempty `Name`, while Game Center link/update builders send none and retain empty display metadata. `PlayerName` may only accompany and exactly agree with `Name`; never stringify, trim, slice, or accept control-bearing provider names at the request, service, or durable-authority boundary.
- For recovered CreateAccount and CreateGcAccount, require and validate `Locale` plus finite bounded invariant-C# `UtcOffset`, and persist Locale in the same initial account transaction. Treat UtcOffset only as a diagnostic hint; never use it for calendars, rewards, deadlines, or receipts. Ignore client Fusebox `StartingGold`/`StartingWarbucks` assertions and keep starting currency server-authored.
- Enforce first-account Locale, DeviceToken, and optional Game Center ID again at the reusable account service before password hashing or persistence. Preserve opaque token and platform-ID bytes exactly in both root and DTO mirrors; never let an internal caller bypass request parsing or trim a padded external ID into a different durable identity.
- Validate application time once as a nonnegative safe Unix second within JavaScript/BSON Date support before new progression creation, shared progression reads, successor publication, `PlayerData` projection, or the top-level player-state response. Reuse that exact value across all nested boot/entitlement/calendar validators; never floor, clamp, or independently recapture malformed time into inconsistent authority decisions.
- Keep achievement groups 6, 7, and 18 closed until combat authority exists. Recovered group 6 adds spawned soldier entities, group 7 adds spawned vehicle entities, and group 18 adds stolen crates; deployment clicks, opaque relay messages, and War Card consumption are not equivalent evidence.
- Validate every complete durable game-catalog release pointer and entry before publication, operational reporting, or lookup. Releases require exact fields, a SHA-256 revision, conserved safe counts, canonically ordered unique source triples, and safe ordered dates; entries require exact kind/availability identity, a source triple present in that release, bounded canonical JSON, a recomputed content hash, and safe dates no later than pointer publication. Insert deterministic revision rows only once, re-read and validate the complete expected set before atomically replacing the pointer, and include source digest in provenance identity because multiple extraction artifacts may record the same path/schema from different snapshots.
- Validate the complete generated weapon economy before inventory, Black Market, Army Power binding, or database-catalog publication. Require all 165 weapon identities in validated shop-then-Black-Market order, exactly 11,640 positive safe price/duration stages, a matching Army Power identity/index with exactly one more DPS row per weapon, and exactly 5,860 ordered positive Gold prices across the 79 source-backed Black Market tables. Keep `Google2u.LMG_GSh` and `Google2u.LMG_Scifi` closed because their recovered tables contain no `WEAPONPRICE`; deep-freeze every accepted stage tuple and price array before lookup construction.
- Validate the recovered unit identity and unit-upgrade artifacts together before gameplay or database-catalog publication. Require exact shared MainScene provenance, 24 ordered player rows plus three helper and 18 unresolved rows, unique bounded identities, source-consistent starter/tier/sentinel fields, and the complete 4,124 normal, 684 special, and 216 Elite levels bound contiguously to each player's `startingSpecial`/`startingElite` offsets. Deep-freeze every accepted row and nested lane before constructing lookup maps; a truncated lane, duplicate name, mismatched digest, non-finite Army Power, or malformed helper sentinel must fail before it changes purchase, upgrade, equip, power, or publication authority.
- Keep every currency, item, reward, receipt, and claim transition atomic and retry-safe. Validate a nonnegative server-authored currency reward with `checkedRewardBalance` before publishing its claim or receipt marker.
- Apply that guard to direct, one-time, and tutorial rewards; conversion proceeds; level-up bonuses; PvP settlement components; and duplicate-item compensation. Do not reintroduce one-off `balance + reward` checks.
- Validate server-owned assignment, starter-assignment proof, achievement, War Arena, and Squad War eligibility/placement counters and receipt timestamps as nonnegative safe integers before comparisons, increments, or claim markers; `NaN` and `Infinity` must never satisfy a reward gate or placement through JavaScript comparison behavior.
- Validate the complete durable `AchievementsData` container at shared progression read/publication and before normalization, acknowledgement, increment, claim, or serialization. Require exact outer/group/tier shapes, bounded unique nonnegative group IDs, signed-client-safe offsets and nonnegative values, bounded known/unknown tier arrays, sequential Boolean claims, known values no higher than their final recovered target, and no claimed known tier whose target is unearned. Missing groups and short known tier arrays remain explicit schema-migration inputs; well-formed unknown groups may be preserved but never map to a reward without a server definition.
- Validate the complete War Arena lifecycle at shared progression read/publication and before boot, entry, battle start/settlement, heart/life changes, rewards, or serialization. Require known fields; nonnegative recovered C# `int` counters and public crown/shield timestamps; only the exact empty/bronze/silver/gold/flawless crown types; configured lives/win/top-run bounds; wins no higher than lifetime matches; Boolean flags; bounded unique opponents and shown-event IDs; a bounded unique terminal-settlement history with supported end reasons and bounded JSON responses; and at most one active receipt bound to the same Arena and no terminal receipt. Only the absent legacy `shownArenaIds` list may normalize empty; malformed history must never be truncated into replay capacity or make a reward gate appear satisfied.
- Parse War Arena `UsedGold`, `UsedGolds`, and `hearthPrice` only as canonical nonnegative decimals emitted by their recovered integer `ToString()` calls or the same safe integers from replacement JSON clients; only an absent optional `UsedGold` selects the free/default zero path. Parse `HeartDialogShown` only as exact `True`/`False` or a JSON Boolean. Null, array, blank, padded, signed, leading-zero, fractional, exponent, lowercase Boolean text, or numeric Boolean substitutes must never authorize an Arena transition through JavaScript coercion.
- Revalidate War Arena price assertions at the reusable mutation boundary as nonnegative C# signed-`int` values even after HTTP parsing. Never floor fractions, accept oversized safe JavaScript integers, or replace `NaN`/`Infinity` with zero; malformed direct/replacement callers must not select a valid free, Ticket, or Gold branch through service-level coercion.
- Resolve the complete deployment-owned War Arena policy once during module startup as one immutable snapshot before config projection or lifecycle use. The ID prefix must use only the durable safe-ID alphabet and leave room for the monthly suffix; maximum battles must be an exact integer from one through twelve, starting lives a positive recovered C# `int`, and every Ticket/Gold price plus guaranteed-Scraps fallback a nonnegative recovered C# `int`. Never round, clamp, replace, or re-read malformed operator values after traffic starts.
- Keep action-206 Arena rankings scoped to the exact current server-generated monthly Arena ID as well as `played=true`; a stale prior-event snapshot must never rank merely because a player has not opened the new event. Reproduce the recovered flawless-first comparison by ranking positive `flawless` before non-flawless `topRun`, use a deterministic name/ID tie break, and revalidate every selected account, Arena snapshot, event identity, and returned order before publishing `Position` or `WarArenaData`.
- Validate the complete Daily Missions lifecycle at shared progression read/publication and before rollover, boot, start/settlement, rewards, or serialization. Require the exact three daily and five ordered heroic definitions/config/unit shapes, consistent completion/reward flags and heroic prefix, bounded levels/reward cursor/heroic points, one derived UTC reset/day key, a source-backed or explicitly empty legacy heroic-unit target, and bounded unique active/terminal receipts with valid modes/indexes/end reasons/timestamps and bounded JSON responses. Cycle, start, and settlement application time must use one exact Date-bounded Unix second; reuse it for expiry, VIP, level-up dog tags, rental cleanup, and terminal receipts instead of flooring or mixing raw values. Require `DatabasePlayer.Level` to be one exact recovered zero-based rank row before issuance, reward selection, start, or settlement; persist that index directly in `dailyMissionLevel`/`heroicMissionLevel`, and use its one-based display number only for source reward-table lookup. Expired cycles must still validate before rollover because heroic progress and replay receipts carry forward; malformed history must never be truncated into replay capacity.
- Validate the complete limited-time Event Assignment snapshot at shared progression read/publication and before boot, initialization, trusted progress, claims, or serialization. Require an exact safe event ID, lowercase SHA-256 config digest, nonnegative signed-client total, canonical bounded sparse day indexes with exact `{v,c}` records, and canonical bounded sparse milestone indexes whose present values are only `true`. Active selection, UTC day derivation, trusted progress, daily claims, and milestone claims must consume an exact Date-bounded application time without flooring malformed values into an eligible event window. Against the active immutable config, cap each day at its target, require a claimed day to reach its target, derive `totalValue` exactly from claimed-day point rows, and require milestone claims to form an earned prefix; malformed or config-inconsistent progress must never be reset into a fresh claimable event.
- Validate the complete shared Squad Event projection before insertion, join replay, confirmed-match mutation, or active-event serialization. Require exact outer/tier/assignment fields, bounded control-free squad identity, exact event/config identity, safe ordered dates inside the half-open season, a non-exhausted safe revision, source-exact rewards/assignments, canonical binary32 fractions, and coherent tier ordering: earlier tiers complete, the active tier incomplete, and later tiers untouched. A future row, skipped tier, prefilled later tier, or completed active tier must never survive one later match and become reward authority.
- Validate every durable Squad War season before allocation, retry classification, authenticated division reads, or scheduler closure. Require exact known fields, a safe half-open UTC window of at least one hour, a season ID derived exactly from its start second, creation inside that window, and an exact active-without-settlement or settled-with-post-window-settlement shape. Live paths must reject future creation or settlement timestamps; a configuration change must never let a terminal season reopen. Calendar derivation must require a valid nonnegative client-width Unix time and an exact safe-integer duration of at least one hour whose end stays client-representable; never floor/clamp malformed policy or permit an `swNaN` identity.
- Validate every complete Squad War round before insertion, assignment/pointer repair, scoring, eligibility changes, authenticated reads, placement, or settlement. Require exact outer/entry/member fields, round ID derived from level/season/division, season-bound windows, bounded unique squad and member identities, safe counters and totals, one-to-50 entries, one-to-50 members per entry, and exact conservation of shared score as the sum of member contributions. Active rounds have no settlement marker; settled rounds require equal update/settlement timestamps after the window. Permit post-window active audit updates only for leave/kick eligibility during scheduler delay; the separate half-open scoring proof must still reject new points.
- Widen Squad War maintenance and allocation guards to select malformed/missing date or status authority that ordinary due-date predicates would hide. Validate the complete round and season candidate batches before the first settlement write, treat any non-settled round as a season-closure blocker, and require the final season compare-and-set to modify exactly one row.
- Validate Player League competition mirrors (`MedalsBalance` and `Skill`), beginner tier, normal tier, and the exact zero-or-one placement counter at the shared account boundary before authentication/gameplay use, boot, tutorial repair, admission, ranking, or settlement; one corrupt member must abort a reward settlement before its first write.
- Validate the complete Player League allocation counter after its atomic upsert and before deriving a `localN` division. Require exact known fields, exact tier/end/season-key binding, a positive safe admission ordinal, signed-client-safe end time, ordered safe audit dates, and an update timestamp equal to the current in-window transaction time. Invalid authority must abort the surrounding PvP/player transaction so it cannot consume capacity or place a player in an unbounded division.
- Keep Player League background discovery bounded inside MongoDB: group scheduler-owned IDs, select only due valid rows plus malformed rows in the exact three-segment `*-local*` namespace, cap the returned batch, and validate every selected ID before the first settlement. Do not use an unbounded historical `distinct`, silently discard malformed locally owned IDs, or claim opaque retired production namespaces.
- Resolve Player League polling, Squad War polling, and Squad War season duration once during module startup as one immutable exact competition-timing snapshot. Scheduler intervals must be safe integers from 10 through 3,600 seconds and the same frozen value must own both timer and renewable-lease sizing; season duration must be an exact 3,600-through-2,147,483,647-second value shared by every window derivation. Never floor, clamp, substitute, or re-read mutable configuration because that hides deployment damage, changes settlement ownership cadence, or derives conflicting durable season IDs/deadlines inside one process.
- Validate the complete `StarterAssignmentsData` snapshot before account creation, boot projection, completion, or claim. Its deadline must be a nonnegative recovered C# signed-int Unix second, only the ten recovered IDs may exist, record flags must be Boolean, and a claimed record must also be completed; corrupt or client-unrepresentable deadlines must never make onboarding rewards permanently claimable. Resolve `STARTER_ASSIGNMENT_DURATION_SECONDS` once during module startup as one immutable exact value in that same signed-int domain, reject deadline overflow before addition, and keep the seven-day default explicit reconstruction policy because the production duration is absent from the recovered clients.
- When a buffered starter action rejects corrupt durable onboarding state, return an expired empty `StarterAssignmentsData` error view without mutating or reserializing the damaged snapshot; one invalid action must not escape its recovered per-action RequestBuffer result.
- Validate the complete daily-assignment snapshot at shared progression read/publication and before rollover, gameplay mutation, or serialization. Require the exact three server-supported records in recovered order with source IDs/targets, finite fractions, consistent done/claimed flags, a completed counter equal to claimed records, a Boolean skip marker, and a nonnegative safe mega cursor. The `issued`/`tomorrow`/`dayKey` tuple must describe one derived UTC cycle with bounded safe Unix seconds; a non-finite reset must never freeze one assignment day permanently.
- Resolve the deployment-owned `ASSIGNMENT_MEGA_REWARD_GOLD` currency-only fallback once during module startup as one immutable exact nonnegative safe integer. Malformed policy must stop startup before players can earn or consume the 50-point cursor; apply `checkedRewardBalance` before cursor subtraction, never floor or clamp policy, and keep the missing production/non-currency reward table explicit.
- Validate a current-month daily-reward calendar as one exact ordered authority snapshot at shared progression read/publication and before check, claim, or serialization: year/month and `lastCheckDay` must describe a real UTC date, and `0 <= claimReward <= canClaim <= daysInMonth`. Action 1002 must supply exactly one of the source `claimRweard` field or repaired-client `claimReward` alias through the canonical 1-31 C# Int32 transport; never let alias precedence choose a reward cursor. Capture one exact Date-bounded application time for both sides of a publication and for check, claim, and wire countdown; never floor malformed time into a UTC day. Discard an expired prior-month snapshot before validating its cursors because it carries no claim authority forward, but reject malformed calendar coordinates or a future-month snapshot rather than treating it as expired. Route Daily Reward Gold, WarBucks, and Arena Ticket grants through `checkedRewardBalance` before advancing the ordered claim cursor; an invalid amount, damaged wallet, or overflow must leave the calendar day unconsumed, and a feature-local arithmetic helper must not replace the shared guard. Resolve deployment-owned ordinary/weekly Gold once during module startup as one immutable pair of exact nonnegative safe integers with weekly no lower than ordinary; malformed policy must stop startup rather than fail only during calendar projection, and direct policy selection must accept only days 1-31.
- Parse action-1002 `claimRweard` only as the canonical 1-31 decimal emitted by the recovered `int.ToString()` call or the same integer from a replacement JSON client. Missing, null, Boolean, array, blank, padded, signed, leading-zero, fractional, exponent, zero, or out-of-range values must never select a reward day through JavaScript coercion.
- Validate the complete rewarded-video state at shared progression read/publication and before grant or boot projection. Grant/comparison application time and every one of the four exact ledgers must contain only positive safe Unix seconds within JavaScript/BSON Date support and the source cap; never floor or clamp malformed time into a reward receipt. The exact private replay receipt must bind a supported reward, bounded safe timestamp, non-future revision, matching ledger grant, and bounded JSON response containing that same ledger snapshot. Only valid entries provably outside their rolling window may be discarded; malformed entries or receipts must never be treated as expired or trusted for replay because that reopens reward capacity or returns forged results.
- Parse action-156 `Reward` only as one of the four canonical positive enum decimals emitted by `BeanstalkServerManager` or the same integer from a replacement JSON client. Missing, null, Boolean, array, blank, padded, signed, leading-zero, fractional, exponent, zero, or out-of-range values must never select a reward-bearing branch through JavaScript coercion.
- Validate the complete ranked-PvP win-streak pair at shared progression read/publication and before settlement or boot projection: `WinCount` must be exactly 0-9, the zero count must pair only with timestamp zero, and a positive count needs a signed-client-`int` Unix second. Keep shared transaction publication deterministic by applying the non-future comparison only against the authoritative boot/settlement request time. Never clamp corruption into the maximum WarBucks tier or expose a future timestamp through the client's `isInfinite` path.
- Validate the offline PvP Win/Loss WarBucks policy during module startup as exact nonnegative safe integers whose normal values and independently truncated 1.5x VIP values both fit the recovered C# signed-`int` reward fields. Keep one immutable policy per process; never replace malformed `PVP_WIN_WARBUCKS` or `PVP_LOSE_WARBUCKS` with hidden 800/400 defaults after the operator supplied different authority.
- Validate the paid-VIP lootbox countdown as an exact 1-4 integer before settlement, progression migration, boot projection, or shared progression publication; only an absent legacy field defaults to four. Before duplicate conversion, reject a selected visual whose persisted parts exceed its catalog target instead of clamping unproven ownership into WarBucks.
- Validate the complete Instant Battle public counter tuple and private replay receipt at shared progression read/publication and before readiness, settlement, or boot. Timer, lifetime battles, and paid batches must be nonnegative signed-client integers; paid five-battle groups cannot exceed the lifetime count. The receipt must retain its exact fields, source price/count bounds, level/refill consistency, and a non-future revision. Keep shared publication deterministic by applying timer/receipt non-future time checks only at authoritative boot/action request time. Do not clamp corrupt authority into a valid recharge, replay, reward, or Gold-price state.
- Preserve the recovered action-199 transport distinction: only an absent `IsPaid` field selects a free Instant Battle, while the paid dialog sends the positive Gold price through C# `int.ToString()`. Parse that value through the exact nonnegative signed-int boundary and reject explicit null, blank, Boolean, array, padded, signed, fractional, exponent, zero, negative, or oversized values with the stock authorization failure before readiness or wallet logic.
- Validate the offline Instant Battle XP/WarBucks policy during module startup as exact nonnegative integers no greater than 429,496,729 per battle, so the recovered five-charge batch remains representable by the action-199 C# signed-`int` response fields. Keep the policy immutable per process, and let free Skirmish rewards preserve or pay down legitimate Gold/WarBucks chargeback debt instead of rejecting a negative safe-integer wallet.
- Treat `DOG_TAG_REFILL_SECONDS=900` and `DOG_TAG_CAP=5` as exact 4.9.5 compatibility assertions resolved once during module startup, not tunable balancing. Require every durable dog-tag tuple to retain that 900/4,500 normal policy, the bounded two-tag VIP debt floor, and signed-C#-`int` boot fields/timestamps; alternate safe integers or post-2038 values must fail closed before account creation, migration, time arithmetic, mutation, or boot projection.
- Validate the persisted squad-creation count at shared progression read/publication and before price calculation, mutation, or boot so `(count + 1) * 25` remains positive and representable by the recovered client's signed `int`. Validate the WarBucks wallet, progression revision, and next count before debit; malformed arithmetic must never publish a squad or counter or survive an unrelated progression replacement.
- Treat squad admission settings as exact client assertions at both the HTTP adapter and authoritative service. `JoinPolicy` must be an integer from 0 through 2, `RequiredMedals` must be a nonnegative C# signed `int`, and `IsPublic` must use an exact Boolean representation; never floor, broadly coerce, silently ignore, or default an explicitly supplied malformed value. Form integers must retain canonical C# decimal spelling with no trimming, plus sign, leading zero, alternate negative zero, fraction, exponent, or value outside signed-`int` width; Boolean text aliases must not be whitespace-normalized or case-folded into authority.
- Advance `SquadDocument.updatedAt` through the shared strictly monotonic helper on every core membership, settings, invitation, join-request, PvP Squad Point, and Squad War write, even when operations share one wall-clock millisecond. Bind each single-document replacement to the exact validated prior timestamp that authorized it. A compare-and-set miss must force a fresh authority read; never retry a stale manager, roster, pending-capability, or admission snapshot or fall back to a name-only write.
- Validate every complete durable `SquadDocument` before roster, rank, admission, chat, card-pool, event, PvP reward, leaderboard, or Squad War authority is consumed and before a projected replacement is written. Require exact known fields, bounded normalized text and identities, source-valid ranks and join policy, safe nonnegative counters and ordered dates, unique members and pending identities, exactly one Leader who is the founder, member count within the configured capacity, bounded admission queues, and no pending identity that is already a member. MongoDB's implicit `_id` may be accepted only as storage metadata and must be removed before a `$set` replacement so an otherwise valid squad update cannot attempt to modify the immutable field.
- Treat `SquadDocument.experience` as the recovered 64-bit `LevelExperience` progress within its exact one-based Squad rank. Validate rank, progress, `maxMembers`, and card-pool capacity as one row from the 50-row 4.9.5 `Squads` table; require the complete generated 1-50 sequence at runtime, monotonic experience/roster/card-pool values, and exact agreement with the duplicated card-pool capacity array so a truncated or split artifact cannot redefine durable authority. The complete validated card artifact must own the single frozen runtime Squad progression snapshot; keep the pure 50-row validator dependency-free so Squad gameplay never needs a second raw JSON import or a card/Squad module cycle. Below rank 50 the progress must be less than that row's `EXPERIENCE`. Confirmed ranked-PvP Squad Points must atomically add competitive/member totals, subtract every crossed rank threshold, unlock the target row's `SIZE`, and publish progress through wire `LevelExperience`. In that same match transaction, create one deterministic match-UUID-bound type-10 inbox row for every current member and crossed rank; store the prior level because recovered `KGALJDLJCEH.HDCCFKAHHOP` adds one, and emit only its exact `Level.N`/`SquadId.S` payload. Never attach a reward or claim lifecycle to this presentation message. Never floor/clamp an invalid rank into the first/final row or authorize `CARDPOOLSIZE` through the recovered presentation clamp. Keep the one-time legacy migration idempotent and abort before writes when the source capacity cannot contain an old reconstructed roster; never delete members to force migration.
- Validate and deep-freeze the complete generated 4.9.5 card catalog before gameplay or database-catalog publication. Require exact artifact provenance/field sets, 58 ordered unique playable rows, 25 ordered unique unresolved rows, all four recovered pack identities and counts, bounded prices/rarities/mission gates/timers/reputation values, and the complete shared Squad progression snapshot. A truncated, duplicate, contradictory, non-finite, unsafe, or later-mutated artifact must never redefine card ownership, purchases, crafting, rewards, or Squad pool authority.
- Validate and deep-freeze the complete generated 4.9.5 visual catalog before entitlement checks, economy mutations, or database-catalog publication. Require the exact four ordered category identities/defaults/counts, unique category assets, 146 exact ordered playable rows, the one distinct unresolved row, category-prefix agreement, bounded numeric fields, known acquisition families, and immutable nested ID arrays. Never let duplicate, truncated, split-category, non-finite, or later-mutated visual authority change ownership, equipment, prices, timed access, parts, duplicate compensation, or catalog sync.
- Validate and deep-freeze the complete generated 4.9.5 Army Power catalog before player progression, combat-power calculation, matchmaking publication, or database-catalog sync. Require all 58 contiguous player ranks, 11 ordered category masks with nine exact feature identities each, 165 unique weapon identities/indexes, and exactly 11,805 finite nondecreasing DPS rows. Truncated ranks, duplicate weapons, split feature masks, unsafe progression numbers, non-finite DPS, or later mutation must fail before changing level limits, rewards, conversion amounts, or Army Power.
- Validate and deep-freeze the complete generated 4.9.5 weapon catalog before inventory/loadout authority or database-catalog sync. Require 84 ordered unique shop rows, 81 ordered unique Black Market rows, nine ordered unresolved rows in each family, globally unique resolved indexes and names, exactly four starter weapons, one-bit source categories, bounded prices/level gates, zero recovered delivery time, and exact unresolved evidence. Never let duplicate/truncated/reordered/unsafe catalog data be overwritten by dictionary construction or authorize a different purchase, equip, card-loadout, Black Market, or publication result.
- Validate profile-owned `renameCount` before price calculation, error recovery, mutation, or PlayerAnalytics boot. Counts 0-29 may be displayed, but a mutation must validate the increment before debit so count 29 cannot publish the signed-overflowing count 30; validate Gold and progression revision before the atomic profile/count update.
- Parse action-29 `PlayerStatus` and action-139 `PayForRename` from only their canonical recovered decimal integer forms. Missing, null, Boolean, array, blank, signed/space-padded, leading-zero, fractional, exponent, unsafe, or out-of-range values must never coerce into Offline/Online presence or a free/paid rename assertion. Invalid presence transport remains callback-safe but must not refresh durable status or `LastAction`; invalid rename payment flags fail before name/economy mutation.
- Validate the complete `collectedRewards` dictionary before progression migration, one-time grant/replay, PlayerAnalytics boot, or shared progression publication. Preserve bounded legacy IDs, but require every present marker to be exact integer `1` because the client uses key presence; any other value must not let the backend pay a reward the client already considers collected.
- Validate the squad-chat read cursor at shared progression read/publication and before action-193 comparison, revision mutation, or PlayerAnalytics boot, using one captured server time for both sides of publication. Only an absent legacy value defaults to zero; the cursor must be a nonnegative signed-client integer within the bounded server-time skew window, remain monotonic across devices, and never let `NaN`, `Infinity`, or a far-future value hide later messages or survive an unrelated replacement.
- Validate every durable Squad Chat row before insertion, sender-nonce replay, history/cursor publication, or cross-node fan-out. Require exact known fields, a UUID message ID, sender-bound idempotency key and bounded nonce, bounded control-free squad/sender/text values, source-valid level/league/rank snapshots, safe ordered dates, and a one-to-365-day retention interval. Use one captured application time to reject future or expired rows. Resolve history rows, retention days, messages per minute, and text characters once during module startup as one immutable exact policy snapshot bounded respectively to 1-100, 1-365, 1-1,000, and 1-2,048; never default, clamp, or re-read malformed policy between admission, storage, and publication. Reserve chat attempts through the shared atomic fixed-window authority under a separate HMAC domain from direct/challenge inbox traffic, and never reintroduce count-then-insert admission across nodes.
- Validate replacement WebSocket Squad Chat wrappers before persistence. `SendSquadChat` must contain exactly string `ClientMessageId` and `Text`; leave nonce grammar, normalization, moderation, membership, rate limits, and replay checks to the shared service. `GetSquadChatHistory` may omit its payload or use an exact empty object for the latest page, but a present `BeforeCursor` must be a string and no aliases or extra keys are allowed. Explicit null, undefined, Boolean, numeric, array, or object cursor values must never collapse into true absence and reopen the latest-page path.
- Validate every durable replay-video publication before insertion or idempotent replay. Require the exact known fields, a canonical bounded HTTP/HTTPS URL, its exact SHA-256 digest, the deterministic player-and-URL video ID, a bounded control-free player ID, equal creation/update timestamps, and an exact 365-day retention interval. Use application time to reject future or logically expired rows because delayed MongoDB TTL cleanup must never return damaged or stale publication authority.
- Validate the complete feature-introduction snapshot at shared progression read/publication and before any one-way shown action or PlayerAnalytics boot. Only the seven recovered keys may exist and every present value must be Boolean; missing legacy fields default to false, malformed truthy values must never suppress onboarding UI or survive an unrelated progression replacement, and a real false-to-true transition must validate progression revision before mutation.
- Validate the complete ordered tutorial lifecycle at shared progression read/publication and before bootcamp start/finish, Play Warcards start/settlement, rental eligibility, or boot projection. Only absent legacy completion markers default to false; both stored values must be Boolean and Play Warcards completion requires bootcamp completion. Each optional battle receipt must contain exactly a bounded control-free ID and a safe Unix start time, terminal phases must not retain consumed receipts, and every real transition must validate progression revision before publishing its receipt or marker. Keep shared transaction publication deterministic by applying the non-future comparison only against authoritative boot/gameplay request time.
- Route action-120 `TutorialEnded.EndReason` through the same exact nonnegative C# signed-int decimal parser as `GameEnded` before the Win-only gate. Missing, null, Boolean, array, blank, padded, signed, leading-zero, fractional, exponent, or oversized input must never reach tutorial currency, placement, receipt consumption, or completion-marker mutation through JavaScript coercion.
- Bind each real-money purchase action to its reviewed entitlement family; sharing store-proof verification must never let a token cross into an incompatible response or grant path. Validate the complete private player account envelope after every direct or background full-document reload and before delivery, subscription revalidation, voided-purchase reversal, or post-delivery boot projection. A genuinely missing player may retain its explicit recovered/reconciliation behavior, but an existing damaged player must abort the whole transaction before a grant, reversal, or terminal receipt marker makes the provider event non-retryable.
- Keep every subscription receipt on one complete provider-polling lifecycle. A nonterminal, non-orphan receipt requires a Date retry cursor strictly after its latest verification audit; terminal Play states require revocation evidence and no cursor. Query malformed/missing cursor rows into the leased sweep, validate the whole selected batch before its first provider call, and validate retry, deleted-account retirement, and provider-result successors before writing them.
- Resolve Google Play subscription cadence, scheduler interval and batch size, voided-purchase scheduler interval, and RTDN interval/batch/terminal retention together once during module startup as one immutable exact provider-policy snapshot. Initial receipt creation and all later subscription checks must share the same 300-to-86,400-second cadence; subscription polling is 30-to-3,600 seconds with batches of 1-to-1,000, void reconciliation polls every 60-to-3,600 seconds, and RTDN uses an exact 10-to-3,600-second interval, 1-to-1,000-event batch, and 30-to-365-day terminal retention. Never floor, clamp, substitute, or independently re-read malformed provider settings, because invalid dates or mismatched cursors can consume purchase proof without recoverable polling authority.
- Resolve Google Play purchase enablement, dependent subscription/void/RTDN-worker enablement, and the exact Android application ID as one immutable startup policy shared by delivery, receipt validation, subscription polling, void reconciliation, and push ingestion. Require a bounded dotted Java-style identifier without trimming; RTDN requires all three provider workers and no background worker may be enabled while purchase authority is disabled. Do not let different provider paths independently re-read package identity, because the package prefixes every recovered SKU and durable receipt binding.
- Treat Google Play RTDN only as an authenticated durable wake-up. Verify Google's OIDC signature and exact audience, issuer, verified service-account email, Pub/Sub subscription resource, Android package, canonical Base64/UTF-8 JSON, event time, and one mutually exclusive supported notification before insertion. Bind each decimal `messageId` to its payload digest, store purchase tokens only as the shared HMAC and pending-refund tokens only as event-bound AES-GCM ciphertext, acknowledge only after durable insert or exact replay, and reuse `subscriptionsv2.get`/Voided Purchases authority before changing entitlement. One-time/test notifications remain audit-only. For pending refunds, require an admin-authenticated exact actor/idempotency-bound recommendation, validate the official evidence shape, and seal its canonical encrypted outbox before the first provider call. Because Google records only the first accepted `orders.reviewrefund` call, retries may resend only that identical intent; atomically close the review and RTDN ledgers after success, never expose the token or detailed evidence on reads, and leave entitlement changes to later provider authority.
- Validate every complete durable Google Play receipt before replay, replacement, subscription revalidation, void reconciliation, overlap preservation, or insertion. Require its exact HMAC identity, platform, bounded owner/order, catalog-bound kind/store product/response, ordered safe dates, source-exact reversible grant, encrypted subscription token/state/expiry/retry shape, and all-or-none void metadata. Only legacy currency receipts may derive a missing reversible grant from their exact catalog-bound response; pack ownership history must never use that migration. Advance subscription retry counters with a bounded compare-and-set so concurrent workers cannot silently overwrite authority.
- Validate every shared scheduled-job lease before acquisition and after compare-and-set. Require exact fields, a bounded control-free job ID, UUID owner, safe ordered dates, and a one-second-to-24-hour duration; malformed expiry must surface as damaged authority rather than permanent contention. Validate the Google Play voided-purchase cursor before it selects a provider window and before publication: exact singleton ID, equal successful-end/update timestamps, safe dates, and no future cursor that could skip provider events.
- Commit every external-identity link, relink, and unlink as one MongoDB transaction with its sparse player-profile mirror and active `AccountType`. On unlink, preserve the current provider when its authority row remains; otherwise choose the remaining oldest row deterministically, and use Guest only when no external identity survives. Before provider authentication or an open provider-existence response, require the authority row's player ID and external ID to match the exact connected root/DTO mirror, reject disconnected sentinels and missing owners, then re-read and reverify the final row after credential comparison so concurrent ownership or credential rotation cannot authenticate a stale owner. The identity collection remains login authority, and a crash must never expose a credential that Unity does not display or display a provider whose credential was already removed.
- Parse `LoginToCustomAccount.AccountType` once as only the canonical recovered decimal values Guest 0, Facebook 2, Game Center 3, or Google Play 4, and share that result between dispatcher credential-family selection and response projection. Missing, null, Boolean, array, blank, signed/space-padded, leading-zero, fractional, exponent, unsafe, or unknown values must never select a provider digest/index through JavaScript coercion; malformed response metadata may fall back only after authentication has declined provider selection.
- Keep the recovered HTTP authentication envelopes distinct. `LoginToCustomAccount` authenticates with its dedicated `Id`/`Password` pair, while ordinary authenticated actions use only the common `PlayerId`/`Token` pair injected by `BeanstalkServerManager`; documented JSON aliases may coexist only when byte-identical. Never use an ordinary action's `Id` payload or `Password` mutation value as fallback session authority, and never use a `Token`-only request as the explicit login credential path.
- Normalize Facebook identity IDs as the canonical invariant string form of a C# signed 64-bit `long`, preserving large values as strings and reserving exact `-1` for the recovered disconnected sentinel. Link/create mutations must reject noncanonical, fractional, or out-of-range IDs; login and open existence lookups must treat them as no match. Google Play and Game Center retain their bounded nonempty string contract.
- Parse every platform identity ID and credential as the exact string emitted by the recovered form request. Never stringify JSON numbers, trim opaque credentials, or choose one of conflicting legacy/camel-case aliases: Facebook precision loss and alias order must not redirect an existence lookup, identity link, or login digest. `RemoveOrUpdateGC.haveGcId` must be exact string `0` or `1`; malformed or missing input must never default to the destructive unlink branch.
- Validate the complete durable platform-identity row at its shared lookup boundary before login, ownership proof, or open existence responses. Provider and external ID must use the supported canonical contract, player ID must be bounded and nonempty, credential HMAC must be exact lower-case 64-character hex, display name must retain its trimmed bounded storage shape, and finite audit dates must satisfy `createdAt <= updatedAt`. Malformed rows are damaged authority, not absent or usable identities.
- Validate the private player account envelope on shared full-document lookup and insertion. A present gameplay session must be exact lower-case 64-character HMAC text; custom-password authority may be only the exact legacy HMAC migration form or a canonical supported `scrypt$v1` digest. Apply that same credential proof to narrow authentication projections after both successful session compare-and-set and concurrent-winner reload; a projection must never become a weaker path that returns a malformed durable token. A missing session remains allowed solely so a proven password/provider login can rotate one for a legacy row. Require a bounded player ID, canonical optional normalized name, trimmed bounded device token, and finite ordered player audit dates before authentication or gameplay uses the document.
- Keep the private player-account proof callable separately from duplicated profile-mirror proof for an explicitly authorized operator repair. Squad-integrity repair may tolerate only the squad mirror damage it is designed to replace, but must validate credentials, identity/name/device shape, audit dates, exact audited `updatedAt`, and any progression successor before writing. Ordinary full-document consumers must continue to apply both proofs.
- Revalidate that complete player account envelope on every raw full-document retry or transaction reload that authorizes an economy/profile mutation. This includes Army Power refresh, Daily Missions, Instant Battle, PvP settlement, inbox reward claims, Player League allocation and its complete settlement population, Squad creation/join/leave/rank/leadership/kick, Squad-card withdrawal participants, and Squad-chat sender publication. Handler authentication proves an earlier snapshot only; a later damaged row must abort before rewards, ranking, inventory transfer, roster/indexed mirrors, messages, or terminal markers are published.
- Validate War Arena opponent candidates at the public/indexed profile-mirror boundary before using their IDs or Army Power. Opponent discovery consumes no private credentials, so it must not couple availability to private account fields, but a split root/DTO profile must never enter a battle offer.
- Validate the complete paid-VIP and exact four-field subscription timelines as nonnegative safe-integer Unix seconds at their shared read/publication boundaries. Paid VIP keeps expiry as the recovered active-benefit predicate, but its display start must be zero with no entitlement and otherwise not exceed expiry. Subscription authority must remain one ordered interval (`subscribeSince <= dogTagTimerLock <= expireTime`); activity requires `subscribeSince <= now < expireTime`, and boot must reject a future start before serializing the expiry-driven stock-client model. A non-finite entitlement expiry must never become permanent access through JavaScript comparison behavior or an invalid durable receipt date.
- Validate the server-authored source upgrade duration as a nonnegative C# signed `int` inside the shared subscription delivery calculator before applying the recovered float32 0.8 multiplier. Do not floor fractions, clamp negatives to instant delivery, or allow non-finite/oversized values to create malformed weapon or unit receipts; validate the discounted result before publication too.
- Validate application time for weapon and unit upgrade receipt creation, normal activation, and instant completion as a nonnegative safe Unix second bounded by JavaScript/BSON Date support. Add delivery duration with a checked deadline boundary; never floor or clamp malformed time because that can publish an invalid receipt, release an upgrade early, or reduce its server-priced instant cost.
- Parse the mandatory VIP and lootbox `discount` field as the exact canonical 0-99 decimal integer emitted by the recovered dialogs before any Gold mutation. Missing, null, Boolean, array, blank, signed, leading-zero, fractional, exponent, unsafe, or out-of-range values must never coerce into the authorized zero-discount path. Resolve VIP and lootbox product IDs only through own catalog properties at both handler and service boundaries; inherited object keys must not become product rows.
- Validate every present subscription authority receipt ID as the exact 64-character lowercase hexadecimal HMAC-SHA256 key generated by purchase delivery before read, comparison, or publication. Absence remains the explicit legacy state; arbitrary readable IDs are test-only inputs and never durable authority.
- Validate the complete RequestBuffer replay cache and message-ignore outbox before lookup, acknowledgement, read, or publication. Replay entries must be bounded, uniquely identified by trimmed control-free BufferIds, contain safe Unix timestamps and parseable result arrays, and outbox IDs must be bounded, unique, trimmed, and control-free; malformed rows must fail closed rather than be discarded and reopen buffered economy actions.
- Parse every assignment and direct RequestBuffer integer through the exact nonnegative C# signed-int boundary. Require `Requests` keys to be the exact contiguous decimal sequence `0..Count-1` produced by `requests.Add(requestCount++, value)` and every nested `Request` to contain exactly the recovered lower-case `action` and `data` fields before comparing `Count` or processing any item. Upper-case aliases, extra or missing members, arbitrary, padded, negative, duplicate-equivalent, gapped, oversized, or nonnumeric keys, and JavaScript-coercible field values must reject the whole envelope before any per-action result or progression mutation.
- Parse nested daily/starter assignment claim IDs and currency echoes, Event Assignment reward/index echoes, and achievement IDs/progress cursors as exact nonnegative Int32 JSON numbers before feature logic. Achievement `Offset` is the sole signed exception because the recovered client derives it from pre-achievement local statistics; it must still be an exact signed-Int32 JSON number. Never use `Number(...)` to interpret these buffered Json.NET dictionary members.
- Preserve the two recovered buffered card-pack shapes: `BuyThreeCards` omits `discount` and `StartTime`, while `BuyCardPack` includes both as Json.NET JSON numbers backed by C# `int`. Apply the zero fallback only to true absence; explicit null, Boolean, string, array, fractional, negative, unsafe, or oversized values must reject before pack eligibility, debit, grant, or replay publication.
- Keep replacement-client card-pack selection distinct from both recovered shapes. Only an exact `ServerSelect: true` object with the reviewed `cardPack`/optional `discount`/optional `StartTime` keys and no `cards` or aliases may request server-selected identities. Use the enclosing validated `BufferId` as the durable operation nonce, persist the exact returned `Cards` array in its response receipt, and keep debit plus inventory grant in the same progression replacement; a retry must never roll again or charge twice. Stock client-selected arrays remain validation assertions because the unmodified success parser cannot apply replacement identities.
- Preserve recovered weapon, unit, and visual inventory numeric transports before price, ownership, loadout, delivery, conversion, or replay logic. Every dictionary member declared as C# `int` must be an exact signed-Int32 JSON number, the weapon/unit Gold coefficients must be finite JSON numbers, and only `WeaponScreen.EquipWeapon.Index` may be canonical nonnegative Int32 decimal text because that call site explicitly uses `ToString()`. Do not default a mandatory `discount` or `deliveryReduce`; missing, null, Boolean, array, string, fractional, non-finite, or out-of-range values must fail in the parser while signed values continue to the feature's recovered negative-value error branch. For `UpdateEquippedUnits`, retain ArmyScreen's sparse map exactly: a present `wasEquipped` or `equipped` member must be JSON `true`, absent means false, at least one member must be present, and no unknown nested member is allowed.
- Keep actions 125/126 as zero-delivery purchase acknowledgements only. They must prove the earlier permanent non-rental unit/weapon purchase, an empty corresponding delivery receipt, exact recovered Gold coefficients, and `ExpectedPrice=0`, then preserve state identity. Any nonzero delivery row remains unsupported until its full source lifecycle is recovered; these actions must never become a second debit/grant or consume an upgrade receipt.
- Parse action-193 RequestBuffer data only as the canonical nonnegative C# signed-int decimal emitted by `PlayerAnalytics.lastSeenSquadChatTimeStamp.ToString()` before applying request-time cursor validation. A repaired-client direct action must supply exactly one recognized timestamp alias through the exact C# Int32 form/JSON-number boundary; conflicting aliases and JavaScript-coercible values must fail before cursor comparison. Empty, padded, signed, leading-zero, fractional, exponent, or oversized buffered text must remain one per-action `UnknownAction` result, must not mutate the buffer working state, and must not escape the recovered per-action isolation.
- Resolve `WARBUCKS_GOLD_VARIANT` once during module startup as one immutable exact literal, `standard` or `b`; never trim, case-fold, or let an invalid injected service value fall through to the standard curve. Validate the complete action-221 WarBucks conversion receipt before replay lookup, read, or publication. Accept only the twelve recovered A/B IDs with their exact source Gold debit, a positive safe grant, safe time, and a non-future progression revision; malformed receipts must fail closed rather than be overwritten and reopen a recent conversion. Keep the missing production experiment assignment explicit.
- Validate the complete paid-VIP daily-card marker before grant comparison, read, or publication. Its UTC day must derive exactly from a safe non-future grant timestamp and its two reward IDs must be implemented recovered Silver/Gold cards, with duplicates allowed; malformed markers must never be overwritten to reopen the daily pair.
- Validate the private War Card play and Gold-card craft lifetime counters as nonnegative safe integers at shared read/publication and reward-derivation boundaries. Missing legacy counters normalize to zero; malformed values must never satisfy starter assignments, achievements, or survive an unrelated progression replacement.
- Validate the complete `CraftData` receipt before boot projection, crafting actions, failure serialization, or progression publication. Only the exact empty zero tuple or three implemented same-rarity Bronze/Silver IDs with the exact source duration is valid; both timestamps must fit the recovered signed C# `int`, and malformed partial state must fail closed rather than reopen already-consumed inputs.
- Validate the complete five-field `CardManagerData` snapshot before boot projection, card gameplay, failure serialization, or progression publication. Normal ownership must remain sparse positive signed-client counts of implemented IDs; Buddy ownership must remain within the source cap with exact bounded loadout records and source-backed unit/weapon indexes; cooldowns must fit the recovered signed C# `int`; and `extraSlot` must be Boolean. Only wholly missing legacy inventory may materialize empty defaults.
- Require the authenticated zero-based player level to identify one exact recovered rank row before card-pack unlock checks or mission-card `FROMMISSION` pool selection. Use the row's one-based display number only for the recovered mission threshold; never floor/clamp `NaN`, infinity, fractions, negatives, or out-of-catalog indexes into unlocked card economy.
- Validate the complete `InventoryData`/`LevelManagerData` snapshot at shared progression read/publication and before mutation or serialization. Require the exact four recovered weapon slots with source-backed identity/index/category/ownership, exact saved weapon/unit record shapes with bounded catalog cursors and consistent ownership flags, the recovered two-per-deployment-category and three-mechanical equipped-unit caps, and either the exact empty delivery tuple or one active delivery matching a permanent saved upgrade cursor. Only a wholly absent legacy item inventory may materialize the recovered starter loadout; malformed nested authority must fail closed rather than be defaulted or survive an unrelated replacement.
- Validate the complete rental lifecycle at shared progression read/publication and before boot projection, loadout, Army Power, acceptance, settlement, or permanent redemption. Only source-backed weapon/unit types, 20-25% discounts, safe timestamps, and status-consistent trial/sale intervals may authorize borrowed inventory; corrupt deadlines must fail closed rather than become permanent access or survive an unrelated replacement. New generation must require the authenticated zero-based level to match one exact recovered rank row before the eligibility gate, candidate window, or deterministic seed; do not floor malformed rank authority, while replay of an already-live durable offer remains level-independent.
- Validate the complete Black Market set at shared progression read/publication and before boot projection, action-217 replay/rotation, serialization, or buffered weapon redemption. Expiry and issue cursors must be safe integers; offer identities must be unique and bounded; feature authority remains zero until the retired weighting source is recovered. Before issuing or rotating a set, require the authenticated zero-based player level to identify one exact recovered rank row; never floor or clamp malformed level authority into an offer, while an already-active replay remains independent of the caller's later level.
- Validate the complete visual inventory at shared progression read/publication and before boot projection, purchase, ownership/equip checks, paid-pack extension, or voided-purchase reversal. Require exact root, saved-row, and slot shapes; source-catalog visual IDs; catalog-bounded part counters; exactly the four recovered category slots; category-correct equipped IDs; and an empty or helmet-category previous-head ID. Timed `expiresOn` values must be bounded safe integers, and malformed or unknown rows must never become ownership, cross-category equipment, or survive an unrelated replacement.
- Derive visual achievement group 15 from the four recovered category defaults plus durable bought or parts-completed non-power-band visuals across every implemented grant path. The scene's 7/14/29 values intentionally correspond to the platform's 3/10/25 collected-equipment labels after those defaults; temporary or borrowed visuals must never advance the permanent collection reward.
- When inventory snapshot derivation follows a chargeback or voided purchase, preserve only the minimum achievement value required by an already-claimed tier. Do not lower the value beneath a durable ordered claim prefix, and do not preserve a larger unclaimed/imported client total.
- Keep account sanctions as audited server-owned records. Enforce active bans through the shared authentication path only after credential proof, evaluate temporary expiry from application time rather than TTL cleanup, return the recovered `AccountBanned` fields, and require retry-safe operator issue/revoke keys. Validate the complete sanction before authentication enforcement, issue/revoke replay, operator publication, appeal intake, or accepted-appeal mutation: require exact known fields and normalized identities, safe ordered dates, an exact optional duration/expiry pair, and status-consistent resolution/revocation evidence. Query the active slot without an expiry predicate so a malformed Date fails closed instead of hiding from MongoDB comparison.
- Keep moderation report review transitions optimistic and append-audited: require the operator's expected status, never reopen a terminal report, and replay only an identical actor/decision/note operation.
- Keep player appeals ownership-scoped and separate from stock gameplay actions. Accept one appeal per active sanction through the current session credential, expose no private sanction fields, require optimistic append-audited review/withdrawal transitions, and revoke an accepted appeal's live sanction in the same transaction. Validate the complete appeal before submission replay, owned/admin read, list, review, withdrawal, or decision replay: exact UUID/sanction/player/submission identity, normalized bounded message, safe dates, open creation equality, and a unique legal one-or-two-entry audit projection through exact `updatedAt` are required; malformed terminal status has no support authority.
- Keep moderation retention explicit and operator-driven: resolve the exact 30-3650-day report/appeal pair once during module startup as one immutable policy while sanctions remain indefinitely retained; freeze one preview timestamp for bounded export and purge; validate every eligible report and appeal lifecycle before either enters counts/exports or destructive authority; delete only the still-eligible validated IDs older than configured cutoffs; and publish purge counts in the same idempotent transaction as deletion. A forged terminal report or appeal must abort before the first delete, and preview/export/apply must never re-read different process-local cutoff policy.
- Validate the complete moderation-retention receipt before replaying its deletion counts. Require exact fields, UUID/operation/actor identity, safe nonnegative counts, safe ordered preview/creation dates, and report/appeal cutoffs that are exact whole-day intervals within the global 30-3650-day bounds. Use global bounds rather than current configuration so a legitimate policy change does not invalidate historical receipts.
- Rotate `AUTH_SECRET` through the bounded fallback-key overlap documented in `Server/README.md`: keep gameplay sessions valid, verify durable password/provider digests against fallbacks only, and compare-and-set rehash a successful fallback login with the active key before retiring the old key.
- Treat each stored custom-password scrypt factor as verification authority within the bounded supported range. Raising `AUTH_SCRYPT_COST` may upgrade a successful login, but a temporary lower setting must never downgrade an already stronger digest.
- Validate the complete persistent login-throttle row after atomic reservation and before a collision replay checks its lock. Require exact known fields, a lower-case HMAC key, bounded positive attempt/revision counters, safe ordered window/update/expiry dates, no future audit time, and a null or bounded post-update lock contained by expiry. Use the globally supported policy bounds so a legitimate deploy-time window/lockout change does not make an older row corrupt; malformed authority must neither become a permanent lock nor reopen credential checks.
- Validate login-throttle deployment policy as exact safe integers during module startup: maximum attempts 2-100, attempt window 60-86,400 seconds, and lockout 60-604,800 seconds. Never floor, clamp, or replace malformed credential-protection settings with hidden defaults; all reservations in one process must use the same immutable reviewed policy.
- Validate the complete persistent report-throttle row after atomic reservation. Require exact known fields, the expected lower-case HMAC reporter key, a safe positive counter bounded by the global maximum plus one denial sentinel, safe ordered window/update/expiry dates, a non-future update, and the exact storage-retention interval. Saturate repeated denials at that sentinel so attacker traffic cannot drive durable arithmetic unsafe; retain the original fixed-window start so continued spam cannot indefinitely postpone the logical reset.
- Validate the complete report-deduplication winner before it can be upserted into the moderation queue. Bind its exact HMAC key, immutable open-report shape, four semantic identity dimensions, UUID, bounded text/client-claim evidence, safe creation time, and exact ten-minute logical/twenty-minute storage windows. When ranked-match evidence exists, require its exact false-authority marker, the same two participants, source-valid snapshots, bounded claim maps, and a safe six-hour chronology; a concurrent winner may retain its own message/evidence but may never redirect the report identity.
- Parse both recovered player-report actions' mandatory `ReportType` as canonical nonnegative decimal text or an exact JSON integer within the existing bounded compatibility range. Missing, null, Boolean, array, blank, signed/space-padded, leading-zero, fractional, exponent, non-finite, and out-of-range forms must never enter moderation category zero or another queue identity through JavaScript coercion.
- Keep the recovered `ReportedPlayerId` transport exact: reject surrounding whitespace, controls, non-strings, and overlong identities instead of trimming or truncating a request into a different account. Reject an invalid or overlong required player message instead of retaining partial evidence; optional cheater-report messages may be absent. Client evidence remains non-authoritative and malformed, non-finite, control-bearing, or overlong claims must be omitted whole rather than sliced into believable values.
- Validate every durable moderation report before admin detail/list publication, review, or review replay. Treat status as a projection of an exact one-or-two-entry audit chain from `open`: operation IDs must be unique and bounded, transitions legal, actors/notes bounded, timestamps safe and ordered from report creation through the exact `updatedAt`, and terminal notes nonempty. An open row must have neither review history nor an update timestamp; malformed status alone is never operator authority.
- Keep ordinary offline-bot battles outside ranked settlement. Their action-64 metadata may select a durable zero-reward lifecycle only after server-owned Play Warcards eligibility is checked; client-simulated bot results must never grant or consume economy, progression, cards, rentals, achievements, assignments, leagues, or events.
- Keep action-3000 replay publication admission in one durable per-player rolling ledger. Validate the complete ledger before pruning, seed first use from valid recent legacy receipts, reserve distinct URL hashes atomically across nodes, and let an already-reserved exact hash recover the reservation-to-receipt crash window without spending another slot; malformed timestamps or entries must never be discarded as expired to reopen the 20-per-24-hour allowance.
- Resolve matchmaking, post-pair join, disconnect grace, and REST result consensus once during module startup as one immutable exact multiplayer-timing snapshot. The first three must be positive safe integer seconds whose millisecond conversion fits Node's timer range; result consensus must be an exact integer from 0 through 15,000 milliseconds. Use the same frozen matchmaking value for local timeout payloads and Redis stale cleanup, the same disconnect value for local and distributed resolution, and the frozen consensus default for every REST bridge; never floor, clamp, substitute, or re-read mutable configuration into one match lifecycle.
- Treat `roomStartedAt` plus the complete two-player `joinedPlayerIds` set as the exact durable ranked-PvP gameplay boundary. Disconnect markers, result/used-card reports, relayed card plays/deliveries, and finished reward authority require that proof at complete-document validation and every mutation compare-and-set; neither a timestamp alone nor two joins without the one-time start marker may authorize combat evidence or settlement. Preserve no-reward pre-start cancellation while `roomStartedAt` is absent.
- Parse lifecycle Booleans according to their recovered transport before selecting a gameplay branch. Actions 64/65 use exact `"0"`/`"1"` form flags and action 62 uses optional exact `"1"` markers; action 138 uses exact C# `"True"`/`"False"`. JSON adapters may send actual Booleans, but numeric and alternate-string aliases must not select ranked, friendly, tutorial, Arena, trial, or permanent-purchase transitions.
- Parse legacy Squad `IsPublic` through the same exact `"0"`/`"1"` form-or-JSON-Boolean boundary before mapping it to the inverse `JoinPolicy` enum. Numeric and alternate-string aliases must never silently change admission between open and request-only.
- Parse the repeated `DatabaseAction`/`requestId` routing value as one canonical nonnegative C# `int`; the URL and body copies must match only after both independently pass that grammar. Do not use JavaScript `Number()` equality because nulls, Booleans, arrays, whitespace, leading zeros, fractions, and exponents must never reach an action handler or the raw configuration fast path. Validate `MIN_CLIENT_VERSION` during dispatcher startup as an exact integer from zero through C# `Int32.MaxValue`, where only zero disables the replacement-client gate. When enabled, require every supplied `ClientVersion`/`clientVersion` alias to use the same canonical value; malformed or conflicting claims must fail instead of using precedence or letting `NaN` bypass comparison. Stock dotted `Version` remains a separate transport field.
- Apply that same separation to remote-configuration publication targeting. A publication with an integer client-build bound requires one canonical, nonconflicting `ClientVersion`/`clientVersion`; malformed or conflicting aliases fail targeting, stock dotted `Version` must never enter numeric comparisons, and an unbounded publication may still target the recovered client.
- Parse stock action-157 targeting from exact string `abTestVariant`, `SheetConfiguraton`, and `Language`. An empty Fuse variant is valid, but missing, numeric, padded, control-bearing, delimiter-bearing, or conflicting cache-ID aliases must never become wildcards or a sanitized release identity. Keep the raw parser callback-safe with `success;0;{}` while withholding every publication from malformed requests; documented replacement cache aliases may survive only when all supplied copies agree exactly.
- Bind a partial remote-configuration rollout only to one exact, bounded, control-free, agreeing canonical `PlayerId` or documented lower-case JSON `id`. Treat the recovered common builder's pre-login `null`/`nullId` values as sentinels with no player bucket, and never borrow upper-case explicit-login/action `Id` for rollout identity. A 100-percent publication needs no player identity; a fractional publication must fail targeting when identity authority is absent or conflicted.
- Validate signed remote-configuration manifests as an exact authority schema before any publication can be selected. Reject unknown or missing root/publication/sheet fields, duplicate language selectors, unsafe build bounds, and any rollout percentage that is not an actual finite numeric value from 0 through 100; a valid HMAC must never make an ignored typo or JavaScript-coercible policy authoritative.
- Preserve manifest order as explicit priority for intentional specific-to-wildcard remote-configuration fallbacks, but require the language wildcard to stand alone and reject canonical duplicate variant/language/build/rollout targeting envelopes at startup. Two semantically identical selectors must never let an array reorder change live balancing.
- Parse every `GameEnded.EndReason` and `MissionIndex` through one exact nonnegative C# signed-`int` decimal boundary before tutorial, War Arena, Daily Mission, friendly, or ranked settlement. Missing, null, Boolean, array, blank, signed/space-padded, leading-zero, fractional, exponent, unsafe, and oversized inputs must never become a reward-bearing value through JavaScript coercion; the optional replacement-transport `WinnerId` alias is an assertion only and must exactly match the participant winner derived from a supported recovered `EndReason`. It must never override, contradict, or supply semantics for that mandatory field.
- Preserve presence when parsing REST `GameEnded.UsedCards` and WebSocket `MatchResult.UsedCards`: the stock client sends a JSON string and replacement transports may use a native array or omit the field as an empty-list shorthand. Only true absence may select `[]`; explicit null, Boolean, numeric, object, malformed JSON, oversized, or invalid card-list input must fail before result consensus instead of being defaulted into a zero-consumption report. Keep this rule centralized so local-room and distributed WebSocket paths cannot drift from REST.
- Validate the replacement WebSocket `MatchResult` payload as an exact runtime object before room or MongoDB lookup. Require bounded trimmed control-free `MatchId` and `WinnerId`, allow only optional `UsedCards` and opaque non-authoritative `Stats`, and reject missing identities, alternate-case aliases, or extra fields. TypeScript interface casts are never input validation and must not select consensus authority.
- Apply the same exact runtime-wrapper rule to WebSocket `JoinMatch` and `MatchEvent`: JoinMatch permits only one bounded `MatchId`; MatchEvent requires bounded `MatchId`/`Event` and permits only optional `Data`. Keep unrecovered non-card Data opaque and non-authoritative. For `CardPlayed`, require `Data` to contain exactly JSON-number `Sequence` and string `CardId`, canonicalize it once through the shared card identity bounds, and use that pair for local or cross-node evidence and delivery. Reject nested extra fields and alternate aliases before durable admission or relay.
- Validate WebSocket `Identify` as the exact two-field `PlayerId`/`Token` runtime object before authentication. Both values must be bounded, trimmed, and control-free; reject missing fields, alternate-case identities, password aliases, and extra keys. Authentication remains the credential proof, but no socket may present competing descriptions of the immutable player binding.
- Validate every replacement WebSocket root immediately after JSON decoding as an exact envelope with one bounded trimmed control-free string `Type` and only optional `Payload`. Keep Payload opaque until its message-specific validator, but reject primitive/array roots, missing or malformed Type, alternate-case aliases, and extra root keys before logging or dispatch.
- Resolve stock `BattleId` and replacement `MatchId` as exact aliases for one match lifecycle. Preserve true total absence/legacy empty BattleId for the downstream tutorial migration only, but require every present alias to be a string and require two present aliases to be identical. Explicit null, non-string, blank-versus-nonblank, or conflicting identities must never select settlement authority through nullish fallback or MatchId precedence.
- Validate every in-memory or Redis matchmaking `QueueEntry` as exactly its four recovered fields before enqueue, restoration, or return: bounded player identity, finite nonnegative Army Power with a signed-client-safe projection, one Bronze3-through-Champion tier, and a positive safe millisecond clock. Validate a complete restoration batch before deduplication or mutation. Redis Lua must protect JSON decoding, bind hash identity/time to the sorted-set member/score, and remove both halves of a malformed transient row instead of aborting healthy matching; MongoDB reload and transactional admission remain final gameplay authority.
- Distinguish deliberate no-Redis local rooms from a failed Redis socket-ownership write. A socket may enter distributed PvP only after its exact player/instance/client owner is stored; an attempted failed claim must reject the new socket without evicting the older valid login, and uncertain cleanup may compare-delete only that prospective owner. Never use the mere presence of a heartbeat timer as proof that a cross-node player route exists.
- Read distributed PvP socket value and PTTL atomically before disconnect settlement. Only the exact instance/client UUID pair with a remaining one-to-30-second lease proves “connected”; a missing exact observation proves offline, while malformed, non-expiring, overlong, or incoherent state is unknown and must extend grace rather than authorize a forfeit or cancellation.
- Keep a live WebSocket bound to its first authenticated player identity. Reject and close a second `Identify` before credential or route mutation; account switching requires a new connection so the prior `onlinePlayers` and Redis owner cannot address a transport that has changed accounts.
- When Redis coordination is selected, require the first crash-expiring PvP coordinator heartbeat to commit before server startup continues. Durable matches must never name a coordinator UUID that the node failed to publish; unknown runtime liveness remains a conservative wait rather than permission for peer orphan recovery.
- Read PvP coordinator owner and PTTL atomically before orphan recovery. Only the exact coordinator identity with a remaining one-to-30-second lease proves alive; the exact missing tuple proves dead, while malformed, non-expiring, overlong, or incoherent state is unknown and must preserve the match for a later observation.
- Treat every Redis PvP fan-out message as a bounded transport hint. Require exact notice/envelope/type-specific fields, reload the complete validated match, and bind opponent name, joined/start state, terminal winner/reason, conflict reason, and disconnect state to it before client delivery. CardPlayed must also reproduce the authenticated source's durable sequence/card evidence and be receiver-deduplicated. A closing local socket is not a successful delivery; committed lifecycle failures must notify remote participants through the same validated path. Opaque combat events remain non-authoritative.
- Parse replacement-WebSocket `CardPlayed.Sequence` only as the exact JSON integer number declared by the relay contract, bounded to the recovered six-card selection. Never apply `Number()` to client or fan-out sequence input: null, Boolean, blank text, numeric text, and arrays must not coerce to sequence zero and create or replay durable card-use evidence.
- Never treat Redis publish success or pre-delivery `CardPlayed` evidence as delivery of an effect. Both local-room and cross-node paths must append the same durable delivery marker only after the live opponent socket send, and the sender may acknowledge the play only after observing that marker. Delivery indexes must be the exact contiguous zero-based prefix of recorded plays; terminal `UsedCards`, normal settlement, and disconnect-forfeit fallback may consume only that delivered prefix. If marker persistence fails after a local send, retry the marker without replaying the effect on the same process.
- In the no-Redis `RoomManager`, require every join to reproduce one exact two-distinct-player durable allowlist and enforce one process-local room per authenticated player. Reject a contradictory pair or second-room join before creating/mutating a room; finish/eviction may clear the player index only when it still points to that exact match.
- Require both assigned participants to be currently present before any process-local event relay reports success. The room retains active lifecycle state during reconnect grace, but that state alone must never acknowledge an opaque event that had no connected opponent recipient.
- Use the same durable `joinedPlayerIds` and one-time `roomStartedAt` transition for no-Redis and distributed `JoinMatch`. Preflight exact process-local admission without mutation, persist the authenticated join, then attach the socket using the reloaded durable pair. If an unexpected local conflict appears after that write, cancel and release the match fail-closed; never retain a durable started row without corresponding local room authority.
- Keep `JoinMatch` response ordering identical across transports: the handler whose durable join activates the pair must send its `MatchJoined` acknowledgement before `MatchStart` fan-out. `RoomManager` may transition local registry state but must not emit the start message from inside that mutation.
- Treat durable `joinedPlayerIds` as retryable admission history, not current socket presence. Before the one-time `roomStartedAt` winner emits `MatchStart`, require the exact two process-local room participants or two definite expiring Redis routes. A missing or unknown participant cancels the unproven activation and releases both profiles; never start one client from a stale pre-start join.
- When `CancelMatch` no longer owns a matchmaking queue entry, resolve the authenticated player's durable unstarted assignment before acknowledging success. Participant cancellation may atomically release the exact pair only while `roomStartedAt` is absent and must notify both nodes from the committed terminal row; once started, cancellation is a gameplay result/forfeit path and must be rejected.
- Treat `RoomManager` result reports only as current socket-membership validation; MongoDB `resultReports` and terminal match state are the sole consensus authority. After each conditional report write, recognize and replay an already-finished immutable receipt or `result_conflict` cancellation before requiring the row to remain active. A stale local pending/conflict observation must never override the durable result, and cancellation/settlement races must be resolved by re-reading the terminal row before notifying either client.
- Permit an authenticated assigned participant to recover a lost WebSocket terminal response after the transient local room has been removed, but never use that exception to bypass active-room membership for pending or confirmed reports. Both local and distributed result-conflict paths must clear join/disconnect timers; the local path must broadcast before deleting the cancelled room so opaque relay cannot continue after durable cancellation.
- Bind distributed disconnect-forfeit settlement to the exact durable loser timestamp captured by its grace timer, and repeat that proof inside the reward transaction. Reconnect clearing must compare-and-unset only the timestamp it read; it must not erase a newer disconnect. If the marker is cleared or replaced before the guarded claim, leave the active match and its newer timers untouched and publish no forfeit.
- Persist the same exact disconnect markers for process-local active rooms. A both-offline cancellation on either transport requires one exact marker for each assigned participant and must repeat the complete timestamp snapshot proof inside the cancellation transaction. Missing, cleared, or replaced peer authority means wait another grace window; never convert an out-of-transaction liveness observation directly into unconditional cancellation and presence release.
- Bind reconnect clearing on both transports to the exact disconnect timestamp returned by that `JoinMatch` durable snapshot. Require the expected marker at the clear API and compare it again after reload plus in the update filter, so a socket that closes again while its reconnect handler is in flight retains the newer generation's offline authority.
- Validate every complete durable ranked-match row before room admission/start, disconnect or presence authority, relayed-card mutation/delivery, result consensus, moderation correlation, cancellation/restart recovery, settlement, or terminal replay, and validate each projected successor before its write. Require an exact UUID match identity and two distinct bounded server snapshots; participant-only unique joined/disconnect/report/card maps; safe ordered lifecycle dates; state-consistent active, cancelled, or finished fields; exact two-player terminal reward receipts with internally consistent VIP multipliers, streak/league/level/lootbox/rental fields; and bounded participant-complete Squad Event/War projections. Only an absent legacy `rentalSettled` marker may enter the existing idempotent repair path; false/malformed markers or a rental payload without the marker are invalid. Player IDs used as MongoDB dynamic paths must contain neither `.` nor `$`; damaged rows must never protect presence, consume cards, create evidence, or replay rewards.
- Load and validate each complete durable player account and its projected successor inside the transaction before ranked-match admission or a presence mutation. Cancellation must bind exactly both assigned participant IDs; restart recovery must freeze its exact stale-player set; both must verify that MongoDB matched every selected account before commit. A narrow projection may be used only for a proof-scoped credential, compare-and-set, uniqueness, or existence check that cannot write broader account authority; it must never let a status/heartbeat update normalize a damaged credential, profile, or progression document or hide a missing participant behind partial bulk success.
- Validate the complete durable player account before publishing a public `DatabasePlayer` or leaderboard projection. Search, friend, squad-roster, challenge, and ranking views must fail closed when private credential or audit authority is damaged even if the duplicated public mirrors and progression remain believable.
- Validate the complete friendly/offline-bot participant receipt before start replay, `GameEnded` discrimination, settlement, or terminal replay. Require exact known fields, bounded control-free player/battle IDs, action 64/65, one supported zero-reward kind, safe ordered dates, exact 24-hour active or seven-day terminal expiry, and an end reason only on finished rows. Missing `battleKind` is the sole legacy direct-challenge migration. Enforce expiry from application time and in the settlement compare-and-set because asynchronous MongoDB TTL cleanup must never extend gameplay authority.
- Parse action-2 challenge requests as the exact fields emitted by `BeanstalkServerManager.Challenge`: required bounded identities/map/game/region/room/version, optional bounded mission fields only when present, exact C# decimal integers, supported game/region enums, and no JavaScript coercion of nulls, booleans, arrays, blanks, fractions, exponents, or malformed values into defaults. Validate the complete type-0 challenge inbox message before insertion, duplicate replay, inbox publication, acceptance, or concurrent acceptance replay. Require exact known fields and recovered payload keys, a parseable bounded opponent snapshot, a sender-and-created-time-derived message ID, an ordered 1-minute-to-7-day TTL, and accepted/read/timestamp consistency with no reward or idempotency fields. A recent room-name lookup may replay only when every stored payload and participant field equals the new request. Use one captured application time to reject future or expired invitations. Resolve new-challenge lifetime once during module startup as one immutable exact safe integer from 60 through 604,800 seconds; never floor, clamp, replace, or re-read malformed deployment authority, while historical rows continue validating their own bounded interval.
- Validate every supported durable inbox row through its complete type-specific authority before insertion/replay, pagination, read/ignore mutation, serialization, claim, or acceptance. The recovered `HHFHFANGCEJ` base constructor parses the final dash-separated `MessageId` segment with `Convert.ToInt32`, so all message families must end IDs with their positive bounded creation Unix second; millisecond, UUID-final, or text-final IDs are invalid. Keep recipient plus message ID unique because stock read/ignore/accept carries no stronger row identity; replay an identical concurrent challenge winner, but reject a different same-second challenge collision. For types 3/10/21/27/28, bind exact lifecycle fields, recovered payloads, sender/recipient or server-owned Squad identity, source-valid returned cards, and server retry identities; validate projected read/ignore successors transactionally, and reject future rows or unsupported types instead of publishing a partial inbox page.
- Keep WebSocket inbox fan-out downstream of the durable insert and, for transactional producers, outside the retryable transaction callback after commit. Redis notices may contain only bounded origin, recipient, and message identities; every receiving node must reload the recipient-owned row, validate the complete type-specific authority and application-time challenge expiry, emit the same Dynamo projection as `GetAllMessages`, and suppress bounded duplicates. Live reward delivery changes presentation only and must never claim the row. Socket or Redis failure must never roll back the row or replace normal inbox recovery.
- Honor stock `GetAllMessages.MessagesCount` before the optional replacement `Limit` alias, retaining the bounded page cap and field-absence default but rejecting a present zero or noncanonical C# integer. Parse mandatory `GetFriendsInfo.Count` as a canonical nonnegative C# signed `int`, bound only the processed prefix, and validate every `FriendN` with the same canonical connected Facebook signed-`long` authority used by the identity index; padded, alternate, disconnected, or out-of-range hashes must never enter `$in` lookup.
- Reserve player-authored direct/challenge inbox traffic through one HMAC-keyed atomic fixed-window counter before insertion. Validate any existing complete exact row before the aggregation can fill missing fields, then validate the concurrent winner again; require safe ordered dates, exact retention, bounded saturated denial sentinel, and expected key. Resolve outgoing-message and report maxima once during module startup as one immutable exact abuse snapshot bounded respectively to 1-1,000 and 1-100; never floor, clamp, substitute, or re-read malformed limits after traffic starts. Resolve identical challenge replay before reserving, keep the original window start on denial, and validate the complete recipient account rather than trusting an ID-only projection. Never reintroduce count-then-insert admission across processes.
- Keep replacement direct-message targeting separate from the common authenticated `PlayerId`: only exact bounded `ToPlayerId` may select the recipient. Require matching body aliases, reject invalid/control-bearing or over-500-character text rather than truncating it, and apply moderation before recipient lookup or rate reservation so rejected text cannot probe account existence or consume sender capacity. Direct ReadMessage, IgnoreMessage, ClaimReward, and AcceptChallenge mutations must select rows only through exact bounded `MessageId`; never reinterpret authentication alias `Id` or buffered transport `ObjData` as direct row identity.
- Keep production MongoDB backups authenticated and encrypted before they leave temporary local storage. Retention may delete only an old, path-confined archive/manifest pair whose database, size, and SHA-256 all validate; malformed, damaged, unrelated, or orphaned files must remain for operator review.
- Keep monitoring artifacts on the bounded metrics contract actually exported by `metricsService`. Alerts must require sustained signals and meaningful traffic where ratios are used; Redis alerts apply only to scrape targets explicitly labeled `redis_required=true`.
- Treat every Redis Lua integer reply as an exact RESP-number authority boundary. Never use `Number(...)`, truthiness, flooring, or clamping to turn strings, Booleans, arrays, fractions, out-of-range values, or incoherent result tuples into rate-limit capacity, queue mutation, coordinator/socket liveness, room activation, or disconnect-forfeit proof; malformed coordination remains unknown and must fail closed or use only the bounded non-authoritative local fallback.
- Parse Redis `TIME` only as its exact canonical nonnegative seconds and six-digit-range microseconds tuple within JavaScript Date support. Distributed matchmaking enqueue, stale cutoff, widening, and restoration must reuse one Redis-time snapshot across nodes, and a candidate must remain strictly newer than the cutoff and no later than that snapshot before selection; bounded stale cleanup must never expose an older second page as eligible.
- Propagate only the generated UUID correlation identity to outbound Firebase and Google Play HTTP calls as `X-Request-ID`. Reuse the current validated request context when present, generate a fresh UUID for background calls, and never derive the header from player IDs, message IDs, purchase tokens, URLs, or malformed manually installed context.
- Keep Firebase monitoring identity-free and fixed-cardinality. Export only the reviewed provider outcomes, terminal suppression reasons, recovered-intent count, and sweep-failure count. A sent provider call increments only the `delivered` attempt; an ineligible row increments only `not_eligible` suppression; an invalid-token response increments the `invalid_token` attempt and, only after terminal ledger publication, the matching suppression. Never count delivery as suppression or relabel eligibility as token failure. Retry-ratio alerts must require meaningful attempt volume and sustained pressure, while worker-failure alerts must require repeated failures rather than one transient sweep.
- Resolve the complete HTTP and WebSocket traffic policy once during module startup as one immutable snapshot before Express proxy trust, local enforcement, or Redis-backed enforcement. Trusted proxy hops must be the exact reviewed 0-32 topology distance, HTTP/WS capacity must be 1-100,000, windows 1-86,400 seconds, HTTP memory entries 100-1,000,000, WebSocket payload 1,024-1,048,576 bytes, and violation tolerance 1-100. Express and the WebSocket listener must capture the same snapshot for client identity, parser allocation, message buckets, and disconnect decisions; never floor, clamp, replace, or independently re-read malformed security configuration.
- Parse Redis coordination, Google Play purchase/revalidation/reconciliation, and Squad Wars enablement only as the complete case-insensitive literals `true` or `false` while configuration loads. Missing values may use their documented defaults, but blank, padded, numeric, or otherwise malformed values must stop startup instead of silently disabling a safety worker or making deployment nodes disagree.
- Keep the Redis experience leaderboard strictly non-authoritative and bounded. Validate the complete Mongo-backed public rows before cache publication, atomically replace rather than append the current top-N sorted-set snapshot, reject duplicate/control-character identities or non-finite scores before Redis I/O, and apply one immutable exact 1-3,600-second TTL. A Redis failure may skip warming but must never block the MongoDB response.
- Keep global and country experience leaderboards in deterministic `(Experience descending, player ID ascending)` order and re-prove that every selected row matches its optional country filter and the returned order before assigning `Position`. Replacement-compatible global rank may count only strictly higher full account rows that each pass the same complete mirror, credential, audit, and progression authority as a visible item; a raw `countDocuments` over unvalidated indexed roots must never let a damaged account influence another player's rank.
- Keep action 101's global Squad leaderboard in deterministic `(Skill descending, squad name ascending)` order, where recovered wire `Skill` is server-owned `squadPoints`. The action's legacy `GetSquadsByExperience` name must never justify preselecting a truncated page by lifetime `experience`; validate every complete selected Squad document, the row cap, and returned order before publication.
- Treat a Squad's `SkillRequirement` as a gate against authenticated `DatabasePlayer.skill` (global medals), exactly as recovered `SquadRecord` does; `MedalsBalance` is the separate weekly Player League counter and must not authorize or deny membership. Action 81's client `Skill` field is an assertion that must equal the complete authenticated profile before it can select recommendations.
- Keep actions 79/81 directory reads bounded and fail-closed: action 79 is an anchored literal `SquadNameStart` prefix search, while action 81 may return only complete non-full open/request-required squads whose medal gate the authenticated player satisfies. The recovered model has no authoritative Squad country owner, so return `IsLocal=false` and retain an explicit global fallback rather than inventing local membership.
- Do not collapse non-open Squad policies at the admission boundary merely because the stock wire exposes both as `IsPublic=0`. A valid stored invitation may route the request button through the atomic join path, policy 1 may create one bounded manager request, and policy 2 must reject an uninvited request; full-document validation makes `maxMembers` authoritative, so do not revive a default-capacity fallback during join or request checks.
- Create a new Squad join request in a transaction that advances a strictly monotonic player-account revision and the exact Squad revision. This player write is the serialization fence against concurrent membership: if request creation wins, the joining transaction must re-read and revoke it; if joining wins, the retried request must observe membership and fail. An identical already-pending request remains a no-write replay and cannot recreate a row removed after its snapshot.
- Parse action 181's recovered decline fields by their actual caller semantics, not their misleading names: `MessageId` carries the applicant player ID and `Id` carries the manager's current squad name. Require the squad assertion to match authenticated membership, remove only that applicant under fresh manager authority, and treat an already-absent applicant as a no-write replay.
- Treat an action-59 Squad invitation as a bounded admission capability, not a membership write. Prove current manager authority before resolving the target account, reject either durable membership mirror with recovered `PlayerAlreadyInSquad` code `13301` plus the callback's `PlayerId` and current Squad `Name`, and return an identical already-pending invitation without advancing the Squad revision.
- Fence every newly created or legacy-materialized action-59 invitation by advancing the target player's strictly monotonic account revision in the same transaction as its Squad queue and type-1 row. Invitation creation and membership must contend on that shared player document so a retried loser either rejects current membership or exposes the invitation to the joining transaction's cleanup; an identical active invitation remains a no-write replay.
- Parse Squad admission identities by each recovered action's exact fields: action 59 accepts only `PlayerToInviteId` and derives the Squad from authenticated membership, while action 133 requires the canonical `SquadId` plus `PlayerToJoin` pair. Generic target/name aliases from promotion, kick, decline, or other actions must never select invitation or approval authority.
- Bind the remaining core Squad actions to their recovered fields: action 38 requires `NewSquadId` and treats optional `MessageId` only as an invitation capability identity; actions 132, 45, and 151 require `SquadId`; action 44 requires `SquadId` plus exact `CheckMessages=0/1`. Actions 49, 131, and 63 carry no Squad identity and must derive their mutable Squad only from authenticated membership; action 63 accepts only `Icon`.
- Parse action 124 only from the exact recovered `RoundId` field. Preserve the server-authored competition identity byte-for-byte and reject alias-only, numeric, padded, control-bearing, or malformed input before division lookup; never trim or coerce one request into another Squad Wars placement authority.
- Bind Squad creation, name availability, leader-event notification, and settings changes to their complete action-specific fields. Actions 37, 41, and 160 require exact `SquadId`; creation also requires bounded `Icon`, exact `IsPublic` (or a nonconflicting equivalent replacement `JoinPolicy`), and nonnegative `SkillRequirement`, while action 131 requires its policy plus nonnegative `RequiredMedals` and derives membership from the authenticated profile. Never borrow `SquadName`, `Name`, `NewSquadId`, generic `Id`, `RequiredMedals`, or `SkillRequirement` across these distinct actions, and never default a missing creation policy into an open Squad after charging WarBucks.
- Keep type-1 `SquadInvitation` messages and `invitedPlayerIds` as one admission capability. Action 59 must transactionally create both, exact retries must return the existing active row, and a partial unique index must allow at most one active recipient/Squad generation. Generic message decline must atomically mark the row terminal and remove the pending player; action 38 must bind `MessageId` to recipient and Squad, then atomically commit membership, pending-state consumption, and the terminal accepted marker. Only that terminal row plus existing roster membership may replay a lost response.
- When any join path commits membership, revoke the player's bounded set of every other Squad admission capability in that same transaction: remove the player from each validated invitation queue and join-request queue, then terminalize every other validated active type-1 row. A later leave must never reactivate an invitation or manager approval issued before the player joined another Squad; fail before the first write if cleanup exceeds its bound or any selected authority is corrupt.
- Bind Squad rank/removal actions to their exact recovered fields and authenticated Squad: actions 55/58 require `PlayerToPromoteId`/`PlayerToDemoteId` plus the canonical `OldSquadRank`, action 57 requires `PlayerToPromoteId`, and action 172 requires `PlayerToKickId`. Treat `OldSquadRank` as an optimistic roster assertion so a lost-response retry returns the stock full-roster rollback error without applying a second rank step.
- Keep `RemoveUserFromSquad` action 42 response-only. The recovered client has UI listeners for that event but no outbound action-42 request; do not expose it as a generic-alias second kick route or duplicate action 172's manager mutation capability.
- Resolve listener port, MongoDB pool size, and database name once before constructing MongoClient or running migrations. Require an exact port 1-65,535, pool size 1-1,000, and a trimmed platform-safe database name shorter than 64 UTF-8 bytes; never let invalid listener configuration surface only after migrations or workers have changed external state. Never log a configured MongoDB or Redis URL because either may contain credentials.
- Keep post-login broad analytics and impression acknowledgements authenticated. Action 179 may persist only the exact bounded `PlayerAnalytics` JSON string in the dedicated expiring diagnostic collection when immutable opt-in policy enables it; reserve its separate atomic HMAC-hidden rate limit first, validate the complete UUID/player/digest/byte/date/expiry row, and never read it into gameplay authority. Its missing operation UUID means equal payloads cannot be guessed into safe deduplication. Actions 194 and 1007 remain explicit authenticated no-op handlers until their retention semantics are recovered; do not move any of these actions back to the handler-less dispatcher fallback, which returns before session proof. Pre-login crash/error logging may remain a bounded non-authoritative no-op.
- Keep action 166 support logs authenticated and bound to the durable exact `SendLogs=1` player consent. Require the recovered `PlayerId` and `PlayerName` echoes to match session authority, preserve the bounded opaque `Logs` bytes, derive identity/name from the account rather than the request, reserve a separate atomic HMAC-hidden rate domain, validate the complete UUID/digest/byte/date/expiry row, and return that UUID as the mandatory stock `LogId`. Support logs never authorize gameplay. Do not guess replay identity because the request has no nonce. Keep action 141 acknowledgement-only until a trustworthy active payload call site replaces its contradictory decompiler-only sender.
- Treat schema-migration receipts and the singleton runner lease as complete durable authority. Read and validate every non-lock history row before planning; require exact receipt fields, canonical checksum, manifest-bound description, safe non-future application time, and nonnegative safe duration. Validate an existing lease before deciding that it expired, acquire an absent lease by singleton insert, replace an expired lease only against its complete prior snapshot, and never renew after its stored expiry even when no competitor has taken it yet.
- Keep cross-node scheduled jobs inside a renewable durable lease for their entire sweep. Validate the exact legacy or renewable row before expiry/ownership decisions; acquire absence by singleton insert, replace expiry only against the complete prior shape and values, renew only before the current deadline, serialize heartbeats with explicit ownership fences, and drain renewal before owner-scoped release. Provider calls and batched Player League, Squad War, subscription, and void-reconciliation transitions must re-prove ownership before their next durable mutation; an expired worker must never resume authority.
- Preserve legitimate negative Gold or WarBucks chargeback debt, but reject non-safe-integer rewards and arithmetic overflow.
- Keep the shared DynamoDB numeric wire adapters fail-closed across boot, public profile, experience/Arena leaderboards, durable inbox, Squad Event responses, Squad War member snapshots, and War Arena configuration. Integer fields may preserve the recovered truncating projection for legitimate finite fractional values such as calculated Army Power; Squad Event fraction fields must use the finite decimal-preserving adapter. A non-finite value or unsafe magnitude/projection must abort the response instead of becoming a believable zero or raw `NaN`/`Infinity` string. Durable legacy inbox numeric strings may be converted before validation, but malformed payloads must not become reward or placement zero. Validate the War Arena clock as a bounded safe Unix second before deriving its monthly ID/window. Do not introduce local numeric helpers that bypass these boundaries.
- Validate the duplicated indexed player profile mirrors after every shared player lookup and before account insertion, authentication/gameplay use, boot, public-profile, leaderboard, Arena-leaderboard, or rank projection. Root `id`, account name/type, sparse Facebook/Google Play/Game Center IDs, device token, league tier, Army Power, lifetime experience, squad points, and squad name must exactly match `DatabasePlayerDTO`. Provider DTO values must satisfy their recovered wire shapes even when both copies agree; disconnected Facebook must be exact numeric `-1`, disconnected Google Play/Game Center must be exact empty strings, and their sparse root keys must be absent rather than storing sentinel/empty index entries. A real lookup miss may remain `null`, but an existing damaged row must throw rather than masquerade as a miss. Full-profile saves must set/unset all duplicate roots in the same write; mismatch is damaged authority that must fail closed rather than letting MongoDB select/order/authenticate by one value while Unity displays another.
- Mirror equality is not numeric authority because matching `NaN` values pass `Object.is`. Require Army Power to be finite and nonnegative with a signed-client-int truncation result, lifetime Experience to be a nonnegative safe integer, and Squad Points to be a nonnegative signed-client integer at the shared profile boundary. Before ranked settlement, check Experience and Squad Point additions against those bounds, write their precomputed literal successors, and validate the canonical progression plus complete projected player account before its first write.
- Route every server-authored Reputation addition through the shared signed-client-int checked increment before publishing it. In particular, Squad-card withdrawal must validate all 5/15/45 normal and 30-point Buddy donor rewards before either participant write enters the atomic transfer; JavaScript-safe arithmetic alone is not sufficient for the recovered C# field.
- Preserve the recovered nested squad-card payload shape before deposit or withdrawal authority. `CardData.amount` and all numeric `BuddyCardData` members are Json.NET JSON numbers backed by C# `int`; reject missing, null, Boolean, string, array, fractional, non-finite, unsafe, or out-of-range values instead of coercing them. In particular, a missing Buddy amount must never normalize into one card, and only the exact numeric `-1` may select the no-secondary-weapon sentinel.
- Route every server-authored unit Elite-part addition through the shared signed-client-int checked reward helper before publishing its claim or receipt marker. Heroic Daily Missions and configured Event Assignments must not rely on JavaScript-safe arithmetic for the recovered `LevelManager.SavedArmySlots.parts` C# field.
- Validate public account-name and Player League identity shape even when every duplicated mirror agrees. Durable names must be nonempty, trimmed, control-free, and no longer than recovered `DatabasePlayer.maxPlayerName` 15; a nonempty `LeagueId` must make DatabasePlayer's first/final dash-segment parser derive the stored tier and division, while an empty placement fallback retains an empty stored division. Preserve opaque middle segments for unknown production-era IDs instead of inventing their schedule or allocation policy.
- Validate the complete six-field `SettingsManager.Settings` notification-consent snapshot at the shared profile boundary and before boot or update comparison. Only total legacy absence may derive the recovered LoadEmpty defaults; partial objects, unknown durable keys, and non-Boolean values must fail closed rather than silently re-enable a category or survive an unrelated profile write. Unknown request keys may remain forward-compatible but must never become stored authority.
- Validate persisted country and locale metadata at the shared player-profile boundary. Country must be either the exact empty new-account sentinel or an upper-case two-letter code, and locale must be a trimmed bounded language tag accepted by the request grammar. Normalize only new request input; malformed durable text must fail closed rather than becoming believable public, boot, or notification state through an unrelated update.
- Keep Firebase inbox wake-ups post-commit, data-only, and non-authoritative. Rebind every recipient/message identity to the complete MongoDB row and validated player envelope, require exact offline status, a nonempty mirrored device token, and the matching recovered notification-consent Boolean, and send only recovered action ID `2` for challenges or `90` for supported system inbox rows. Type-10 Squad rank changes use the explicit Squad-status consent and remain a generic inbox-refresh wake with no visible provider copy. Do not invent visible notification copy or borrow another consent category for type-27 direct messages; provider failure must never mark, remove, claim, or roll back the durable inbox row.
- Keep Firebase delivery retry-safe through the fixed-shape MongoDB recipient/message ledger. Reconcile every committed unread push-eligible inbox family missing an enqueue receipt, including type-1 Squad invitations and type-10 rank changes; share one exact family list with live wake policy so crash recovery cannot silently omit a supported producer. Validate the complete ledger batch before provider I/O, run delivery under the renewable cross-node lease, and compare-and-set each pending snapshot into delivered, suppressed, or bounded exponential retry state. Preserve the honest at-least-once boundary when provider acceptance is ambiguous; duplicate data-only wake-ups may reload the inbox but must never duplicate a gameplay reward.
- Retire an FCM device token only after a structured HTTP v1 response proves `UNREGISTERED`, or proves token-specific `INVALID_ARGUMENT` through the FCM error detail. Generic bad payload, sender/project/auth configuration, quota, service, internal, and transport failures must retain the token. Clear root and DTO mirrors with one exact-token compare-and-set so a delayed response cannot erase a newer action-13 registration.
- Treat action-13 FCM registration tokens as opaque credentials. Empty is the exact unregister sentinel; otherwise require a control-free, already-trimmed string of at most 4096 characters and preserve it byte-for-byte. Never trim or truncate malformed input into a different token, and enforce the same shape on both durable token mirrors before authentication, push selection, or unrelated profile mutation.
- Validate client-visible `PlayerStatus` and `SquadRank` as exact recovered enum members at the shared player-profile boundary. Unknown numeric values must fail before authentication, public projection, roster/rank use, or presence mutation; a heartbeat must not overwrite and thereby conceal damaged durable status authority.
- Validate DTO-only player scalars at the shared profile boundary before gameplay or publication: `Level` must select an exact recovered 4.9.5 rank row; Reputation must be a nonnegative signed-client integer; SendLogs must be exact 0/1 consent; awaiting-Squad state must be Boolean; RenameCount must remain in the source-backed 0-29 positive-price range; the legacy `DatabasePlayer.vipExpiration` fallback must be a nonnegative safe integer even when canonical progression owns a different current entitlement; LastAction must be a nonnegative signed-int Unix second; and the War Arena crown must use only empty/bronze/silver/gold/flawless with a signed-int expiry. Do not require the legacy/canonical VIP generations to mirror, but do not wait for final wire serialization or a narrower benefit/rename/boot handler to detect malformed dormant authority.
- Revalidate `DatabasePlayer.Level` as one exact recovered zero-based rank row inside normal weapon/unit/visual purchases, unit promotion, Heroic elite-part target selection, rank Army Power derivation, and Squad Buddy loadout creation before applying or publishing catalog data. `CardBuddy.CreateDataForCurrentPlayer` writes `currentLevel.index`, so never subtract one from the stored index. Never floor, clamp, replace non-finite input with rank zero, let `NaN` bypass a comparison, or accept an oversized integer as shop, tier, reward-pool, indexed-power, or Buddy authority; promotion error construction must keep corrupt rank failures inside their per-action RequestBuffer result, while an already-issued Black Market offer remains independent of a later rank argument because its durable offer row owns redemption eligibility.
- Keep public `PlayerStatus` and `LastAction` as one atomic presence snapshot. Action `29` must return the exact signed-int Unix second as `Time`, while valid exactly parsed heartbeats, account creation, and every backend-owned ranked admission/release transition update both durable fields together. Invalid status enums or malformed/missing transport may receive callback-safe response time but must never refresh durable presence; remote clients apply the recovered 2700-second freshness window to the stored pair.
- Validate the persisted Photon routing snapshot at the shared player-profile boundary as well as on action 140 input. Only the ten exact recovered region keys, integer 0-60000 millisecond pings, and numeric `None`/`Cellural`/`Wifi` enum members may survive; only total legacy absence may map to empty regions/`None`. Rebuild the public region JSON in canonical recovered order, and keep these client measurements as routing hints that never authorize matchmaking results, economy, or rewards.
- Validate core progression balances at the shared persisted read boundary, before `PlayerData` boot projection, and again on every progression mutation result before publication. Gold and WarBucks may be safe-integer chargeback debt; Tickets, Scraps, and level experience must be nonnegative safe integers, and malformed values must never bypass a price comparison or be silently serialized as zero.
- Validate the complete dog-tag tuple shape at every shared progression read/publication and before boot projection, regeneration, counting, spend, or refill. Callers that own an authoritative request/boot time must supply one captured server time for both snapshots and enforce a non-future cursor; context-free publishers must remain deterministic while still rejecting every malformed tuple value. Refill seconds and the divisible base cap must be positive safe integers; base seconds must remain between the normal cap and the exact two-tag VIP debt floor; malformed time must never freeze regeneration through min/max behavior or survive an unrelated replacement. Detect the legacy count-only schema by canonical-field absence, never by numeric validity, so corrupt canonical values cannot be silently reconstructed. Require its old count to be a nonnegative safe integer and deployment refill/cap policy to be exact positive safe integers with a safe product; never floor, clamp, or turn non-finite legacy authority into spendable energy.
- Validate progression revision and core-balance authority through the shared publication validator for ordinary writes and every custom full-document replacement, including external-entitlement transactions. Only an absent legacy revision may migrate to zero; every produced revision must advance monotonically as a nonnegative safe integer, and both snapshots must retain valid core balances before MongoDB can publish them. RequestBuffer may advance multiple internal steps in one atomic replacement.
- Keep all custom progression publishers on that shared successor proof: inbox, Google Play delivery/revalidation/reversal, missions, Instant Battle, rename, Squad creation/cards/departure, tutorial, Squad War, PvP, and operator integrity repair. A tutorial profile-only repair must omit the unchanged progression field; an operator repair must reload and validate the exact audited `updatedAt` snapshot before replacement.
- Require the exact supported embedded progression `schemaVersion` at the shared read and publication boundaries. A wholly absent progression may use the new-account fallback, but a missing, malformed, or future version inside a present document must fail closed until an explicit migration understands it.
- Keep reward-bearing inbox claims in the same MongoDB transaction as their terminal message marker. Validate their pure wallet/revision successor and bind the player replacement to the stored progression revision so a stale custom transaction cannot overwrite unrelated economy state.
- Validate every type-9, type-11, and type-23 reward message as one complete durable authority snapshot before producer insertion, inbox publication, claim, or replay. Require the exact type-specific payload, bounded control-free identities/text, safe creation time, non-challenge lifecycle, parseable bounded embedded arrays, and a terminal claim marker whose stored response exactly equals its server-authored Gold payload and is both read and ignored. Only canonical safe-integer strings are accepted for explicitly migrated DynamoDB numeric rows, and only the absent legacy idempotency key may remain optional; malformed or extra authority fields must fail closed. Return only newly inserted reward-row identities from the successful settlement transaction attempt, then publish their live inbox hints sequentially after commit; an idempotent upsert match must not announce a new reward, and live presentation must never replace action 91 as claim authority.
- Keep every recovered `DbAction` in exactly one executable disposition: direct handler, RequestBuffer-only, client response/local-only, retired, debug-disabled, raw route, open non-authoritative telemetry, or sentinel. Update `handlerlessActionDispositions`, its coverage test, and the backend documentation whenever a contract moves between dispositions; never hide an unclassified action behind generic error `90`.
- Keep action `92` an open acknowledgement because it may report login/boot parsing failures, but retain its recovered diagnostic fields only behind explicit deployment opt-in, an attached valid session, and exact durable `SendLogs=1` consent. Preserve the stock `DbAction` field-name collision under a server-only alias while `requestId` remains transport authority; validate one exact server-response or message-parse shape, bind stored bytes to a digest and TTL, use an independent atomic rate domain, and never let error telemetry authorize gameplay.
- Keep administrative diagnostic reads behind the independent admin Bearer credential. Bind canonical pagination cursors to the exact diagnostic family and optional player filter, revalidate every selected analytics/support-log/client-error row before returning any page, enforce expiry from application time instead of waiting for TTL cleanup, and never join diagnostic bytes into gameplay authority.
- Resolve the stock action-166 `LogId` only through an exact admin-authorized diagnostic UUID lookup. Validate the family and canonical UUID before storage access, exclude expired rows at application time, validate the complete selected family row, and return no diagnostic bytes for missing, expired, or damaged authority.
- Keep replay-video administration behind the independent admin Bearer credential. Bind canonical cursors to the optional player filter and exact descending `(createdAt, videoId)` tuple, reject expired or malformed action-3000 receipts before publication, expose only the reviewed receipt fields, and never dereference a player-supplied replay URL from the backend.
- Fail closed when authoritative combat evidence, live-event configuration, or source-backed balancing is unavailable.
- Add detailed English comments where recovered names are opaque or atomicity, replay, migration, or fallback decisions are not obvious.

## Change discipline

- Keep `npm run build` destructive only toward the generated `Server/dist` tree and clean that tree before TypeScript emission. Source renames/deletions must never leave stale compiled handlers or ghost `.test.js` files that can run alongside current sources and produce false verification counts.
- Run `npm test` from `Server` for backend changes.
- Update `Server/BACKEND_FEATURES.md` and `Server/README.md` when backend behavior or a documented gap changes.
- Keep unresolved backend gaps explicit. Do not mark a feature complete while its authoritative source or external provider integration is missing.
- The worktree may contain unrelated recovered-client changes. Stage and commit only files belonging to the current increment.
